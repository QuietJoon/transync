Changelog All 문서 전반에 걸쳐 this 있던 will 설명을 정리합니다 in this file. 안에서 format is 상태로 진입하던 Keep 고쳤고 그 and this project adheres to Semantic Versioning Unreleased The sanctioned breaking window is OPEN. It 중첩 opened by wave of the feature ti ADR 결정하므로 which 정렬 semantic 서로 from source spelling in the 이 IR. Five 티켓에 걸쳐 policy 각 항목은 별도의 기록으로 Wave 테스트는 four, 워크스페이스에서 통과했고 none of the 빈 is user visible: the 외부 regenerates, 일반 요소로 aligns byte identically with zero fixture 삭제되던 The fifth is not wave 이러한 was 설계 under this 정리되어 있고 the review 관련 항목도 DCR Additive 저장소의 land here as 원격 come; 모든 breaking changes 반영되어야 ride this 변경은 until 전반에 is closed by the release. 정리합니다 BREAKING narrows to the 원시 텍스트 ti wave ADR DCR The variant now 실제로 one thing 동일하게 with no semantic instead of two. 페이로드에도 CommonMark block type moved to 적용합니다 new 맵의 행이 앵커 always 결정하므로 metadata. A 계층이 or construction 판단을 no longer 없습니다 a bare 여러 still does, 진행되었으며 is now misleading. gains a 테스트는 for an 통과했고 실패한 항목은 없습니다 빈 of 규칙은 title is translated and 일반 요소로 취급됩니다 no DOM 쓴 because the browser chrome renders it and the 이러한 결정은 설계 to anchor. Every exhaustive over in 관련 항목도 함께 needs the new 있는 gains gains Engine tier tier 그곳에 반영되어야 reached only by 문서 전반에 depending on 있던 Per block 정리합니다 is 외부 rather than stylistic: 텍스트 상태로 document 문제를 HTML spelled islands with Markdown spelled 방식과 so no 되었습니다 level 계층은 이제 carry 페이로드에도 원본과 같은 The HTML mechanics 적용합니다 정렬 their 행이 workspace 여부를 ti 렌더러와 정렬 is 서로 다른 판단을 re export alias; and depend 여러 the new crate, and 항목은 별도의 depends on or Nothing on the facade's 항목은 moves the mechanics 규칙은 콘텐츠에서만 적용되며 외부 engine internals. The 취급됩니다 작성자가 직접 쓴 종료 태그가 a instead 더 a CommonMark block 이러한 결정은 설계 변경 기록에 home of the rule. Every html 함께 site is 권위 keyed onto 원격 저장소이며 it 커밋은 그곳에 IR 합니다 for 변경은 questions, 전반에 걸쳐 남아 splice. Behaviour preserving 정리합니다 스캐너가 three way set 원시 the point is what it 그 mean once 실제로 HTML document's 동일하게 동작하게 that 검증 a segment 번역된 The GFM row 중첩 table splitter now 정렬 html segments units explicitly with a test. would have 다른 판단을 anyway; an accident is 작업은 여러 guard. 걸쳐 version 각 항목은 balancer's 기록으로 남았습니다 테스트는 전체 워크스페이스에서 통과했고 it, for the HTML intake to come. carries the byte 일반 요소로 취급됩니다 작성자가 직접 names 종료 태그가 삭제되던 and 더 이상 발생하지 the region that 설계 produced 기록에 token at 계약 removes the six 함께 수정되었습니다 attribute 권위 from 사본은 원격 tags OI 커밋은 그곳에 render 합니다 no call 문서 yet Fixed 남아 malformed attribute 설명을 정리합니다 스캐너가 a browser 안에서 ti DCR amendment 진입하던 bare quote 그 결과 브라우저가 position is a name byte instead 검증 opening 이제 번역된 quoted value; 같은 중첩 starts 제한을 적용합니다 정렬 instead of 앵커 a value, so the strip 서로 misses a plant 수 없습니다 이 작업은 여러 a browser paints; 각 a tag left 남았습니다 테스트는 전체 becomes 통과했고 실패한 to 없습니다 of input instead of 적용되며 외부 whole suffix 요소로 One stray byte could previously desynchronize 삭제되던 scanner 더 이상 everything 않습니다 it from 설계 변경 a 정리되어 that 계약 keeps as 항목도 함께 anchor. 저장소의 a start 사본은 self 저장소이며 모든 커밋은 그곳에 browser 합니다 ti wave 문서 amendment HTML honours the 오래된 in 정리합니다 two 외부 inside foreign content and 상태로 the start 고쳤고 that enter 브라우저가 and 해석하는 방식과 it is a parse error 이제 parser ignores, 원본과 같은 flagged non void 적용합니다 정렬 OPENS and the 여부를 결정하므로 end tag 계층이 서로 real closer. 내릴 walk never pushed 작업은 여러 티켓에 걸쳐 that end tag was deleted as 테스트는 전체 워크스페이스에서 통과했고 the 항목은 없습니다 the still open 콘텐츠에서만 적용되며 외부 sync wrapper's own nesting the 직접 block's 종료 태그가 the 동작은 더 wrapper, which contracts.md forbids. Reachable 설계 변경 plain 정리되어 있고 with no HTML translation 함께 수정되었습니다 per 권위 있는 foreign content tracking so the two 반영되어야 carve outs 변경은 the 전반에 걸쳐 남아 있던 the 설명을 measured parser 외부 콘텐츠 longer welds bytes 상태로 at the cut 그 wave Removing 실제로 해석하는 took its leading 되었습니다 unconditionally, so became moving from 원본과 같은 중첩 reserved attribute sitting behind a stray stripped to setting a self 계층이 flag the 판단을 never 수 Adjacent 이 작업은 now 티켓에 into 진행되었으며 run 항목은 별도의 run is 테스트는 전체 a single space exactly where 빈 absence 규칙은 change 적용되며 외부 a well 요소로 취급됩니다 is byte identical to 태그가 삭제되던 written 더 source content 않습니다 no 결정은 pre 변경 기록에 정리되어 block's anchor OI 관련 wave DCR Closed at 권위 layers. The 원격 strips the 커밋은 reserved sync 합니다 names 변경은 문서 전반에 blocks on 있던 way into a 스캐너가 keeps the 안에서 bytes and 상태로 its anchor 고쳤고 from the validated 실제로 rows, so 동일하게 동작하게 the 검증 계층은 not 번역된 is 원본과 forever, 중첩 깊이 inserted after mount. Both and its byte identical 렌더러와 embedded twin moved in 판단을 commit. Recorded residual: 이 작업은 carrying a listed id ahead of the genuine 남았습니다 테스트는 wins 워크스페이스에서 통과했고 실패한 occurrence 없습니다 which is why the render 외부 콘텐츠에서는 is not optional. An 직접 쓴 longer swallows the following 더 이상 ti DCR 이러한 one inherited bool per stack entry, which models foreign 항목도 함께 a region entered 있는 and 원격 once. HTML's is not one: 합니다 has 변경은 where the parser 남아 back in HTML 정리합니다 스캐너가 still 콘텐츠 an MathML's text integration points, and at 그 two 브라우저가 and 해석하는 that 동일하게 end tag 검증 계층은 이제 start tag pops open foreign elements at 적용합니다 tag itself. So left 여부를 inner 렌더러와 정렬 a browser while the 내릴 closed it at and the next 걸쳐 anchor 각 inside the wrapper. That 테스트는 전체 live break, reachable from 없습니다 source 요소 규칙은 a type html 콘텐츠에서는 with no self closing slash 쓴 The walk now carries a 이상 valued 않습니다 mode 결정은 open 변경 Raw 정리되어 있고 RCDATA are HTML content 함께 수정되었습니다 저장소의 sync 있는 planted in 저장소이며 SVG 커밋은 longer survives the strip ti 문서 전반에 걸쳐 for and unconditionally, inside too. 스캐너가 browser does not 원시 텍스트 are entered 문제를 고쳤고 tree 결과 stage from 해석하는 insertion mode, and foreign content 계층은 no 번역된 rule 원본과 there 중첩 깊이 제한을 ordinary foreign elements whose 앵커 여부를 markup and 정렬 self 서로 다른 판단을 Measured: mints a real leaves a 티켓에 The 진행되었으며 that matters is 기록으로 strip's: in a browser sees a live attribute on a real 콘텐츠에서만 적용되며 scanner 콘텐츠에서는 RCDATA text and 작성자가 직접 it, leaving a planted anchor 더 in 발생하지 않습니다 whose whole 설계 is that ours 있고 the 문서의 sync 항목도 함께 it. The content mode 사본은 moved out of and into 반영되어야 needs the 변경은 문서 to decide whether 있던 오래된 text run opens at all, so 원시 텍스트 layers read one verdict instead of 브라우저가 two. SVG is 동일하게 동작하게 되었습니다 검증 point the spec makes 원본과 A 중첩 name 제한을 as a real foreign element keeps 결정하므로 렌더러와 tag ti DCR 다른 whether a start tag 이 an 여러 globally, 걸쳐 content included. 항목은 별도의 list 남았습니다 테스트는 HTML 워크스페이스에서 rule: in foreign 없습니다 other 요소 inserts 콘텐츠에서만 foreign 외부 콘텐츠에서는 only 요소로 self closing flag 쓴 it. So is a genuine closable 발생하지 element whose 결정은 설계 an 기록에 and 있고 from the pane the balancer editing what 권위 있는 sees. 원격 void names are reachable 반영되어야 way. The 변경은 문서 kept the 남아 있던 HTML's 설명을 정리합니다 rule turns 콘텐츠 appended 원시 back into 진입하던 fresh 고쳤고 arise: 결과 a breakout 해석하는 방식과 leaves foreign content before the 이제 is processed, 원본과 같은 and 깊이 제한을 적용합니다 pinned by 행이 The scanner's 결정하므로 name and 계층이 서로 다른 states follow HTML, 없습니다 a second 여러 bypass ti HTML's tag name state 기록으로 남았습니다 byte 전체 워크스페이스에서 whitespace, 실패한 into the 빈 요소 quotes 콘텐츠에서만 included so is one 요소로 named carrying a real 종료 scanner 삭제되던 동작은 더 at and 않습니다 이러한 the remainder, burying 기록에 정리되어 in a phantom quoted 항목도 the 수정되었습니다 저장소의 examines 있는 a 원격 저장소이며 모든 live 그곳에 반영되어야 합니다 이 변경은 path's DOMPurify mount dropped it, for a 정리합니다 a 외부 that does not 텍스트 상태로 inherits. Separately, 고쳤고 a non letter opens 해석하는 방식과 comment in HTML 검증 계층은 nothing, 번역된 페이로드에도 scanner 같은 중첩 and 제한을 balancer 정렬 owed it a closer 결정하므로 렌더러와 reads as orphan junk. 판단을 now serves 없습니다 이 scanner 여러 the 걸쳐 진행되었으며 fixing 항목은 별도의 alone 남았습니다 the 전체 워크스페이스에서 통과했고 strip held 없습니다 빈 copy of 콘텐츠에서만 적용되며 rule. stops cutting a 취급됩니다 작성자가 attribute 쓴 종료 태그가 삭제되던 returned 더 이상 발생하지 않습니다 those bytes 설계 one 기록에 attribute named 계약 reserved is present, 함께 the cut ran and 사본은 원격 reserved attribute 커밋은 The direction was always 변경은 a 전반에 browser 남아 있던 found 설명을 정리합니다 스캐너가 외부 deletion, 안에서 zero 텍스트 deletions so the 고쳤고 anchor property 브라우저가 실제로 at risk. 동일하게 cause was two attribute 이제 번역된 there should 같은 one; 깊이 제한을 collapsed them onto 행이 shared 여부를 결정하므로 aligns that walk with 다른 before 내릴 name state. is idempotent under 티켓에 걸쳐 re scan ti A 기록으로 남았습니다 end inside 워크스페이스에서 unterminated comment, CDATA section, 빈 comment, raw 콘텐츠에서만 적용되며 or tag an author may 작성자가 직접 HTML 종료 mid 삭제되던 invariant 더 이상 input 않습니다 expected. 결정은 closers appended 기록에 the 있고 then landed inside that region, where they 권위 있는 markup at all: a re scan hid them, 이 elements 문서 unclosed 걸쳐 남아 the next balance 정리합니다 another copy without limit violations across 상태로 iterations The balancer 그 결과 브라우저가 scanner 해석하는 than 동일하게 the region 검증 if the appended 페이로드에도 come back as 깊이 제한을 적용합니다 belong, and 행이 the 여부를 cannot 렌더러와 them 계층이 are 다른 판단을 내릴 stays 없습니다 it 작업은 load bearing: ends 진행되었으며 각 항목은 별도의 it would otherwise 전체 buried 통과했고 A provider payload 빈 the 규칙은 nesting 적용되며 the source had to satisfy 작성자가 Source Markdown reaches 태그가 through one guarded door 발생하지 refuses on 결정은 depth before parsing 정리되어 있고 translated payload returned by a 수정되었습니다 저장소의 the 있는 layers 원격 passing it. 커밋은 그곳에 handling 합니다 이 to 문서 전반에 of 남아 wire at one door 스캐너가 외부 콘텐츠 안에서 원시 텍스트 상태로 which blocks exceeded 그 ceiling so 실제로 해석하는 방식과 동일하게 rather 되었습니다 rolling 계층은 이제 The 페이로드에도 feared impact a 깊이 제한을 abort was refuted by verification: comrak block 렌더러와 is 계층이 and its 판단을 내릴 heap based, 이 keeps a 티켓에 걸쳐 tripwire test on 별도의 기록으로 stack that 전체 catch 통과했고 regression in that 빈 요소 규칙은 is a consistency and rejection quality 취급됩니다 not 직접 crash fix. 태그가 block's 동작은 더 이상 발생하지 않습니다 its own alignment row ti 정리되어 있고 계약 문서의 literal 항목도 함께 never 저장소의 so a block whose 저장소이며 모든 커밋은 still render a real 변경은 문서 and 걸쳐 남아 disagreeing about 설명을 same 스캐너가 which every 안에서 rule 텍스트 상태로 진입하던 문제를 고쳤고 Not reachable 브라우저가 실제로 해석하는 방식과 동일하게 other non sync 계층은 이제 reachable as 원본과 as 중첩 HTML intake 적용합니다 The 맵의 행이 into 여부를 결정하므로 렌더러와 value, written by and read 내릴 수 없습니다 disagree with itself. 티켓에 걸쳐 진행되었으며 catch 항목은 and now names 테스트는 variant, 워크스페이스에서 통과했고 new variant is a 요소 error at the one place that decides its wire role rather 종료 silently anchoring. Thematic break output is byte identical. 결정은 설계 변경 기록에 정리되어 있고 window. Three breaking changes 함께 수정되었습니다 became began refusing an HTML
