Changelog All notable changes to this project will be documented in this file. The format is based on Keep a Changelog and this project adheres to Semantic Versioning Unreleased The sanctioned breaking window is OPEN. It was opened by wave of the feature ti ADR DCR which splits semantic kind from source spelling in the block IR. Five breaking by policy changes ride the window. Wave contributes four, batched, and none of the four is user visible: the corpus regenerates, renders and aligns byte identically with zero fixture edits. The fifth is not wave unreachable was removed under this window by the review fix pass DCR Additive changes land here as they come; further breaking changes may ride this window until it is closed by the release. Changed BREAKING narrows to the unit variant ti wave ADR DCR The variant now means one thing content with no semantic instead of two. The CommonMark block type moved to the new where it was always spelling metadata. A pattern or construction naming no longer compiles; a bare pattern still does, and is now misleading. gains a variant for an HTML document's and an explicit of a title is translated and aligned but has no DOM anchor, because the browser chrome renders it and the pane has nothing to anchor. Every exhaustive over in a consumer's tree needs the new arm. gains gains Engine tier tier c types, reached only by a consumer depending on directly. Per block spelling is required rather than stylistic: one Markdown document interleaves HTML spelled islands with Markdown spelled blocks, so no document level bit can carry the axis. Changed The HTML mechanics moved to their own workspace member, ti wave DCR is gone with no re export alias; and depend on the new crate, and no longer depends on or Nothing on the facade's surface moves the mechanics were always tier c engine internals. The publication roster is seven members. takes a instead of a CommonMark block type is the one surviving home of the rule. Every html dispatch site is re keyed onto the axis it meant for IR questions, for unit questions, for the layer splice. Behaviour preserving today, by three way set identity; the point is what it will mean once an HTML document's is a that ships a segment array. The GFM row window table splitter now excludes html segments units explicitly with a test. would have refused one anyway; an accident is not a guard. Workspace version Added the balancer's stack walk lifted out from under it, for the HTML intake to come. carries the byte span the scanner already computed; names comments, CDATA sections and bogus comments, including the region that previously produced no token at all. removes the six reserved sync attribute names from element open tags OI route c render half; no call site yet Fixed tokenizes malformed attribute regions the way a browser does ti DCR amendment a bare quote in attribute name position is a name byte instead of opening a phantom quoted value; a stray starts an attribute named instead of opening a value, so the strip neither misses a plant behind one nor deletes text a browser paints; and a tag left unterminated at EOF becomes a spanning to end of input instead of a silent whole suffix abort. One stray byte could previously desynchronize the scanner and hide everything after it from and including a planted that DOMPurify keeps as a live anchor. read a start tag's self closing the way a browser does ti wave DCR amendment HTML honours the flag in exactly two places inside foreign content and on the start tags that enter it and everywhere else it is a parse error the parser ignores, so a flagged non void tag now OPENS and the author's matching end tag is its real closer. The walk never pushed such a tag, so that end tag was deleted as an orphan, and on the render path the still open fragment consumed the sync wrapper's own nesting the next block's anchor inside the html block's wrapper, which contracts.md forbids. Reachable from a plain author written with no HTML translation involved. gained per stack entry foreign content tracking so the two real carve outs keep the XML reading, and gained the four measured parser voids no longer welds bytes together at the cut ti wave Removing an attribute took its leading whitespace unconditionally, so became moving from to and a reserved attribute sitting behind a stray stripped to setting a self closing flag the source never carried. Adjacent cuts are now coalesced into one run and the run is replaced by a single space exactly where its absence would change the parse; a well formed strip is byte identical to before. A written into source content can no longer pre claim a real block's anchor OI ti wave DCR Closed at both layers. The renderer strips the six reserved sync attribute names from raw HTML blocks on the way into a pane keeps the author's bytes and takes its anchor set from the validated alignment rows, so an element the map does not claim is inert forever, including one inserted after mount. Both and its byte identical CLI embedded twin moved in one commit. Recorded residual: an impostor carrying a listed id ahead of the genuine anchor still wins the engine's first occurrence lookup, which is why the render side strip is not optional. An wrapped no longer swallows the following block's anchor ti DCR carried one inherited bool per stack entry, which models foreign content as a region entered once and left once. HTML's is not one: it has islands where the parser is back in HTML content while still inside an MathML's text integration points, and at exactly two values and exits that no end tag marks a breakout start tag pops open foreign elements at the tag itself. So left that inner open in a browser while the walk closed it at and the next block's anchor mounted inside the wrapper. That is a live break, reachable from untrusted source Markdown through a type html block with no self closing slash involved. The walk now carries a three valued content mode per open element. Raw text and RCDATA are HTML content states, and a sync attribute planted in an SVG no longer survives the strip ti DCR entered raw for and unconditionally, inside too. A browser does not those states are entered by the tree construction stage from the insertion mode, and foreign content has no such rule so there all four are ordinary foreign elements whose contents are markup and whose self closing is honoured. Measured: mints a real leaves a sibling. The consequence that matters is the strip's: in a browser sees a live attribute on a real while the scanner saw RCDATA text and walked past it, leaving a planted anchor standing in a pane whose whole premise is that ours are the only sync attributes in it. The content mode stack moved out of and into which needs the same answer to decide whether a raw text run opens at all, so the two layers read one verdict instead of deriving two. SVG is now the HTML integration point the spec makes it. A void name used as a real foreign element keeps its end tag ti DCR decided whether a start tag opens an element globally, foreign content included. HTML's void list is an HTML content rule: in foreign content other start inserts a foreign element and only the self closing flag pops it. So is a genuine closable SVG element whose was classified an orphan and deleted from the pane the balancer editing what the reader sees. Thirteen void names are reachable that way. The hazard that kept the rule global HTML's end tag rule turns an appended one back into a fresh cannot arise: is a breakout tag, so leaves foreign content before the tag is processed, as do and That set is pinned by test. The scanner's tag name and end tag open states follow HTML, closing a second strip bypass ti HTML's tag name state consumes every byte up to whitespace, or into the element name quotes and included so is one element named carrying a real The scanner ended the name at and attribute walked the remainder, burying that plant in a phantom quoted value the strip never examines as a name, so a live anchor survived; only the pane path's DOMPurify mount dropped it, for a reason a consumer that does not sanitize never inherits. Separately, before a non letter opens a bogus comment in HTML and mints nothing, where the scanner emitted an and the balancer then owed it a closer a browser reads as orphan junk. One now serves both the scanner and the strip, because fixing the tokenizer alone left the hole open the strip held its own copy of the boundary rule. stops cutting a non reserved attribute across a stray ti returned where a browser folds those bytes into one junk attribute named nothing reserved is present, yet the cut ran and a non reserved attribute moved. The direction was always safe a case browser equivalence probe found equal, that one over deletion, and zero under deletions so the impostor anchor property was never at risk. The cause was two attribute walks where there should be one; DCR had collapsed them onto the shared and this aligns that walk with HTML's before attribute name state. is idempotent under its own re scan ti A fragment can end inside an unterminated comment, CDATA section, bogus comment, raw text run or tag an author may close an HTML block mid and invariant says that input is expected. The closers appended at the end then landed inside that region, where they are not markup at all: a re scan hid them, the elements read unclosed again, and the next balance appended another copy without limit violations across fuzz iterations The balancer now asks the scanner rather than classifying the region itself if the appended bytes come back as close tokens they belong, and if the scanner cannot see them they are dropped. The append stays where it is load bearing: ends the raw text run it would otherwise be buried in. A provider payload meets the same nesting ceiling the source had to satisfy ti Source Markdown reaches comrak through one guarded door which refuses on nesting depth before parsing but a translated payload returned by a provider reached the validation layers without passing it. Untrusted input handling now applies to both directions of the wire at one door in before any layer parses, and identifies which blocks exceeded the ceiling so a rollback names them rather than rolling back nothing. The originally feared impact a stack overflow abort was refuted by verification: comrak block parse is iterative and its walkers are heap based, and keeps a deliberate level tripwire test on a KiB stack that would catch a regression in that dependency. The fix is a consistency and rejection quality change, not a crash fix. A block's DOM anchor is decided by its own alignment row ti DCR enforced with a literal test and never consulted so a block whose row says could still render a real the row and the DOM disagreeing about the same block, which every anchor rule in rests on not happening. Not reachable today nothing mints the one other non sync kind, and reachable as soon as the HTML intake does. The omission moved into keyed on one value, written by and read at emission, cannot disagree with itself. also lost its catch all and now names every variant, so a new variant is a compile error at the one place that decides its wire role rather than silently anchoring. Thematic break output is byte identical. This release closes the sanctioned breaking window. Three breaking changes rode it: became began refusing an HTML
