# Project Status

## Active Baseline
- Design baseline ID: **`BL-2026-07-B`** (issued 2026-07-13) — see `docs/project/design-baseline-2026-07.md`
- Workspace version: **`0.5.0-dev`** — the sanctioned **v0.5.0 breaking window is open** (`ff788f7`, 2026-08-24; the root `Cargo.toml`'s `[workspace.package].version` is the one canonical record, and this bullet follows it rather than leading it). It was **opened by ti `490d97` wave 2** (ADR-0025 / DCR-0034: the block IR splits semantic kind from source spelling), whose **four** breaking-by-policy changes ride it batched — `Block.spelling` added, `Document.format` added, `BlockKind::Html` narrowed to a unit variant, `BlockKind::Title` added — and **none of the four is user-visible**: the corpus regenerates, renders and aligns byte-identically with **zero fixture edits**. Four is *this wave's* contribution, not the window's total: the window also carries `transync-openai`'s unreachable `ProviderError::RateLimited` removal (`8d4efda`, DCR-0040 §3), which is the review-0009 fix pass's, so the running count is **five**. Nothing is released from an open window, so this is *not* a released version — the two bullets under it are release history, and the most recent release is **v0.4.0**.
- Prior release: **v0.3.0** — **released 2026-08-07** (annotated tag `v0.3.0` — **archived, not resolvable**: `git tag -l` does not list it in this clone (it lists only `v0.4.0`, created 2026-08-20), because the tag object and the commit it named went with the 2026-08-10 history restart — `docs/project/git-history-loss-2026-08-10.md`; the CHANGELOG's `[0.3.0]` entry is the readable record of what was released, and the `v0.1.0`/`v0.2.0`/`v0.3.0` tag objects themselves survive — readable, not merely archived as hashes — in the salvaged store at `/Volumes/Common/git-backup/transync-broken-git-20260810`, e.g. `git --git-dir=/Volumes/Common/git-backup/transync-broken-git-20260810 show v0.3.0`); the sanctioned 0.3.0 breaking window is **used and closed** (three curated-surface changes rode it — `CacheKey.instruction_hash`, `transync::VALIDATION_REPORT_SCHEMA_VERSION`, `ListTopologyEntry.depth: u32` — each in §0/`public_surface.rs` lockstep)
- Last release: **v0.4.0** — **released 2026-08-20**, **annotated tag `v0.4.0` on the release-prep commit `6fa4e88`** (tagged the same day, after the release; the owner reversed the 2026-08-13 ships-untagged decision because consumers need a ref to depend on). The tag makes no claim about ancestry — this history's root is the 2026-08-17 restart commit, so `git log v0.4.0` does not show the development that produced the release; it names which tree is v0.4.0, and the `[0.4.0]` CHANGELOG entry says what shipped. The sanctioned 0.4.0 breaking window is **used and closed** — three changes rode it: `TranslationOutput::translated_markdown` → `translated_document`, `transync translate` refusing an HTML document instead of corrupting it, and `BlockKind::CodeBlock` gaining `fenced: bool` (ti `457e51`). The next breaking change needs a new window. Release tallies: workspace **1066 passed / 0 failed / 5 ignored**, CLI stub **208 passed**, browser suite **33 passed**, wasm `raw=1,648,009B gzip=675,725B` within budget. The live-endpoint gate (OI-0030) **PASSED on 2026-08-20** — 2/2 live round-trips against `gpt-4o-mini` (Chat Completions) and `gpt-5-mini` (Responses), recorded in the `[0.4.0]` CHANGELOG entry. The `anthropic` leg is still unrun and remains a standing gap
- Baseline status: **approved** (steady state)
- Object store: this clone's git history begins at root commit **`59ce8df`** (2026-08-17) — the **second** restart after object-store loss, the first having been 2026-08-10. A file-sync client writing conflict copies inside `.git` cost 33 objects; 17 were recovered by hash from sibling stores, 16 were not, and of the 23 commits still reachable exactly one had a tree that walked to completion. The tracked tree came through byte-identical (342/342) and the suites were green on both sides of the restart. The record — with the cause, its physical evidence, the recovery runbook and the archive locations — is `docs/project/git-history-loss-2026-08-17.md`; the earlier event is `docs/project/git-history-loss-2026-08-10.md`. **Every commit hash in this file that predates the restart names a commit that is not in this object store** — that was *every* hash when this bullet was written, and the sentence said so; it stopped being every hash as the tree accumulated work, so read it as the rule it is rather than as a count. Hashes from 2026-08-17 onward (the wave-0/1/2 commits, `ff788f7`, the review-0009 fix pass, `v0.4.0`'s `6fa4e88`) resolve normally. The pre-restart ones are dated records and stand as written; read the prose beside each hash, and reach for the archived stores named in the 2026-08-17 record only when a diff is genuinely needed.
- Analysis bundles: `docs/investigation/` and `reviews/` are **untracked** (owner, 2026-08-08) — present on disk and read as sources, but not version-controlled. This reverses the 2026-08-06 decision recorded in the wave summary below, which committed `docs/investigation/` in `727d3bc`: the bundle is regenerated from the code rather than authored, so each regeneration produced a diff that carried no decision. `Cargo.lock` stays committed (`6cf4164`, oi-0020) — that half is unaffected.
- Latest wave: **the road from v0.3.0 through the open v0.5.0 window to the review-0009 gate** (landed 2026-08-09 → 2026-09-01) — four arcs in one. (1) The six commissioned post-0.3.0 feature tickets all landed 2026-08-09/10: the oversize row-window splitter (DCR-0026), section-aware batching (DCR-0027), the disk-backed cache (DCR-0028 / ADR-0021), the sync-engine pass (`d3acc3`, closing OI-0015 and OI-0024), `transync serve` (`b791d6`, closing STUB-061) and `transync-anthropic` (DCR-0029) — the roadmap action below records what each one changed. (2) **v0.4.0 released 2026-08-20**, annotated tag on `6fa4e88`, live gate PASS (see the release bullets above). (3) The HTML→HTML feature (ti `490d97`) opened: **wave 0** lifted the HTML mechanics into `crates/transync-html` (DCR-0032, 2026-08-20 → 2026-08-22, making the publication roster seven members plus `transync-wasm`), **wave 1** closed OI-0035 at both layers (DCR-0033, code 2026-08-22 / record 2026-08-23), and **wave 2 — the IR split — was in flight from 2026-08-24 and LANDED 2026-09-01** (DCR-0034): it opened the sanctioned v0.5.0 breaking window on 2026-08-24 (`ff788f7`, workspace version `0.5.0-dev`) and has already landed **both** new axes — `Spelling` as a sibling to `BlockKind` on every block, and `SourceFormat` as `Document.format` on every document (`20b78b7` adds the types, `258ba01` puts them on the IR) — plus `BlockKind::Html` dropping the CommonMark type (`954711a`), `BlockKind::Title` with the two explicit arms the compiler could not ask for (`8b556e7`), and the re-key of every html dispatch site onto the axis it actually meant — `Spelling::Html`, `InputMode::HtmlSegments`, `constraints.html` (`1ed72d3`, behaviour-preserving today by three-way set identity). **Wave 3 — the HTML intake and the identity round-trip — LANDED 2026-09-03** (DCR-0035): `transync-syntax::intake::html` produces the same `Document` from an HTML document that `parser` produces from Markdown, and `regenerate(parse, &empty)` is byte-identical over the SCN-16 fixture, a messy marketing page, an unclosed docs fragment and the CRLF/lone-CR/BOM/NUL/doctype-only/plain-text edge documents. **Wave 4 — the layer-6 twin — LANDED 2026-09-03** (DCR-0036): `validate::full_rescan_html` re-reads a regenerated HTML document with the same scanner the splice used and the same segmenter the intake used, and `pipeline::finalize`'s `layer6_gate` dispatches on `Document.format`. **The standing hard rule is discharged** — the twin exists, so wave 5 proceeded. **Wave 5 — the entry point — LANDED 2026-09-03** (DCR-0037): `TranslateOptions.input_format` routes a declared-HTML source through the HTML intake and out through the wave-4 gate, and `translate()` returns translated HTML with a real alignment map. **Wave 6 — panes, the wire, the CLI — LANDED 2026-09-03** (DCR-0038): the OPERATOR surface now exists — `transync translate --input-format html --out-dir out/` writes an anchor-free `out.html` plus a bundle whose panes mount and sync by block id, and the alignment wire is schema **1.3.0**. **Wave 7 — the browser gate and closure — LANDED 2026-09-04** (DCR-0039), and with it the whole feature: all eight waves 0–7 are in. **Wave 2's records task LANDED 2026-09-01** — the wave-2 plan's Task 8 delivered ADR-0025, DCR-0034 and the contracts / rough-schema / module-map / `CLAUDE.md` / CHANGELOG edits, so wave 2 now has the same record shape every other wave has. (4) **The review-0009 decision gate ran 2026-08-25 → 2026-08-29**, the first gate whose fix pass rode the open window. **Twenty-six findings were fixed** across three commits: `88964df` (eleven — five stale documentation claims and six gates that could not fail, among them `install-hooks.sh` deleting a developer's own untracked `pre-commit` on a basename match), `8d4efda` (six, one breaking — `transync_html::splice`'s phase-A/phase-B agreement was a `debug_assert` whose *release* failure mode was silent: a short phase B dropped a translation's tail and a long one shifted every later replacement onto the wrong node, both returning `Ok`; it is a real `Err` on the existing splice-failure degrade path now, and `transync_openai::error::ProviderError::RateLimited`, dead and `pub` in a published crate with no `#[non_exhaustive]`, was **removed under the v0.5.0 window**) and `69701cd` (nine — `serve`'s response half gains a total 60 s `RESPONSE_TIMEOUT`, closing a slot-exhaustion path that needed no malformed byte, and five head/host/map parsers stop accepting what they cannot mean). The three shipped behaviour changes are recorded in **DCR-0040** (`5ff6d17`), which also amended ADR-0002 and corrected `contracts.md` §4a to describe the probe `mountSync` actually has. The **thirty-five findings routed `track`, plus R0009-0014**, landed as ten open-issue entries — **OI-0039..OI-0048** — with ten paired `docs/backlog.md` entries (`e663422`), clustered by theme rather than transcribed, and deliberately **not** filed as TicGit tickets: a tracked finding is not-being-worked-now, so filing one would hand `ti-pick-next` a task nobody chose to start.
- Previous wave: **Review-0001 hardening arc + v0.3.0 release** (landed 2026-08-06/07, waves C–H, ~60 commits `a713190`..the release tag) — `reviews/0001.md`'s 52 findings were triaged into 20 tickets; resolving them surfaced 26 follow-ups; **all 46 closed**, each with an implementer, an independent reviewer (sonnet or fable per risk), and a resolution comment on the ticket. Highlights, by layer. **Provider:** Retry-After parses the HTTP-date form; every non-success Responses status (`failed`/`cancelled`/`queued`/`in_progress`) and Chat `finish_reason` (`length`, `content_filter`) is diagnosed explicitly instead of falling through as a malformed body; the provider's error `code` is capped like its `message`; the adapter's HTTP surface is resolved **once at construction** with a per-instance `with_api` pin, `Api` at the crate root with a `FromStr` sharing ONE parser with the env path; `transync-openai` joined the rustdoc gate. **Batching/prompt:** `FIXED_ENVELOPE_TOKENS` deleted — the per-batch instruction reserve is priced from the **serialized envelope the prompt actually ships** (`InstructionVariant`), with the html clause reserved on the document-level fact so ADR-0009's same-budget-per-round invariant holds; a compiled-then-mutated profile can no longer stack its sections (both mutation paths rewind); the shipped default profile carries **zero active glossary entries** (the Korean examples are commented documentation). **Identity (0.3.0 window):** `CacheKey` gained `instruction_hash` — cache identity covers exactly the prompt bytes the unit was translated under; `VALIDATION_REPORT_SCHEMA_VERSION` became a facade constant (tier a, symmetric with `ALIGNMENT_SCHEMA_VERSION`); `ListTopologyEntry.depth` widened to `u32`, deleting the saturation ceiling. **Parser robustness:** NUL is normalized at intake (OI-0034) **and refused in translated payloads** (validation failure → verbatim retry → fallback), so `out.md` never contains a NUL across fresh and cached paths; a depth-128 pre-scan plus an iterative topology walker ended the stack-overflow **abort** a 20 KB hostile input could cause (measured: the abort was our recursive walker, not comrak — comrak's block parse is iterative); inline nesting is pinned by subprocess probes that turn a child abort into a red test. **CLI/output:** a tracing subscriber (CLI-only dependency) surfaces the library's warnings with `--quiet`/`--verbose` and two double-print hazards closed; prompt overrides are template-diagnosed at the translate boundary and `auto` is whitespace-tolerant; concurrent publishes into one destination **take turns** (`PublishLock`, DCR-0021) and the crash-safe-not-atomic reality plus unreclaimed-temp policy are documented; bundles carry a real `<title>` (flag > first H1 > fallback) and the target-language `lang`; an opt-in `--strict-csp` confines a bundle to its own directory (headless-verified: tracking pixel blocked). **Repo/meta:** MSRV floor is the proven-exact **1.88** (CLI keeps its honest 1.89 pin); the publish roster and version requirements are **welded by test**; the pre-commit hook runs the rustdoc gate from one shared crate list; `install-hooks.sh` cannot clobber a foreign `core.hooksPath`; `.gitignore` covers the `.env` family; `Cargo.lock` and the `docs/investigation/` + `reviews/` bundles are committed by owner decision (`6cf4164`, `727d3bc`). Ten controller decisions were made under owner delegation and are flagged for ratification in the tickets (`0d8277`'s widening recorded veto-open-until-tag in §0). Verified at the release commit: workspace **583 passed / 0 failed / 3 ignored** (pre-arc 408), CLI stub **77/0/0**, Playwright **16**, `scripts/smoke.sh` exit 0 (wasm raw 1,641,070 / gzip 672,629 within budget), `cargo publish --dry-run --workspace` green, `resp-translator` check green with no migration; alignment schema still `1.2.0`, `VALIDATION_SCHEMA_VERSION` still `2`, both `sync.js` copies moved **together** (4f8dde) with drift tests green. The live gate (OI-0030) was **triggered but not run** — no key in the release session; the CHANGELOG's 0.3.0 preamble records it and the post-tag action below owns it. `docs/backlog.md` was rewritten: Type 1 = the six commissioned post-0.3.0 roadmap tickets, Type 2 = one open design question (`40e2a5`) + ten owner-deferred product items, Type 3 = gated items with unlock pointers.
- Prior wave: **Track C wasm render+edit demo** (landed 2026-08-05, 6 commits `94d7e39`..`1d04135`) — **Track C proper**, the browser integration OI-0028 / DCR-0017 deferred after delivering only the compile path. Recorded in **ADR-0019** (the decision) and **DCR-0020** (the implementation); OI-0028 stays **RESOLVED** and gains a dated "Track C proper landed" note, and **OI-0020 stays OPEN by owner decision** (pin over lock). Everything is **additive** — the frozen set (`web/index.html`, `web/js/sync.js`, `crates/transync-cli/web/`, all of `crates/transync-syntax`, and `crates/transync/tests/public_surface.rs`) has an **empty** `git diff 94d7e39..HEAD`, which is the wave's proof it added a layer rather than moving one. **A sixth workspace member, `crates/transync-wasm`** (`cdylib` + `rlib`, `publish = false`), wraps the syntax layer for the browser and depends on **`transync-syntax` alone** — a double rationale, both halves load-bearing: the crate charter, and the `getrandom` trap (`transync-core`'s `wasm32` tree reaches getrandom via tokio/tiktoken; syntax's does not, so a widening edge breaks the *link*, late). All logic lives in `engine` with zero wasm-bindgen types, so the whole surface is **host-testable** and the three `#[wasm_bindgen]` wrappers stay verifiable by inspection. **The boundary is JSON strings in, one JSON string out** (approach A1 — no `serde-wasm-bindgen`, no new dependency): `render_pair` (view) reproduces the CLI's pane fragments **byte-identically**, `rebuild` (edit) runs the *whole* local loop — regen → alignment map → render — and deliberately never reuses the fetched map, because the html/skipped render arms slice `target_range` **bytes** that an edit has shifted (probe-confirmed silent mis-render). `schema_version()` is the lockstep leg, pinned host-side to `ALIGNMENT_SCHEMA_VERSION` so a blob can never disagree with the crate it was built from. **Edit model:** per-block payloads (absence *is* regen's fallback contract), statuses **preserved not rewritten** across edits (the status records *LLM* provenance), html blocks **read-only** (their payload is a segment array, not Markdown — ADR-0018), and unknown ids **rejected** rather than dropped. Because identity comes from the source parse the demo never edits, the block set is structurally preserved and the ID-stability wall (invariant 8) is never approached. **Build pipeline:** `scripts/build-wasm.sh` drives wasm-pack `--no-opt` plus an **explicitly resolved** binaryen `wasm-opt` — wasm-pack's cached binaryen **117 rejects** current rustc output (`--enable-bulk-memory`), and wasm-pack silently ignores its profile-metadata key under a custom profile, so both traps are handled by construction; every prereq failure is **loud**, never a skip, and each was tested rather than assumed. **Size budget raw ≤ 1,950,000 B / gzip ≤ 810,000 B, enforced by the script**, against a measured **1,739,791 / 718,136** (1.66 MiB / 701 KiB). The spec's old 1,400,000 / 500,000 was **wrong input corrected by measurement**, not size creep: the exploration probe compiled **view mode only**, and the attribution — control 1,148,050 → +42,127 JSON boundary → +435,770 edit-mode reserialization → +113,844 `rlib` fat-LTO scope (ticketed `92ac61b9`) → 1,739,791 — lives durably in DCR-0020. **The CLI bundle deliberately carries no wasm** (~41× the entire 42 KB JS payload, for a capability the bundle's rendered HTML already provides); `HTML_BUNDLE_ENTRIES` and the ADR-0006 six-file contract are untouched. **The demo** (`web/demo-wasm.html` + `web/js/wasm-demo.js`, artifacts in the gitignored `web/wasm/`) mounts through the shells' fail-closed DOMPurify path with **three** independent refusal gates (DOMPurify absent, module-vs-demo schema, fetched-map schema), slices payloads by **UTF-8 bytes**, and restores scroll and `<details>` state *between* the `innerHTML` writes and `mountSync` (restoring after the listeners are live reads as a user scroll — measured 120/140 → 131/131 versus an exact 347 → 347). **Playwright grew 8/8 → 12/12**, the headline being a **CLI-vs-browser parity pin** promoting the probe's byte-identity to a standing gate (structural in the browser because DOMPurify may normalize; pre-sanitize byte parity is proven host-side by the two-mode agreement tests, and the test names them). Verified: fmt/clippy clean, widened wasm gate green for **both** crates, `cargo test --workspace -- --test-threads=4` **393 passed / 0 failed / 3 ignored** (pre-wave 385; the delta is exactly the eight new `transync-wasm` host tests — zero removals, zero adaptations), CLI stub suite 34, `scripts/smoke.sh` green incl. the rustdoc gate (now covering `transync-wasm`) and the size budget, Playwright **12/12**, `resp-translator` `cargo check --workspace` green with no migration; alignment schema still `1.2.0`, `VALIDATION_SCHEMA_VERSION` still `2`, `CacheKey` and both `sync.js` copies untouched. ADR-0003 gained a dated six-member amendment. One new ticket filed and left open — **`0c6a5c8b`**, the demo's non-fatal error-strip path, which is implemented and smoke-proven but has no committed Playwright coverage — joining `92ac61b9`. *(Resolution note, 2026-08-05: both are closed — `0c6a5c8b` by `eab1111` (Playwright test `e` pins the strip from a ghost `source_block_id`; suite 12/12 → **13/13**) and `92ac61b9` by `223535b` (`crate-type = ["cdylib"]` alone, re-measured −101,228 B raw / −46,758 B gzip, budget re-tightened to 1,840,000 / 760,000). A third demo ticket filed after the wave against DCR-0020, **`2a276cb4`** (topology-changing payload edits degraded unannounced), is closed by `cc9fb8f`: `rebuild` now returns a nullable `structure_warning` and the demo shows it non-fatally.)* *(Resolution note, 2026-08-06: this wave's **pin-over-lock** decision was reversed the next day — `Cargo.lock` is committed (`6cf4164`, repo-local `!Cargo.lock` un-ignore) and **OI-0020 is RESOLVED**. The `=0.2.126` pin stays: a lock binds only the resolves that read it, and no lock can pin `wasm-bindgen-cli`. ADR-0019 carries a dated amendment.)*
- Earlier wave: **OI-0008 + OI-0033 internal-quality wave** (landed 2026-08-05, 11 commits `b1c3c7f`..`73fd331`) — **OI-0008 and OI-0033 are both RESOLVED**, and **OI-0034** was filed. First wave after the v0.2.0 tag; everything is internal, and `crates/transync/tests/public_surface.rs` is green and **byte-identical to the tag** — that diff being empty is the wave's proof it never touched the curated surface. The behavior-change budget was **exactly two** items. **(1) Lone-CR sources now slice correctly** (OI-0033, posture **Support**): `LineOffsets::new` gained one arm — a `\r` whose next byte is not `\n` — naming comrak's own `strings::is_line_end_char` as the authority. Probe-verified that VT/FF/NEL/U+2028/U+2029/NUL are **not** line endings (so `str::lines()` and Unicode line-break APIs are inadmissible), and that CR-aware offsets are **byte-identical** for every LF/CRLF document, making the fix behavior-preserving by construction on the whole current corpus. The two duplicated line-end-cap derivations collapsed into one terminator-aware helper; fixtures are inline `&str` constants, never checked-in `.md` files (no `.gitattributes` protects CR bytes). A defensive parse warning now fires when a translatable **list item** has an empty range at a non-degenerate position, and **DCR-0017's Guard-1 residual (a) is closed** — asserted directly, not inferred, so both DCR-0017's note and the `full_reparse.rs` comment now read "void unqualified". **(2) `ValidationReport.total_retries` counts content retries only**: the old `attempts.len() - 1` also counted batch-schema-fault rounds and rejected cache-hit re-validations, contradicting doc text in *both* `pipeline.rs` and `validate.rs`; no test pinned the old value and the two discriminating new tests were RED first. A `validation-report.json` from a run with a batch fault or a rejected cache hit now reports a **lower** `total_retries`; `batch_schema_faults` and `provider_retries` are unchanged, and the three now sum without double-counting. Everything else is behavior-preserving motion, each task carrying its own proof (line-multiset diffs, identical test-name multisets, byte-identical output trees). **Retry/fallback (R0001-0057 — from the removed `reviews/reviewed/0001.md`, not `reviews/0001.md`; see `reviews/README.md`)** became `pipeline/policy.rs`, a **pure** decision core (approach A1 — no `tokio`, no `Cache`, no `Translator`, nothing `async`), so twelve decision tests now run with no mock translator; extracting it surfaced that `max_per_batch_provider_retries` is charged per **dispatch round** despite its per-batch name — preserved verbatim and ticketed (`294ddabd`). **Six concern splits:** `run_pipeline` → `pipeline/{dispatch,finalize,report}`; `unit.rs` → `structure{,/labels}` (removing the inversion where `validate::per_kind` imported its oracle from the unit builder) plus `unit/{payload,budget}`; the OpenAI client → `client/{classify,transport,endpoint,dispatch,chat,responses}` + `tokenizer`, behind ONE surface abstraction shared by translation and glossary extraction, with the previously **untested** HTTP-status classification table pinned tests-first (29 unresolved-name errors as the RED step) before being moved character-for-character; `translate_cmd` → `translate_cmd/{args,input,provider,publish,report}` behind an `execute(args, reporter)` seam that collapses fifteen exit-code sites into one table and one file-set builder that replaces two duplicated `OutputTarget` arms; and the parser walker restructured onto a `WalkState` across `parser/{options,classify,emit,sections}`, deleting three `#[allow(clippy::too_many_arguments)]` and the `emit_here!` macro. **Subtractive slice:** `Document::hierarchy` (a whole extra pass with allocations, read by nothing) and `Block::parent_id` (provably always `None`) **deleted**, simplifying **six** tautological filter sites — the exploration digest listed five; `walk.rs` was the sixth — plus one home each for `emit`, the section-scope algorithm, and the `heading_level` mapping. **The wire did not move:** `AlignmentBlock.parent_id` stays at schema 1.2.0 as an always-null RESERVED field, proven by regenerating **112** CLI output files from the pre-change tree and diffing byte-for-byte. `transync_openai::pagination` was deleted — the one deliberate crate-surface removal outside the window (zero callers in-tree and in the sibling repo; the oversize-split deferral stays tracked in `stub-manifest.md` / `contracts.md` §5). The **IR freeze rule** held: beyond those two fields, `Document`/`Block`/`Section` are unchanged. Recorded in **DCR-0019**; DCR-0017 gained a dated residual-(a) note and ADR-0016 a dated path note. Verified: fmt/clippy clean, wasm gate green, `cargo test --workspace -- --test-threads=4` **385 passed / 0 failed / 3 ignored** (pre-wave 340; zero test removals with one sanctioned 1-for-1 swap), CLI stub suite 34, `scripts/smoke.sh` green (rustdoc gate included), Playwright SCN-13 **8/8**, `resp-translator` `cargo check --workspace` green with no migration; alignment schema still `1.2.0`, `VALIDATION_SCHEMA_VERSION` still `2`, `CacheKey` and both `sync.js` copies untouched. Two tickets filed and left open: `294ddabd`, `69b9b3db`. *(Resolution note, 2026-08-05: both are closed — `294ddabd` by `1436304`, which took route (a) so `max_per_batch_provider_retries` is now charged **per batch** across the whole retry ladder (CHANGELOG `[Unreleased]`, ADR-0009's amendment, `contracts.md` §5), and `69b9b3db` by `7572c45`, folding `constraints_for` onto `BlockKind::heading_level()` so `id.rs` is the single kind→level home.)*
- Older wave: **OI-0027 public-surface curation** (landed 2026-08-04) — **OI-0027 is RESOLVED**, and it was the last item on the road to 0.2.0. `crates/transync` stopped being `pub use transync_core::*;` + `pub use transync_syntax;` and became an **explicit curated re-export list** (the facade's `transync-syntax` dependency went with them), while `transync-core`'s tier-(c) modules — `batch`, `error`, `pipeline`, `validate`, plus the five re-exported syntax aliases `align`/`id`/`parser`/`regen`/`render` — went `pub(crate)`, so the tier boundary is a **compiler** fact rather than a documented intention. One deliberate exception: `unit` is `#[doc(hidden)] pub` because `transync-openai`'s offline `live_smoke` fixture-shape test reaches it cross-crate, and that reach is now declared as engine **dev-dependencies** instead of borrowed from the facade. **`#[non_exhaustive]` landed on nine items** (workspace count 2 → 11): `TranslatorError`/`TransyncError`/`ParseError`, the four profile structs, `TranslateOptions`, and `TranslationUnit`. Construction is **default-then-assign** everywhere except `TranslationUnit`, which got `new(unit_id, block_kind, input_mode, source_payload, source_hash)` + `with_*` (`Default` is meaningless for it, and `source_hash` is a `CacheKey` axis with no honest zero); the exhaustive-by-policy set (`UnitResult`, `TranslationBatchResult`, `TranslationBatch`, `GlossaryEntry`) is unchanged. `pipeline::run_pipeline` is unreachable — **`translate_with_cache` is the promoted entry point**, and not a bare alias: it preflights an empty `target_language` and fails fast. `contracts.md` gained **§0 "Public Rust surface"** (78 rows), welded to the code by `crates/transync/tests/public_surface.rs` in six parts (compile-assertion re-export module, `DOCUMENTED` mirror, table ↔ `DOCUMENTED` scrape equality, `first_class` ↔ `DOCUMENTED` self-scrape equality, segment-based negative test over the hidden module names, and a DCR-0017 **M6** `generator.version` guard) — table, `DOCUMENTED`, and `first_class` are mutually welded; only a `pub use` added to the facade's `lib.rs` alone still widens the surface unchecked, pending a rustdoc-JSON diff. DCR-0017's handover list is **closed**: `htmlseg::balance_fragment` / `outcome::is_translatable` / `walk::label_for` went `pub(crate)`, and `cargo doc --no-deps` is warning-free for all three library crates under a **standing** `RUSTDOCFLAGS="-D warnings"` gate in `scripts/smoke.sh`. Closure fallout was resolved with **zero test removals** and no new `#[allow]`s — `pipeline::retry`'s three no-op hooks, `batch::group_by_unit_count` (with its pre-existing `#[allow(dead_code)]`), and `ValidatedBatch::batch_id_str` deleted behind a greppable tombstone comment; `batch::estimate_unit_tokens` + `validate::schema::check_schema` became `#[cfg(test)]` — and **OI-0008 still owns the retry/fallback redesign**. Two owner-decision corrections are recorded in the record: the 2026-08-03 decision's "construction via `..Default::default()`" was **unsound** (FRU is illegal cross-crate on a `#[non_exhaustive]` struct; re-decided as default-then-assign), and tier (a)'s "`TranslateOutput`" is a naming slip for `TranslationOutput`. The sibling repo `resp-translator` migrated the same day (commit `8b6dbd5`) with one behavioral delta: an empty profile `target_language` now fails fast with `stable_code` `"internal"`. **Discovery:** the tracked pre-commit hook was **not live** — `core.hooksPath` was unset until `install-hooks.sh` set it, so only `.git/hooks/pre-commit` ran; that copy is now inert but kept byte-identical. Recorded in **DCR-0018**; DCR-0005 gained a dated note (the firewall it created is now *curated*), ADR-0018 a second dated path note. Verified: fmt/clippy clean, wasm gate green, `cargo test --workspace -- --test-threads=4` **340 passed / 0 failed / 3 ignored**, `scripts/smoke.sh` green (rustdoc gate included), Playwright SCN-13 **8/8**; alignment schema still `1.2.0`, `VALIDATION_SCHEMA_VERSION` still `2`, `CacheKey` and both `sync.js` copies untouched.
- Earlier still: **`transync-syntax` crate split + AST-direct renderer** (landed 2026-08-04) — **OI-0028 is RESOLVED**. The syntax layer became the workspace's fifth member, `crates/transync-syntax`, so it compiles for `wasm32-unknown-unknown` behind a standing gate present in both pre-commit hook copies and `scripts/smoke.sh` (the hook fails loudly, never skips, when the rustup target is missing). It owns `parser` (+`ranges`/`refdefs`), `id`, `regen`, `render` (+`attrs`), `align`, `htmlseg`, `error::ParseError`, plus two new modules — `outcome` (the per-block `HtmlOutcome` closure) and `walk` (ONE shared top-level normalization + per-list item count, consumed by both the render zip and `validate::full_reparse`); `transync-core` keeps `pipeline`/`unit`/`batch`/`llm`/`validate`/`cache`/`profile`. **No consumer path broke** — core re-exports the moved modules and the facade gained `pub use transync_syntax;` — while two signatures did (plus one helper deletion), on purpose and inside the open 0.2.0 window: `regen::regenerate` takes `&HashMap<BlockId, String>` (absence IS the fallback contract; `collect_translations` deleted) and `align::build_alignment_map` takes neutral statuses + `source_language`/`target_language` (arity 6→7). The **renderer was rewritten to whole-document AST-direct**: one Comrak parse per pane — 2 parses per run instead of `2 × (N_blocks + N_ol_runs)` fragment reparses — no per-fragment ref-defs append, and `strip_outer_wrapper` / `block_inner_html` / `ordered_list_start` **deleted** — which clears OI-0008's two renderer items and fixed a latent bug (4-space-indented code blocks rendered as `<p>` because the pre-reparse `md.trim()` destroyed the indent). Accepted shape changes: loose source lists render loose in **both** panes and loose task items keep comrak's `<input …/>`-then-`<p>` shape. Two guards added: `full_reparse`'s per-list item count (defense in depth behind `per_kind::check_list`; **no `VALIDATION_SCHEMA_VERSION` bump**) and a render-side degrade that can never drop an anchor. The **dialect trait was deliberately NOT instantiated** — DCR-0005's deferral condition is still unmet. Recorded in **DCR-0017**; ADR-0003 gained a dated amendment (five members, `transync-core → transync-syntax`), ADR-0006/ADR-0007 dated mechanism amendments, DCR-0005/DCR-0007 dated notes. Filed **OI-0033** (lone-CR `LineOffsets` desync — pre-existing, found by the probe that disproved the spec's Guard-1 residual (a)). Verified: fmt/clippy clean, wasm gate green, `cargo test --workspace` green — `transync-syntax` **96**, `transync-core` **165** (+1 ignored golden-regen helper), 21 scenario + 6 `boundary_v02` + 2 `error_fixtures` + 2 `reader_honesty` + 2 `docs_index_drift` in the facade crate, `transync-openai` 25 (+2 ignored live), CLI 11 + 3 drift; alignment schema still `1.2.0`, both `sync.js` copies untouched.
- Older still: **HTML-content translation feature** (landed 2026-08-04) — block-level raw HTML became the translatable kind `BlockKind::Html`: a shared `lol_html` routine extracts entity-decoded text segments, the LLM sees only a JSON array of strings (tags never appear in the translatable payload; neighbor-context summaries may carry raw markup as untrusted context data), and a positional splice with identity-skip preserves every non-text byte. Successful blocks **live-render** in both panes (render-side fragment auto-balancing) through the shells' DOMPurify mount; the DCR-0013 escaped placeholder becomes the **failure-only** presentation. Adds an always-on inline raw-HTML tag guard, `<details>` toggle mirroring across panes, alignment schema **1.1.0 → 1.2.0** (additive `html` `block_kind`, full lockstep) and `VALIDATION_SCHEMA_VERSION` **1 → 2**; new deps `lol_html` 2.9.0 + `htmlize` 1.1.0 (canaried: they **compile** for `wasm32-unknown-unknown`). Recorded in **ADR-0018** (which amends `CLAUDE.md` invariant 7) and **DCR-0016**; ADR-0012 gained a dated amendment (raw inline-HTML tags become protected structure, with no policy gate) and DCR-0013 a superseded-in-part banner; new scenario **SCN-15**. Verified: `cargo test --workspace` green — `transync-core` 235 (+1 ignored golden-regen helper), 21 scenario/integration tests across five `transync` test binaries, `transync-openai` 25 (+2 ignored live), CLI 11 + 3 drift and 19 with `--features test-stub-provider`; Playwright SCN-13 suite **8/8**.
- Older yet: **OI-2026-08** open-issue resolution wave (2026-08-03) — resolved five of the seven issues DR-2026-07 opened: auto-extracted candidate glossary (OI-0026) and per-batch schema-fault fairness (OI-0031) in **DCR-0014**; the provider-neutral prompt lift into `transync-core::llm::prompt` + `TokenizerHint` + model-identity demotion (OI-0029), RTL pane direction (OI-0032), and the double-gated machine-asserted live smoke (OI-0030) in **DCR-0015**. ADR-0009 and ADR-0013 gained dated amendments (a second bounded retry budget; a presentation-layer direction hint over still-opaque labels). All changes ride the still-open 0.2.0 window; the alignment-map schema stays `1.1.0` and `CacheKey` is unchanged. Verified: fmt/clippy clean, core 164 tests, 17 scenarios, `transync-openai` 25 (+2 ignored live), CLI stub 11 + 19 + 2. Still open from that cluster: OI-0027, OI-0028.
- Oldest tracked wave: **DR-2026-07** design-review wave (2026-07-27) — output-aware batch packing + five CLI batching flags (DCR-0012), reader-honesty skipped-node placeholders + refmap resolution + alignment schema 1.1.0 (DCR-0013), and the owner-settled abort-all decision (ADR-0017). Verified: fmt/clippy clean, workspace tests green, CLI stub green, Playwright SCN-13 7/7. Opened OI-0026..OI-0032.
- Summary: the 2026-07 external-review wave (EXT-2026-07) hardened the shipped MVP across three implementation phases — `transync-core`/`transync` facade split (semver firewall), `Translator::fingerprint()` + provider-scoped `CacheKey` with validation-schema version and context hash, `Cache` trait v2 with targeted eviction, inline-protection validation layer, non-content `RetryContext` retry side channel (ADR-0009), `--out-dir` staged fileset commit + `--max-input-bytes` input caps, automated headless Playwright SCN-13 suite, and load-time rejection of conditional-on-section glossary scope (ADR-0014 amended). `design-baseline-2026-07.md` declares the document authority hierarchy.
- Superseded baseline: `BL-2026-05-01-A` (issued 2026-05-01; design handoff + MVP completion) — historical, preserved unedited under a banner in `docs/project/design-baseline.md`. All 14 mandatory scenarios shipped against it (see Implementation Track below).

## Design Track
- Current phase: 5 (Validation & Handoff — complete)
- Highest completed phase: 5
- Open change records: none
- Closed change records: **DCR-0001 through DCR-0048, with no gaps** (see `design-change-records/`; 0001–0004, 0006, 0010, 0011, 0013, 0015, 0017, 0018 and 0019 sit under `design-change-records/archive/`, the rest at the top level, 0009–0011 record the 2026-07-13 external-review wave, 0012–0013 record the 2026-07-27 design-review wave DR-2026-07, 0014–0015 record the 2026-08-03 open-issue resolution wave OI-2026-08, 0016 records the 2026-08-04 HTML-content translation feature, 0017 the 2026-08-04 `transync-syntax` crate split + AST-direct renderer, 0018 the 2026-08-04 OI-0027 public-surface curation, 0019 the 2026-08-05 OI-0008 + OI-0033 internal-quality wave, 0020 the 2026-08-05 Track C wasm render+edit demo, 0021 the 2026-08-06 publication lock + honest `--out-dir` replacement wording from Review-0001, 0022 the 2026-08-08 Review-0002 hardening wave, 0023 the 2026-08-08 provider error taxonomy (ticket `1a85f3`), 0024 the 2026-08-08 run cancellation (ticket `43331a`), 0025 the 2026-08-09 Review-0003 content-loss + refusal-precedence wave, 0026 the 2026-08-09 oversize-table row-window split (`fc0304`), 0027 the 2026-08-09 section-coherent batching + glossary section scope (`43cfb4`), 0028 the 2026-08-09 disk-backed cache + document metadata (`f12b8b`), 0029 the 2026-08-10 `transync-anthropic` second provider (`bda471`), 0030 the 2026-08-13 Review-0004 credential + origin hardening, 0031 the 2026-08-16 indented-code normalize-on-translate (ticket `457e51`), 0032 the 2026-08-20 `transync-html` crate extraction + tag-scanner extension (ti `490d97` wave 0), 0033 the 2026-08-23 OI-0035 closure at both layers (ti `490d97` wave 1), 0034 the IR semantic-kind / source-spelling split (ti `490d97` wave 2 — code 2026-08-24, record 2026-09-01, with ADR-0025), 0040 the review-0009 fix pass's three shipped behaviour changes — code 2026-08-26, record 2026-08-29 `5ff6d17`, 0041 the 2026-09-01 foreign-content integration points and breakout (ti `e77173`), 0042 the 2026-09-01 raw-text-is-an-HTML-content-state change that moved the content-mode stack into the scanner (ti `2e2453`), 0043 the 2026-09-01 voidness-is-an-HTML-content-rule change (ti `48f3c6`), 0044 the 2026-09-01 row-decides-the-anchor change (ti `18b9c3` / `9ffb97`), **0035 the 2026-09-03 HTML document intake + identity round-trip (ti `490d97` wave 3), 0036 the 2026-09-03 HTML layer-6 twin + the one format branch (ti `490d97` wave 4), 0037 the 2026-09-03 HTML translation entry point (ti `490d97` wave 5) and 0038 the 2026-09-03 HTML panes + alignment wire 1.3.0 + CLI surface (ti `490d97` wave 6)** — the first four of the reserved block to be claimed by the waves that reserved them). **DCR-0039 LANDED 2026-09-04** with wave 7, the last — it was reserved from the start and was still free at execution, which is exactly what the reservation was for; each wave's own records task appended its number when it ran, wave 2's on 2026-09-01, which is how DCR-0034 left the reserved block. DCR-0040 through DCR-0044 are therefore out of sequence deliberately: taking "the next free number" would have collided with an unrun wave's paperwork, which is the race the wave-2 plan warns about in its own Task 8. Read a gap in this roster as a reservation, not as a missing record — which is why 0045 (`e4f4b0`, the `transync-lang` source-language gate), 0046 (`30a744`, offline runs from a warm cache), 0047 (`c1f9a8`, the balancer repairing an unterminated trailing region) and 0048 (`c887bc`, visible-text presence as a validation layer) are named here rather than left to be inferred, and why 0039 (`490d97` wave 7 — the browser gate and closure, landed 2026-09-04) closed the last reservation: the roster ran ahead of these records, and leaving any of them unnamed inside an unbroken span would have read as reservations that do not exist
- Rewind required to phase: none
- Summary: Design handoff completed 2026-05-01. `docs/project/design-baseline.md` documents `BL-2026-05-01-A`, the Phase-4 hard gate evidence, the contract inventory, and the pointer set (all now historical — superseded by `BL-2026-07-B`, see Active Baseline above). `phase-state.yaml.design.status = handoff_complete`.

## Implementation Track
- Current phase: 5 (MVP Completion Gate — passed)
- Highest completed phase: 5
- Synced baseline ID: `BL-2026-07-B` (MVP originally shipped against `BL-2026-05-01-A`; the v0.2.0 external-review wave brought code and baseline forward together)
- Sync status: synced
- Closed slices: SL-00 through SL-14 (15 of 15)
- MVP complete: **yes** (2026-05-01)
- Allowed parallel slices: n/a
- Blocked slices: none
- Summary: MVP complete. All 14 mandatory scenarios pass on a clean checkout (`cargo test --workspace` 13 scenario tests + the transync-cli test suite via `cargo test -p transync-cli --features test-stub-provider`; SCN-13 verified by the automated headless Playwright suite `scripts/test-browser.sh` (`web/tests/scn13.spec.js`), with the manual smoke checklist at `web/SMOKE.md` kept as fallback). Zero `STUB` markers remain in MVP-required source. Post-MVP placeholders are retained as `DEFERRED` rows in `docs/project/stub-manifest.md` with written justifications (live OpenAI Responses API, `tiktoken-rs` token estimation, HTML bundle template substitution); two rows left that list on 2026-08-09 — the row-window splitter when DCR-0026 shipped it (STUB-017), and the real static HTTP server in `transync serve` when ticket `b791d6` shipped it (STUB-061), which is also what serves the SCN-13 Playwright suite now.

## Phase 1 Validation Evidence
| Checklist item                                            | Status | Where validated |
|-----------------------------------------------------------|--------|-----------------|
| Architecture overview written                             | ✅      | `docs/architecture/README.md` |
| MVP scope locked                                          | ✅      | `docs/architecture/mvp-scope.md` |
| Scenario matrix lists every mandatory scenario            | ✅      | `docs/architecture/scenario-matrix.md` (SCN-01..14) |
| At least one ADR written                                  | ✅      | `docs/decisions/0001..0004` |
| User review of Phase 1 artifacts                          | ✅      | 2026-05-01 |

## Phase 2 Validation Evidence
| Checklist item                                                              | Status | Where validated |
|-----------------------------------------------------------------------------|--------|-----------------|
| Every mandatory scenario mapped to binaries/modules                         | ✅      | `docs/implementation/module-map.md` (Scenario → component table) |
| Every mandatory scenario mapped to persistence/files                        | ✅      | `docs/implementation/module-map.md` (Persistence/file touches table) |
| Every mandatory scenario mapped to integrations                             | ✅      | `docs/implementation/module-map.md` (Integrations table) |
| Essential persistence interfaces exist                                      | ✅      | `Cache` trait + `transync::profile` loader in module-map.md; lifecycle in `persistence-and-files.md` |
| Essential file-storage interfaces exist                                     | ✅      | `transync-cli::output` atomic-write spec in `persistence-and-files.md` §atomic-write |
| Rough schema outlines exist where durable state lives                       | ✅      | `docs/architecture/rough-schema.md` |
| Source-of-truth recorded                                                    | ✅      | `docs/architecture/source-of-truth-table.md` |
| Contract pack written                                                       | ✅      | `docs/architecture/contracts.md` (Translator trait, Profile TOML, AlignmentMap JSON, HTML attrs, retry policy, CLI args, transync-openai constructor) |
| BlockId format ADR                                                          | ✅      | `docs/decisions/0005-block-id-format.md` |
| User review of Phase 2 artifacts                                            | ✅      | 2026-05-01 |

## Phase 3 Validation Evidence
| Checklist item                                                              | Status | Where validated |
|-----------------------------------------------------------------------------|--------|-----------------|
| Skeleton plan written (repo + workspace + binary layout, gen order, deps)   | ✅      | `docs/project/skeleton-plan.md` §1–§5 |
| Local run / demo script plan defined                                        | ✅      | `skeleton-plan.md` §7 |
| Smoke test path defined                                                     | ✅      | `skeleton-plan.md` §8 |
| Core verification skeletons listed                                          | ✅      | `skeleton-plan.md` §9 |
| Marker policy defined (STUB / DEFERRED / traceability tags)                 | ✅      | `skeleton-plan.md` §10 |
| Implementation slice checklists exist for every mandatory scenario          | ✅      | `docs/project/implementation-slice-checklists.md` (SL-00 through SL-14) |
| Initial stub manifest seeded                                                | ✅      | `docs/project/stub-manifest.md` (31 transync rows + 7 transync-openai + 4 transync-cli + 3 web rows) |
| Smoke fixture contents resolved (Phase-3 deferred item)                     | ✅      | `skeleton-plan.md` §6 (12 fixtures spec'd; SCN-03 + SCN-10 generated programmatically) |
| Renderer output shape resolved (Phase-3 deferred item)                      | ✅      | ADR-0006 locks two pane fragments + templated `index.html` shell |
| User review of Phase 3 plan                                                 | ✅      | 2026-05-01 (Phase 4 advanced after approval) |

## Phase 4 Validation Evidence
| Checklist item                                                              | Status | Where validated |
|-----------------------------------------------------------------------------|--------|-----------------|
| All in-scope binaries / processes / modules generated                       | ✅      | `crates/transync-core/src/`, `crates/transync-openai/src/`, `crates/transync-cli/src/`, `web/` |
| Major types + traits defined with traceability tags                         | ✅      | `id.rs`, `llm.rs`, `align.rs`, `cache.rs`, etc. — every `pub fn`/`pub struct`/`pub trait`/`pub enum` carries at least one `TRACE:`/`ADR:` tag |
| Internal dummy logic sufficient for end-to-end invocation                   | ✅      | `pipeline::run_pipeline` composes STUB sub-steps and returns a well-typed `TranslationOutput` |
| Local run/demo script exists                                                | ✅      | `scripts/smoke.sh` (executable; runs build + test + CLI dry path) |
| Smoke verification artifact for the local end-to-end path                   | ✅      | `scripts/smoke.sh` exits 0; 7 outputs land in `/Volumes/Temp/claude/transync-smoke/` |
| Verification skeletons for every mandatory scenario                         | ✅      | `crates/transync/tests/scenarios.rs` aggregator + 12 `scn_*.rs` files |
| Markers tracked in `stub-manifest.md` with type + path + scenario + exit    | ✅      | 45 rows; status flipped to `generated` |
| `cargo build --workspace` clean                                             | ✅      | only the reserved `ExitCode::ArgumentError`/`AllUnitsFellBack` warning |
| `cargo test --workspace` passes                                             | ✅      | 12 scenario tests pass (historical, as of 2026-05-01) |
| `cargo test -p transync-cli --features test-stub-provider` passes           | ✅      | `cli_translate_smoke` passes |
| `./scripts/smoke.sh` exits 0                                                | ✅      | output bundle materialized; all five `--html-out` files non-empty |
| User review of Phase 4 skeleton                                             | ✅      | 2026-05-01 (Phase 5 advanced after approval) |

## Phase 5 Validation Evidence
| Checklist item                                                              | Status | Where validated |
|-----------------------------------------------------------------------------|--------|-----------------|
| Structural skeleton reflects the scenario matrix                            | ✅      | `design-baseline.md` "Repo / workspace topology"; 12 fixtures + 12 scenario tests cover SCN-01..14 |
| Required binaries / processes / modules exist                               | ✅      | `crates/transync-cli` binary + `transync` lib + `transync-openai` lib |
| Essential repository and file-storage interfaces exist                      | ✅      | `Cache` trait + `InMemoryCache`; `transync-cli::output::write_atomic` is real |
| Rough schema and file lifecycle design represented in structure             | ✅      | every type from `rough-schema.md` exists in code; CLI writes the 5-file `--html-out` bundle |
| Source-of-truth placement documented                                        | ✅      | `source-of-truth-table.md` |
| Local run / demo scripts exist                                              | ✅      | `scripts/smoke.sh` |
| Smoke verification runs                                                     | ✅      | `./scripts/smoke.sh` exits 0 |
| Core verification skeletons exist and compile/run                           | ✅      | 12 `scn_*.rs` smoke tests pass |
| `STUB` vs `DEFERRED` usage correct                                          | ✅      | 45 `STUB` rows, 0 `DEFERRED` rows (pre-hardening snapshot, 2026-05-01; post-hardening: 0 MVP-blocking `STUB` rows, 11 `DEFERRED`); `stub-manifest.md` "Hard-gate evidence checklist" |
| Implementation slice checklists cover all mandatory scenarios               | ✅      | SL-01..SL-14 (one per SCN) plus SL-00 foundation |
| `CHANGELOG.md` exists with `[Unreleased]` + handoff entry naming baseline   | ✅      | `CHANGELOG.md` "Handoff" entry naming `BL-2026-05-01-A` |
| `design-baseline.md` published                                              | ✅      | `docs/project/design-baseline.md` |
| `phase-state.yaml.design.baseline_status = approved`                        | ✅      | `phase-state.yaml` |

## Open Issues
Issues are open and tracked in `docs/project/open-issues.md`; none block the shipped MVP. The 2026-07-13 external-review cluster:
- ~~**OI-0008** — Refactor / perf debt cluster.~~ **RESOLVED 2026-08-05** (DCR-0019): every bullet group consumed — the `R0001-` ids here are the removed `reviews/reviewed/0001.md`'s, not `reviews/0001.md`'s (`reviews/README.md`) — R0001-0057 by the pure `pipeline/policy.rs` decision core, R0001-0076 by the `pipeline/{dispatch,finalize,report}` split, R0001-0078 by `structure{,/labels}` + `unit/{payload,budget}`, R0001-0079 by the client split behind one surface abstraction, R0001-0080 by the `translate_cmd` split behind the `execute` seam, and R0001-0077 by the parser subtractive slice + `WalkState` restructure. (R0001-0069 and R0001-0096, the two renderer items, had been cleared 2026-08-04 by DCR-0017.)
- ~~**OI-0015** — Browser sync module mixes four concerns.~~ **RESOLVED 2026-08-09** (ticket `d3acc3`): both open questions answered in the pass the issue was waiting for. Single-file packaging is **re-affirmed** — the byte-twin discipline (`sync_js_drift.rs`) and the no-build, framework-free `web/` tree outweigh a source split — and ID-identity pairing is now a **normative, tested schema-1.x invariant** (ADR-0001 amendment, `contracts.md` §3) with the map's source/target indirection reserved for a future divergence revision, so a map contradicting it is refused rather than mounted-but-unsyncable. That also resolves Review 0003's `R0003-0002`.
- **OI-0016** — Active-block selection scans every block on every scroll frame (revisit behind a profiling gate).
- ~~**OI-0035** — an injected `data-sync-id` can pre-claim a real block's anchor.~~ **RESOLVED 2026-08-23** (ti `490d97` wave 1, DCR-0033; the code landed 2026-08-22) via route **(c) — both layers**: the renderer strips the reserved sync-attribute namespace out of raw-HTML blocks on the way into a pane (`out.md` keeps the author's bytes), and `mountSync` builds its anchor set from the validated alignment rows so `collectAnchors` — the one choke point at mount and at every reflow — ignores what the map does not claim. That also closes the 2026-08-09 correction's residual: an anchor inserted after mount is no longer activated by the next quiet recompute. **The residual that remains is accepted and recorded**: an impostor carrying a *listed* id ahead of the genuine anchor still wins first-occurrence-wins, which is exactly why the render-side strip is the other half rather than a nicety. Record archived to `open-issues-archive.md` the same day.
- ~~**OI-0017** — Detected-source-language caching (narrowed 2026-07-13): a fully-cached resume run reports `null` because the per-unit cache does not persist language detection — fold into the disk-backed-cache design point.~~ **RESOLVED 2026-08-09** (ticket `f12b8b`; DCR-0028 / ADR-0021, slices SL-111 + SL-114): the fold-in happened as written — document-level metadata rides the `Cache` seam (`DocumentMetaKey` / `DocumentMeta` plus two **defaulted** trait methods), so the pipeline writes the run's detection from a live qualifying envelope and replays it on a run that dispatched **zero** provider batches; `DiskCache` makes the record durable across processes and `transync translate --cache-dir` makes the scenario literal. All four items of the original issue have now shipped — the provider-namespace / staged-commit / Hard-failure-rollback three in the EXT-2026-07 wave, this one here — and the record was archived to `open-issues-archive.md` on 2026-08-13.
- ~~**OI-0018** — Bundle CSP / remote-image posture (owner-accepted tradeoff; re-open if bundles are shared externally).~~ **RESOLVED 2026-08-07** (ticket `14307f84`): the re-open marker fired as an owner posture update rather than a default change — `transync translate --strict-csp` stamps the CSP meta into the emitted bundle, and a run without it is byte-identical to every prior version's output. The accepted default tradeoff (remote images render) stands; the escape hatch is per run.
- ~~**OI-0020** — No committed Cargo.lock (machine-global gitignore; owner policy decision).~~ **RESOLVED 2026-08-06** (`6cf4164`): the owner reversed the 2026-08-05 pin-over-lock deferral — `.gitignore` carries a repo-local `!Cargo.lock` un-ignore and the lockfile is tracked, so the resolution satisfying `wasm-bindgen = "=0.2.126"` lives in the tree. The exact pin is kept alongside it (a lock binds only resolves that read it, and no lock can pin `wasm-bindgen-cli`). One half of the concern was **declined, not fixed**: the same global ignore still hides this repo's `CLAUDE.md`, untracked by owner choice.
- ~~**OI-0024** — Archive-cleanup consolidation (2026-07-11): browser-sync residuals (deferred reflow-recompute hooks).~~ **RESOLVED 2026-08-09** (ticket `d3acc3`): item 2 (forward-drift warning) had landed 2026-07-14; item 1 lands here as three reflow signals — a `ResizeObserver` on both panes, `document.fonts.ready`, and capture-phase `load`/`error` on `<img>` inside either pane — each coalescing into one animation frame that re-collects both anchor sets and re-drives the follower from the last driving pane. Caller-driven destroy-and-remount is now needed only for an actual HTML replacement.

OI-0019 (provider resource bounds — response-size cap + output-token ceiling) and OI-0022 (fingerprint depth residuals) were resolved 2026-07-13 (EXT-2026-07 P1-7 / P1-4).

The 2026-07-27 design-review wave (DR-2026-07) opened seven new issues, none blocking. The 2026-08-03 resolution wave (**OI-2026-08**) closed five of them — **OI-0026** (auto-extracted candidate glossary, DCR-0014), **OI-0029** (provider-neutral prompt lift + tokenizer hint + model-identity authority, DCR-0015), **OI-0030** (double-gated machine-asserted live smoke, DCR-0015), **OI-0031** (per-batch schema-fault fairness, DCR-0014), and **OI-0032** (RTL pane direction, DCR-0015). **OI-0028** (WASM render compile path) closed 2026-08-04 with the `transync-syntax` crate split (DCR-0017), and **OI-0027** (public-surface hardening) closed later the same day with the curation (DCR-0018), which consumed DCR-0017's entire widened-surface inventory — the three narrowing candidates went `pub(crate)` and the rustdoc warnings are gone under a standing gate. **All seven DR-2026-07 issues are now resolved.** The sequence, all inside the 0.2.0 window (closed by the v0.2.0 tag, 2026-08-05): **HTML-content translation feature (LANDED 2026-08-04 — ADR-0018 / DCR-0016) → OI-0028 crate split + renderer rework (LANDED 2026-08-04 — DCR-0017) → OI-0027 facade curation (LANDED 2026-08-04 — DCR-0018) → 0.2.0 release (TAGGED 2026-08-05)**.

~~**OI-0033** (filed 2026-08-04) — `LineOffsets` counts only `\n`, so a lone-CR source desyncs the sourcepos→byte mapping.~~ **RESOLVED 2026-08-05** (DCR-0019) under the owner-decided **Support** posture: the line table now counts a lone `\r` as a boundary exactly as comrak does. Normalize was rejected (mutating source bytes collides with the byte-verbatim round-trip guarantee `regen`/`htmlseg` exist to keep) and reject was rejected (it would make transync stricter than CommonMark). DCR-0017's Guard-1 residual (a) is closed by direct assertion, not inference.

~~**OI-0034** (new, 2026-08-05) — comrak substitutes a 3-byte U+FFFD for a 1-byte NUL, so its byte **columns** run past the real line length and the sourcepos→byte mapping drifts within the line. The column-side sibling of OI-0033, found by the same probe.~~ **RESOLVED 2026-08-06** (ticket `743d27f0`, commit `e75e815`) under the owner-decided **Normalize at intake** posture: `parser::parse` substitutes U+FFFD for every NUL *before* comrak is handed the document, so comrak's columns and the line table count the same bytes by construction, and the normalized string is what becomes `Document::source_text` — and therefore what ranges, hashes, IDs, regen and both panes index. Support was rejected (it means carrying comrak's preprocessing in the range mapper forever) and reject was rejected (CommonMark §2.3 *requires* the substitution). The record's "one extra byte" impact estimate is corrected there: the clamp was returning the **correct** answer for every block shape, because a block's `Sourcepos` always ends where its line's content ends — the drift is only observable at a column strictly inside a line, which is why the resolving fixture needs trailing whitespace. One residual is ticketed and out of scope: a NUL arriving in a **translated payload** still reaches `out.md` verbatim while the pane renders U+FFFD (`d06c4349`).

Still open in the register besides the above: ~~**OI-0038**~~ **RESOLVED 2026-09-03** (`30a744`, DCR-0046 — `transync translate --offline` serves a fully warm run with no credentials; the product call it was blocked on was taken) and the review-0009 cluster **OI-0039..OI-0048** (filed 2026-08-26; see the review-0009 action under Immediate Next Actions). **OI-0037** was RESOLVED 2026-09-01 by ticket `148fcf`, its record still in the register. All other filed issues are closed. Their records were archived to `open-issues-archive.md` (OI-0001, 0002, 0003, 0005, 0006, 0009, 0010, 0011, 0012, 0014) or pruned as self-evident fixes (OI-0004, 0007, 0013) in the 2026-07-11 record prune.

## Immediate Next Actions
- **Action (road to 0.2.0, owner-approved 2026-08-03):** ~~HTML-content translation feature (spec v2 at `docs/superpowers/specs/2026-08-03-html-content-translation-design.md`)~~ **DONE 2026-08-04** (ADR-0018 / DCR-0016) → ~~OI-0028 Option B crate split + renderer rework (clears the OI-0008 renderer debt in the same effort)~~ **DONE 2026-08-04** (DCR-0017; OI-0028 RESOLVED, OI-0008 narrowed) → ~~OI-0027 facade curation + `#[non_exhaustive]` + constructor (the window-closing ritual)~~ **DONE 2026-08-04** (DCR-0018; OI-0027 RESOLVED, DCR-0017's widened-surface inventory closed) → ~~0.2.0 release~~ **DONE 2026-08-05** — annotated tag `v0.2.0`; the breaking window is **closed**. `#[non_exhaustive]` — 17 items after the pre-tag owner addendum extended it to the six read-only output types (`TranslationOutput`, `ValidationReport`, `AlignmentMap`, `AlignmentBlock`, `ValidationSummary`, `GeneratorMeta`) — makes the next field/variant addition additive instead of breaking. The release gate below ran for the tag; date + models are recorded in the CHANGELOG's 0.2.0 entry.
- **Action (post-0.2.0, done 2026-08-05):** ~~OI-0008 + OI-0033 internal-quality wave (spec at `docs/superpowers/specs/2026-08-05-oi0008-0033-internal-quality-design.md`, 12-task plan alongside it) — land the refactor debt *before* Track C, because Track C ships `transync-syntax` to the browser and refactoring a shipped WASM artifact costs more.~~ **DONE 2026-08-05** (DCR-0019; OI-0008 and OI-0033 both RESOLVED, OI-0034 filed).
- **Action (next):** ~~**Track C** — wasm-bindgen entry points, JS integration, browser demo.~~ **DONE 2026-08-05** (ADR-0019 / DCR-0020; see the Track C wave bullet above). → ~~**Review-0001 hardening arc + v0.3.0 release.**~~ **DONE 2026-08-07** — 46 tickets across waves C–H, annotated tag `v0.3.0`; the 0.3.0 breaking window is used and **closed**.
- ~~**Action (post-release):** run the live gate against the v0.4.0 release-prep commit.~~ **DONE 2026-08-20** — `scripts/smoke-live-gate.sh` **PASS**, 2/2 live round-trips against `gpt-4o-mini` (Chat Completions) and `gpt-5-mini` (Responses); date and models recorded in the CHANGELOG's `[0.4.0]` entry. The gate was genuinely triggered by this range (`unit::payload` changed what an indented code block puts on the wire; `transync-openai` and batching both moved), and it ran against a tree identical to the release-prep commit in every file but `CHANGELOG.md`. This ends the two-release run — v0.3.0 and v0.4.0's cut — of shipping with the gate triggered and unrun. The pattern is still worth naming even though it broke here: nothing in the ritual forces a key to be available at cut time, so the gate keeps landing *after* the release rather than gating it.
- **Action (open): the `anthropic` leg of the live gate has never been executed in this repository.** `ANTHROPIC_API_KEY=… ./scripts/smoke-live-gate.sh anthropic` — one round-trip against `claude-haiku-4-5`. `transync-anthropic` ships in the tree but sits on no CLI run path (DCR-0029 scope), so its only standing evidence is its offline suite; a provider adapter that has never spoken to its provider is an untested one. Note `smoke-live-gate.sh`'s default surface is `all`, which refuses up front unless **both** keys are present — run the leg by name.
- **Action (owner decision, open 2026-09-04; ti `84bf37`):** **re-confirm the PHRASING widening's custom-element sentence splitting against real output** (spec §14.1). ADR-0025's D4 makes an unknown element DEFAULT-STOP, so a custom element mid-sentence ends the run before it and starts a new one after; the owner required this be re-confirmed against real output before it stands. Wave 7 staged the evidence, and **it narrows the question**: measured through the shipped intake, a mid-sentence custom element inside a `<p>` does **not** split the sentence (the `<p>` is the element block and the element is interior — three such sentences gave three `paragraph` blocks, not nine), while the same sentence as **naked prose** does split into `p-…`/`html-…`/`p-…`, and known phrasing (`<em>`) is absorbed either way. So the hazard needs prose that is not wrapped in an element, which real pages rarely are — and §14.1's own example is written in the naked form without saying that the form is what makes it split. Three options in the ticket: amend §14.1 to state the wrapping condition, add a per-run phrasing-extension list, or leave it. Artifacts under `/Volumes/Temp/claude/ti490d97-wave7/gate/phrasing-evidence/`.
- **Action (standing gap, open 2026-08-10):** the **`anthropic` leg of the live gate has never been executed.** `crates/transync-anthropic/tests/live_smoke.rs` and `scripts/smoke-live-gate.sh anthropic` are written, double-gated and syntax-checked, but there is no `ANTHROPIC_API_KEY` in this environment, so no request has ever been made to the real Messages API by this repository. Run `ANTHROPIC_API_KEY=… ./scripts/smoke-live-gate.sh anthropic` once a key exists and record date + model in the CHANGELOG. **What stands in for it until then**, and what does not: the crate's offline end-to-end test drives `transync::translate` over the adapter against a loopback socket that answers the request the adapter actually sent, so request shaping, the schema-profile pass, transport, headers, envelope reading and every core validation layer are proven — what is *not* proven is that the provider accepts this crate's request as written, which is exactly the claim only a live call can make.
- **Action (roadmap, commissioned 2026-08-06):** the six post-0.3.0 feature tickets in recorded order — ~~`fc0304` (oversize row-window splitter)~~ **LANDED 2026-08-09** (DCR-0026, slices SL-100..SL-105: an oversize table is split at *packing* time into windows that each carry the real header and delimiter rows (`unit::split`), and `pipeline::merge` reassembles one table block before regeneration, so a window id never reaches the alignment map or the DOM. `default_table_strategy` became effective with `"row-window-first"` as the shipped default profile's value, `max_split_retries` was **removed** (a deterministic packing-time split has no split retry to bound), STUB-017 closed, and SCN-03 stopped being stub-only) → ~~`43cfb4` (section-aware batching, unlocks glossary section scope)~~ **LANDED 2026-08-09** (DCR-0027, slices SL-106..SL-109: the batcher partitions units at heading boundaries and packs each section on its own, which made per-section glossary filtering cheap enough that `[[glossary]].scope = "section"` gained a `sections` selector and its three-gate `ProfileError::Unsupported` rejection retired end to end) → ~~`d3acc3` (sync-engine pass, unlocks OI-0024)~~ **LANDED 2026-08-09** (no DCR — the owner settled its three decisions on 2026-08-06 under delegation and the ticket records them; OI-0015 and OI-0024 both RESOLVED) → ~~`b791d6` (`transync serve`)~~ **LANDED 2026-08-09** (no DCR — the owner un-deferred STUB-061 on 2026-08-06; the real loopback server ships, STUB-061 closes, and `scripts/test-browser.sh` serves SCN-13 with it) → ~~`bda471` (`transync-anthropic`)~~ **LANDED 2026-08-10** (DCR-0029, slices SL-115..SL-119; the second in-tree provider, the first exercise of ADR-0002's "future providers are sibling crates, purely additive" promise — the core trait did not move, and the one thing that reached the seam is a `TranslatorError` variant, `ContextWindowExceeded`, riding the open 0.4.0 window. Half the Type-3 `provider-capability-set` revisit condition has fired; `docs/backlog.md` records the entry as awake with the evidence read both ways) → ~~`f12b8b` (disk-backed cache design, resolves OI-0017)~~ **LANDED 2026-08-09** (DCR-0028 / ADR-0021, slices SL-110..SL-114: `DiskCache` joins `InMemoryCache` behind the unchanged lookup contract — a self-contained JSON-lines log replayed at open, versioned and capacity-bounded — and the `Cache` trait gained two **defaulted** document-metadata methods, which is what resolves OI-0017's last item; `transync translate --cache-dir` is the CLI surface). **All six have landed**; the commissioned roadmap is complete. Each larger one opened with a DCR + SL number.
- **Action (content integrity, ti `c887bc`):** ~~nothing in the pipeline asked whether the translated text survived~~ **RESOLVED 2026-09-04** (DCR-0048). Every per-unit layer compared **structure** — heading level, `(cols, rows, alignments)`, fence info string, node-kind fingerprints, segment count, kind labels, destinations, ledgers, gap bytes — and none asked whether the words survived, so a provider that kept each kind's structural fact and erased its text was **accepted**: `fallback_status: translated`, no warning, a blank for the reader. Measured shipping payloads on **all six Markdown kinds and both HTML paths** (`"##"` → `<h2></h2>`, blank table cells, a bare `rust` fence, `"- \u{200B}"`, `"&nbsp;"`, `"\u{2800}"`, `" \u{FEFF}"`). The one arm that did look at text — `per_kind::check_html`'s whitespace guard — was wrong twice: its predicate was `char::is_whitespace` (25 codepoints; U+200B/200C/200D/2060/00AD/FEFF are not among them), and its stated premise ("source segments carry at least one non-whitespace char by construction") was false, because `transync_html::extract`'s drop test is the *same* predicate. **The ticket as first filed was also wrong on scope** — it claimed the layer-6 twin covered an HTML-document run; the twin's net is a **singleton** (U+FEFF only), and even there it sees only a rule-T anonymous run dissolve, never an element block. Fixed by `validate::text_presence`: ONE scoped comparison — the source carried visible text and the translation carries none — for both intakes, reading the segment **strings** on the HTML path (the splice re-escapes `&`, so an entity renders visibly) and comrak's **decoded text** on every Markdown path (nothing re-escapes there, so `&nbsp;` / `&#8203;` must be resolved before the test). The scope is load-bearing: it is what lets the invisibility test be deliberately over-inclusive without false-positiving a `<td>&#8203;</td>` shim. No new `ValidationLayer` variant, no schema bump, no `transync-syntax` or JS change. Evidence: 17 unit tests + 4 end-to-end `run_pipeline` tests, **staged red observed** (3 of 4 failed against the unwired code, printing a fully blanked document with every row `translated`); workspace 46/1291/0, clippy clean. One consequence recorded: wave 5's layer-6 routing proof needed a new lever (`HTML_SRC_INVISIBLE_RUN`, a ZWSP-only run) because its old one now stops at layer 2 — **layer 2 owns "the words are gone", layer 6 owns "the block is gone"**.
- **Action (HTML→HTML feature, ti `490d97`):** ~~wave 7 — the browser gate and closure~~ **LANDED 2026-09-04** (DCR-0039; six commits `0578272` → this one). **THE HTML→HTML FEATURE IS COMPLETE — all eight waves (0–7) landed.** The demonstrable outcome: the SCN-16 bundle syncs in a real browser through the shipped shell, and every record says what the feature cost. `web/tests/scn16.spec.js` holds §12's acceptance line — bidirectional sync by block id over HTML-derived anchors, the `<title>` absent from both panes and present exactly once on the browser tab, zero console warnings on the engine's channel — with every negative riding a positive (each pane's anchor list must equal the map's anchor-role rows *in order*, so a leaked title, a rendered `<hr>`, a dropped block or a bare `<li>` all surface as list drift rather than a vacuous pass). **The gate is proven falsifiable**: four probes against mutated copies of the real bundle drove a leaked title anchor, a dissolved `li` group, an anchor-role title row and the **pre-ruling self-injected custom-element anchor** each red on its named assertion — the last is the first demonstration *through the real sanitizer* of the hazard wave 6's wrapper ruling closed, since every earlier browser proof bypassed DOMPurify, and probe 2 re-measured §8 step 6's erratum (DOMPurify 3.2.6 passes a bare `<li>` through unchanged and in place, so the grouping stands on D9's model, not on relocation). Also landed: the weld pinning four spec files with all three naming documents in one commit; SCN-16's scenario-matrix rows; the serve **fidelity door** documented; the first-contact documents no longer describing a Markdown-only library; and the §10 contracts sweep, which found **nothing owed**. Acceptance: `scripts/test-browser.sh` 42 passed / exit 0; four probe captures each non-zero; `docs_browser_suite_drift` green around every commit that touched a guarded document; the diff over `crates/` and `web/js/` names exactly one file (the weld test) and the diff over fixtures, `web/js` and `web/tests/support` is **empty** — the engine moved zero bytes. workspace 46 binaries / **1291 passed / 0 failed** / exit 0 with every docs-drift weld green (`docs_index_drift` included); CLI stub suite **217 passed / 0 failed** / exit 0; the two-package wasm gate exit 0 with its string unchanged. Getting the last two cost three lost captures to a macOS Gatekeeper condition that parks a *spawned* binary before it executes (0% CPU, 0:00.00 total CPU over 5+ minutes on unchanged bytes); the captures taken while parked carry no exit marker and no number from them is recorded — see DCR-0039's verification section for the signature and the remedy. **Still open, each with a home:** the owner's §14.1 re-confirmation (ti `84bf37`), island reclassification and the oversize-leaf splitter (Type 3, blocked), the `parser` → `intake::markdown` rename (Type 1), and the **nine-item spec-amendment batch A–I** recorded in DCR-0039 with exact replacement wording — the spec file is byte-untouched. **Cutting v0.5.0 is a separate owner decision** via `docs/project/release-checklist.md`; this closes the feature, not the release, and the ti `490d97` close is the owner's after acceptance.
- **Action (HTML→HTML feature, ti `490d97`):** ~~wave 0 — move the HTML mechanics into their own member and extend the tag scanner~~ **LANDED 2026-08-20 → 2026-08-22** (DCR-0032; Task 1 `1d6f19d` on the 20th, Task 8 and the wave's close `1299280` on the 22nd — dated from `git log --date=short`, corrected 2026-08-24 from a single "2026-08-20" that was the plan's writing date): `transync-syntax::htmlseg` left verbatim for `transync-html` with **no** re-export alias, `splice` took a `BlankLinePolicy` in place of the CommonMark `u8`, `TagToken::Open` gained the span `scan_tags` already computed and `TagToken::Skip` named the comment / CDATA / bogus-comment regions, `element_extents` is `balance_fragment`'s stack walk lifted out from under it rather than a second copy, and `strip_reserved_sync_attrs` landed in its final home ahead of its callers. The publication roster is now **seven** members plus `transync-wasm` (`publish = false`). Pure refactor, and the acceptance evidence says so: the workspace suite green with zero fixture or expectation edits, the two-package wasm gate exit 0 with its string unchanged, and a token-stream pin whose goldens were generated before the scanner changed and are byte-identical after. **Waves 1–7 were unstarted at that point** (`docs/superpowers/specs/2026-08-20-html-to-html-translation-design.md` §12; the dependency order is 0 → 2 → {3, 4} → 5 → 6 → 7, with wave 1 parallel to 2–5). **Wave 2 — the IR split — is the one that needs a sanctioned breaking window**, carries the version to `0.5.0-dev`, and blocks every wave after it; it is also the only part of the feature that cannot slip past a window. **Wave 1 also LANDED — code 2026-08-22, record 2026-08-23** (DCR-0033): OI-0035 closed at both layers before the HTML pane producer that would have widened it exists — the Markdown pane's html-block arm strips the reserved sync-attribute namespace, `mountSync` takes its anchor set from the validated alignment rows, both `sync.js` copies moved in one commit, and the residual neither layer closes alone is written into the DCR, `contracts.md` §4a and the archived issue rather than glossed. Wave 1's own first call site also surfaced a wave-0 defect in `balance_fragment` and `strip_reserved_sync_attrs`, fixed in `d146a53` and recorded in DCR-0032's 2026-08-23 amendment. **Wave 2 — the IR split — was in flight from 2026-08-24 and LANDED 2026-09-01** (DCR-0034; code 2026-08-24, record 2026-09-01): it opened the sanctioned v0.5.0 window (`ff788f7`, workspace version `0.5.0-dev`) and its breaking IR changes are landing (`20b78b7` + `258ba01` the **two** axes — `Spelling` on every block *and* `SourceFormat` as `Document.format` on every document, the second being half of `258ba01` and not a restatement of the first; `954711a` `BlockKind::Html`; `8b556e7` `BlockKind::Title`), followed by `1ed72d3`, which re-keys every html dispatch site onto the axis it actually meant — `Spelling::Html` for IR questions, `InputMode::HtmlSegments` for unit questions, `constraints.html` for the layer-3 splice. **Its records task LANDED 2026-09-01**: ADR-0025, DCR-0034 and the contracts / rough-schema / module-map / `CLAUDE.md` / CHANGELOG edits are in the tree, so wave 2 is recorded where every other wave is rather than only in this file and the commit messages. **Wave 3 LANDED 2026-09-03** (DCR-0035): the HTML document intake and the identity round-trip. **Wave 4 LANDED 2026-09-03** (DCR-0036): the layer-6 twin and the one format branch — the gate that had to precede any HTML run. **Wave 5 LANDED 2026-09-03** (DCR-0037): the library entry point — the feature working. **Wave 6 LANDED 2026-09-03** (DCR-0038): panes, alignment schema 1.3.0, and the CLI — the operator-visible feature. **Wave 7 LANDED 2026-09-04** (DCR-0039): the shell-driven SCN-16 browser gate and the closure ledger — the feature is complete.
- **Action (HTML→HTML feature, ti `490d97`):** ~~wave 6 — panes, the alignment wire, the CLI~~ **LANDED 2026-09-03** (DCR-0038; five commits `5ba9783` → `b25ab75`): **the operator surface exists.** `transync translate --input-format html --input page.html --out-dir out/` writes `out.html` — the full page, head, scripts and doctype verbatim, and **anchor-free** — plus a bundle whose panes mount and sync by block id, proven by a direct-drive browser test over the real bundle rather than asserted. Alignment schema is **1.3.0**: per-row `source_format` (the block's SPELLING — a Markdown run's html-island row says `html`), map-level `input_format` (the run's INTAKE), and the `title` kind. Two facts, two names, never one field doing double duty, and forward-minor **by design** rather than by assertion — the engine's logic is untouched, its whole `sync.js` diff being the schema mirror, and a one-minor-newer map carrying exactly 1.3.0's additions is *driven* in the browser suite. The pane derivation is strip → inject into the block's own element → balance → group, with the transparent `<div>` kept for `html`-kind and element-less blocks: the ti `d4bce2` re-keying, whose ground is measured — DOMPurify removes an unknown element **and every attribute riding it**, so an anchor injected into `<x-note>` dies at the mount, and the earlier "is it named in §4's tables" key left `iframe`/`noscript` self-injecting into tags the sanitizer also removes. Keying on the kind refuses a second opinion about what DOMPurify accepts and needs no list at all. **Two silent-reintroduction routes are pinned shut**: `regen.rs` carries zero `data-sync-id` (injection is bundle-only, and `out.html` is read off disk in two independent places to hold it), and strip-then-inject goes red in both wrong orders. **The forward-specimen sweep found six hard-coded literals where the plan predicted three** — including one the plan did not list and two in a file it called untouched — so `forwardMinorVersion()` now lives in the shared harness and every specimen derives from the served engine's own constant; a literal specimen is precisely what this bump had to sweep. Acceptance: the hand-run demo exits 0 with an anchor-free `out.html` and no `out.md`; workspace 46 binaries / 1271 passed; CLI 217; browser 39 / exit 0; **zero** fixture edits; `exit_code_docs_drift` and `sync_js_drift` green at zero edits. **Two spec amendments are owed** and recorded in DCR-0038 (§7's over-broad `walk` sentence; §8 steps 3/4's wrapper population), carried to wave 7's batch. **Wave 7 — the browser gate and closure — is the last.**
- **Action (HTML→HTML feature, ti `490d97`):** ~~wave 5 — units, context, prompt: the library entry point~~ **LANDED 2026-09-03** (DCR-0037; eight commits `0fa8a08` → `b0c5c72`): **the feature works.** `TranslateOptions.input_format: SourceFormat` (additive, `Markdown` by default) routes a declared-HTML source through `intake::html` on the way in and through `layer6_gate` → `full_rescan_html` on the way out, and the routing is proven by a LIVE test rather than claimed: a Hard failure on an HTML run speaks the twin's vocabulary (`fresh segmentation`) behind the historic `full reparse failed: ` prefix, with `reparse_full`'s vocabulary asserted absent. Also landed: `[system].prompt_html` (additive key, shipped default body, selected at the cohort-compile door BEFORE the first compile so every re-compile and the template rewind inherit it; a profile without it falls back to the operator's `prompt` **verbatim** plus one advisory — never a core-synthesized merge); the run-level `InstructionVariant.html_document` clause with its single derivation and a `debug_assert` weld to `Document.format`; `heading_plain_text` projecting Html-spelled headings and the `<title>` through `transync_html::extract`; `transync::SourceFormat` across the facade with its §0 row; the re-texted dominance tail; and SCN-16's three legs. **The non-changes are the load-bearing claims**: NO new cache axis (contracts §5a carries the verdict — both prompt routes are already axes, and `CacheKey`'s exhaustive out-of-crate destructure makes an addition a red compile), NO `VALIDATION_SCHEMA_VERSION` bump (the bump rules' explicit "Do NOT bump" case), NO alignment-schema move, NO edit under `validate/` or in `finalize.rs` (the gate consumed, never re-implemented — verified by an empty containment diff), and Markdown runs byte-identical throughout: the three prompt goldens pass against **unregenerated** files with `git status` over the golden directory silent. **Two findings came out of execution.** The plan's routing lever (`[" "]`) does not reach layer 6 — `per_kind` rejects whitespace-only segments, defense in depth working — and the lever that does, U+FEFF, walks a real gap: it is not `char::is_whitespace` in Rust while rule T strips it, so a provider can blank a block's visible text with nothing downstream to catch it. Filed as ti `c887bc` with options, deliberately not fixed in a wave that adds no validator. **The scope recorded here on 2026-09-03 was wrong and is corrected below** — it read "for a raw-HTML ISLAND in a Markdown run (where layer 6 is `reparse_full`)", implying an HTML-document run was covered by the twin; verification found the twin's net is a singleton and the Markdown path had no check on any kind. And spec §6's context-projection hazard is stated backwards — the observed red was empty COLLAPSE (`[(1, "")]`), not raw tags — recorded as an owed §14 amendment in DCR-0037. **Waves 6–7 remain**; the operator surface (CLI flag, `out.html`, panes, alignment schema 1.3.0) is wave 6's.
- **Action (HTML→HTML feature, ti `490d97`):** ~~wave 4 — the layer-6 twin, the gate that must precede any HTML run~~ **LANDED 2026-09-03** (DCR-0036; `6503255`): `transync-core::validate::full_rescan_html` — the ordered document-wide tag ledger, fresh segmentation back through `intake::html::parse` (per-`<li>` under D9, attribution mirroring `attribute_offenders`), gap byte-identity including preamble and tail, and boundary sanity against the **actual** output length — plus `layer6_gate` in `pipeline::finalize`, the one format branch, exhaustive over `SourceFormat` with both arms named. **The reason the gate had to exist is sharper than "untested":** comrak over an HTML document produces *some* node sequence and can pass while checking nothing, so an HTML run gated by `reparse_full` is worse than ungated — it carries a gate's authority and none of its coverage. The recorded red shows it live: before the branch the failure reason read `source kind paragraph != regenerated kind html` and `FallbackPerBlock` over-downgraded past an honest translation on its way to `FallbackAll`; after it, stage 1 downgrades exactly the dissolved run and the translation survives. **Check 3 is a gate, not decoration, and that too is recorded rather than argued**: a dropped `<!DOCTYPE>` or comment leaves no *ledger entry* (each scans as a `Skip`, which `tag_inventory` filters) and mints no block, so checks 1, 2 and 4 all accept it — both blind-spot tests were observed RED against the two-check twin and green after check 3 landed. **The DCR-0004 cascade is untouched by diff, not by assertion**: `full_reparse.rs`, `pipeline.rs` and `pipeline/report.rs` are byte-untouched, and `finalize.rs`'s five hunks are three one-line call-site reroutes, `layer6_gate` and the new test module — no hunk in `downgrade_units` / `widen_to_neighbors` / `fall_back_all` / `regen_pass` / `collect_validated`. **No HTML run became reachable**, verified mechanically: one document constructor (`parse(source)?`, the Markdown intake), zero `input_format` in core's `lib.rs`, and `intake::html` in exactly two files. Acceptance: `-p transync-core` 480 passed with **zero** edits to any pre-existing test, workspace 46 binaries / 1234 passed / 0 failed, wasm gate exit 0, and an empty containment diff outside `crates/transync-core` and the named docs. **Two spec amendments are owed** and recorded in DCR-0036: §12's "parallel with 3" claim (check 2 consumes the intake, so this execution ran 3 → 4) and §7's blind-spot wording (the regions produce a `Skip` token; what they leave none of is a ledger entry). One deviation recorded: the plan's three commits became one, because `validate` is `pub(crate)` and `-D dead_code` fails the hook on a twin with no caller — every staged red was still produced and recorded.
- **Action (HTML→HTML feature, ti `490d97`):** ~~wave 3 — the HTML intake and the identity round-trip~~ **LANDED 2026-09-03** (DCR-0035; `949ec94` the module + fixtures + spine + three welds, `e257937` rule T's fine grain and the five-class table pinned per element, `1a977e2` warnings/hostile input/the invariant tripwire, `390051e` ids/sections/paths/the alignment map): `transync-syntax::intake::html` lands **beside** `parser`, not instead of it — the `intake::markdown` rename is deferred on record with its measured blast radius (30+ files, zero behaviour). Identity is a **theorem**, not a test hope: `regenerate` copies gaps verbatim and splices source ranges, so byte-identity holds exactly when the intake's ranges are ordered, non-overlapping and in bounds — which `parse` debug-asserts. The corollary shaped every test: a zero-block parse satisfies byte-identity trivially, so every spine test pairs the round trip with a block-set assertion, and the module landed with rule T deliberately classifying nothing first (2 passed / 3 failed, identity already green, the three diffs naming exactly the missing runs). **Four of the plan's premises had moved since it was written on 2026-08-20** and were re-derived rather than transcribed: the three passages wave 1 superseded (a `<div/>` opens and DOES mint an extent), plus `TagToken::Skip`'s two new fields from ti `c1f9a8`, which the plan does not mention and which make its `Skip { span }` patterns non-compiling. One pin was re-derived the same way — `<div>Price: <my-price/> today</div>` is **two** blocks, not the three the plan predicted, because `</div>` now closes the custom element implicitly and its trailing text is inside it, as a browser has it. The plan's blocking precondition (ti `e77173`, the foreign-content breakout gap) was **discharged** by DCR-0041 before the corpus was blessed. Acceptance: 46 binaries / 1214 passed / 0 failed, two-package wasm gate exit 0, **zero** edits to pre-existing fixtures or expectations, and a mechanically verified empty diff over `transync-core`, `transync-cli`, both provider crates, `transync-wasm`, `transync-html` and `web/` — the "no core change" half of the wave's claim. **One spec amendment is owed** and recorded in DCR-0035: §4's img-exception sentence, read literally, would strip the anchor from every `<a href><img></a>`, so the wave implements the reading the spec's own rationale states.
- **Action (HTML→HTML feature, ti `490d97`):** ~~wave 2 — the IR split, the breaking window~~ **LANDED 2026-09-01** (ADR-0025 / DCR-0034; code 2026-08-24, record 2026-09-01, both dated from `git log --date=short`): `Spelling` and `SourceFormat` joined `BlockKind` in `transync-syntax::id`, `Block` gained `spelling` and `Document` gained `format`, `BlockKind::Html` narrowed to a unit variant, and `BlockKind::Title` landed with the **two explicit arms the compiler could not ask for** — `align::sync_role_for` ends in `_ => SyncRole::Anchor` and would have made a title an anchoring row, and `unit::context::document_title` matched `Heading1` alone, so a title did not enter the document title for free. Both got tests that assert the answer, not the compile. Nine dispatch sites were re-keyed onto `Spelling::Html` / `InputMode::HtmlSegments` / `constraints.html`, and the `(Table × Html)` split exclusion is explicit and tested. The workspace is at **`0.5.0-dev`** and the **v0.5.0 window is open**. Acceptance held: workspace + CLI suites green with **zero fixture edits**, the two-package wasm gate exit 0 with its string unchanged. **Waves 3, 4, 5 and 6 then LANDED 2026-09-03** (DCR-0035 through DCR-0038) and **wave 7 is unblocked** (dependency order `0 → 2 → {3, 4} → 5 → 6 → 7`); wave 4's layer-6 twin was a **hard precondition for any HTML translation run** and is now **discharged**, and neither wave made a run possible.
- **Action:** Steady-state maintenance alongside: the OI-0016 perf residual (Review 0006; revisit behind a profiling gate; OI-0015 closed 2026-08-09 with `d3acc3`) remains trigger-gated, and it is now the **only** one — **no Review-0008 follow-up is still open**. All four are closed: OI-0017 (2026-08-09, DCR-0028's `Cache` metadata seam + `--cache-dir`), OI-0018 (2026-08-07, `--strict-csp`), OI-0019 (2026-07-13, provider resource bounds) and OI-0020 (2026-08-06, committed lockfile). The six TicGit tickets that stood open after the two post-0.2.0 waves (`0fba5082`, `69b9b3db`, `294ddabd`, `2a276cb4`, `92ac61b9`, `0c6a5c8b`) were **all resolved 2026-08-05**, commits `242f03a`..`eab1111` — the five the waves themselves named carry dated resolution notes on the wave bullets above, and `0fba5082` (the CHANGELOG's `[Unreleased]` / `[0.2.0]` link stanza) was fixed in `242f03a`. Open work is deliberately **not enumerated here**: the ticket backlog is TicGit (`ti list`, whose current open set is the Review-0001 residual triage filed 2026-08-05), and the index of open items — deferred findings, follow-ups, and residuals — is **`docs/backlog.md`**.
- **Action (review-0009 decision gate, ran 2026-08-25 → 2026-08-29):** ~~triage the round and land its `fix`-routed findings~~ **DONE** — **26 findings fixed** across `88964df` (eleven), `8d4efda` (six, one of them a breaking removal riding the open v0.5.0 window) and `69701cd` (nine). The three *shipped behaviour* changes — the splice's two-pass agreement becoming a real `Err`, `serve`'s 60 s response deadline, and `ProviderError::RateLimited`'s removal — are recorded in **DCR-0040** (`5ff6d17`), which also gave ADR-0002 a dated amendment and corrected `contracts.md` §4a to describe the probe `mountSync` actually has. **Still open from the same gate:** the thirty-five findings routed `track`, plus R0009-0014, are registered as **OI-0039..OI-0048** in `docs/project/open-issues.md` with ten paired `docs/backlog.md` entries (`e663422`). They carry **no TicGit ticket by design** — `reopen`'s selection gate decides which of them become work, so picking one up means filing it first. R0009-0014 (the pre-commit hook's JS leg validating nothing while exiting 0) is the one `fix`-routed member left half-done: `88964df` made the skip visible and deliberately refused to make the *status* honest, because both routes out — adopt a linter and conform ~190 KB of existing JS including `sync.js` and its drift-pinned twin, or fail on staged JS and block work in progress — are decisions, not fixes. **All ten count against the v0.5.0 gate**; `docs/backlog.md`'s header carries the rule and the census, and `release-checklist.md` step 2a is where a release consults it. That also dates the bullet above twice over. The OI-0016 perf residual was the only open trigger-gated item when that sentence was written and is no longer the only open register entry; and TicGit's open set is no longer "the Review-0001 residual triage filed 2026-08-05" — the 2026-08-24 sweep (`e89b537`) found **24** open tickets, **18 of them missing from `docs/backlog.md`**, and indexed all 24 there. Read the backlog's header for the current census; read `ti list --all` for the current queue.
- **Action (release ritual, gate new 2026-08-03; checklist landed 2026-08-06):** The ritual has a dedicated home — **`docs/project/release-checklist.md`**, a living document covering preflight, the standing gates (`smoke.sh` incl. the rustdoc gate and the wasm size budget, Playwright, the sibling-consumer path-dependency checks — `resp-translator` and `dynwebserver`, step 8), the DCR-0015 live-endpoint gate, the CHANGELOG promotion **and its reference-link stanza**, the single-line workspace version bump, the record updates, and the annotated-tag ritual. Follow it for every tagged `vX.Y.Z` release. **The v0.4.0 ships-untagged decision (owner, 2026-08-13) was reversed by the owner on 2026-08-20**, the day the release landed, because consumers need a ref to depend on: the annotated tag `v0.4.0` exists on `6fa4e88`, `git tag -l` lists it, and the checklist's *When it applies* heads that section **"Superseded. `v0.4.0` is tagged."** while keeping the 2026-08-13 reasoning below it unedited. Both decisions are dated records and both are readable there; of the nine steps the original decision changed, seven are spent and **two — steps 3 and 10 — are still live**, because they rest on the 2026-08-17 history restart rather than on the tag (no `v0.3.0` exists to name, and no `v<prev>..HEAD` range can be formed). **Step 2a is new (added 2026-08-29)** and is where a version's owner-stated window-closing condition is checked before the cut: for **v0.5.0** that condition, stated by the owner 2026-08-24, is that 0.5.0 proper ships only after every **registered task** and every **open issue** is resolved, excluding those explicitly deferred — the *union* of the TicGit queue and the open-issue register, censused in `docs/backlog.md`, whose header carries the rule and the counts. The gate rule itself is unchanged: before tagging a release that touches `transync-openai`, `llm::prompt`, the output schema, or batching, run `./scripts/smoke-live-gate.sh` and record the date + models used in that release's CHANGELOG entry (OI-0030 / DCR-0015).
- **Owner:** Maintenance.
- **Why:** MVP is complete and all 14 mandatory scenarios pass; remaining work is post-MVP quality/perf hardening tracked as review-gate follow-ups, not new-implementation slices.
