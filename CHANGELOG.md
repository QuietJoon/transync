# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

The decision gate over independent reviews **0010** and **0011** (188 findings
between them). 63 were routed `fix` and applied here; 3 were recorded as ADRs
(0029–0031); 4 are tracked as open issues (OI-0050…OI-0053), two of them
waiting on a breaking window; the rest were rejected or dropped, most of them
against a record that already ruled on the question.

**Two breaking changes are sitting in this section, and they need a window.**
v0.5.0 closed the sanctioned window it had carried since 2026-08-24, and the
rule is unchanged: a further breaking change needs a **new** window and the
owner decision that opens one. Both are below, both are invisible through the
`transync` facade, and both are breaking only for a consumer that depends on
`transync-syntax` directly: `LineOffsets::offsets` becoming private
(R0010-0023), and the Markdown intake's move from `parser` to
`intake::markdown` (DCR-0053). They are recorded here rather than held back so
the decision is taken deliberately — either the next release is the minor bump
that carries them (alongside OI-0051 and OI-0052, which are queued for exactly
that window), or the field goes back to `pub` and only the accessor ships. The
rename has no such fallback: it is the discharge of a deferral DCR-0035 wrote
down, and reverting it would re-open the asymmetry that record exists to
explain.

### Changed (BREAKING)

- **`transync_syntax::intake::markdown::ranges::LineOffsets::offsets` is
  private**, and a read-only `line_starts() -> &[usize]` accessor takes its
  place (R0010-0023). The field and its `source` are one fact: every method
  reads the table as `new`'s output *over that source*, so an entry a caller
  could reassign is a line start describing bytes that are not there. The
  `transync` facade is **not** affected — `public_surface.rs` pins the module
  hidden, so it never resolved through the facade — but `transync-syntax` is
  itself a published crate and `ranges` is reachable through a `pub mod` chain,
  which is what makes this breaking. The saturating guards in
  `line_content_end` and `pos_to_byte` stay: their comments now say they defend
  against an in-module edit rather than a caller, which is a narrower claim, and
  `pos_to_byte`'s oversized-`col` exposure is untouched because a column is
  still whatever an arbitrary caller passes. (Its **path** also moved in this
  same window — see the rename below — so a direct `transync-syntax` consumer
  reaching this type sees both changes at once.)

- **The Markdown intake moved from `transync_syntax::parser` to
  `transync_syntax::intake::markdown`** (DCR-0053, ticket `b7e4cc70`,
  `18c1ab4`), making the format seam symmetric: `intake::html` and
  `intake::markdown` are now siblings under the module that names the seam, the
  shape ADR-0025's D3 specified and DCR-0035 deferred. **No behaviour changes** —
  the workspace tally is identical to the pre-rename baseline. Like the entry
  above, this is invisible through the `transync` facade, which never re-exported
  the module, and breaking only for a consumer depending on `transync-syntax`
  directly. It is listed here rather than under "Changed" because that is the
  same audience and the same unopened window.

### Changed

- **Glossary and section keys use Unicode full case folding** instead of
  `str::to_lowercase` (R0010-0063). They are different operations, and the
  difference is the identity question the key exists to answer: a case
  *mapping* leaves `ß` as `ß` (so `Straße` and `STRASSE` were two terms) and
  the long s `ſ` as `ſ`, and Rust's `to_lowercase` *introduces* final sigma (so
  `ΟΔΟΣ` and `οδοσ` were two terms, in the opposite direction). This **extends**
  DCR-0027's 2026-08-12 amendment rather than reversing it: that amendment
  settled the composition half — NFC, and that it runs *after* the fold — and
  composition is untouched. `caseless` is not a new crate in the dependency
  graph; it is the crate comrak's `unicode-normalization` edge already comes
  through, so the lockfile gains one edge and no package. **Cache consequence:**
  `glossary_key`/`section_key` are a `CohortDigest` axis (contracts.md §5a), so
  a cached run whose glossary carries a non-ASCII term will **miss** rather than
  mis-serve, and re-translate it once.
- **`--strict-csp` adds `form-action 'none'`** (R0011-0033). It is the one
  directive in that policy guarding a *navigation* rather than a load, and
  `default-src` does not cover it, so a `<form>` the sanitizer kept from
  untrusted source content was a live off-origin submit target for a reader's
  typed input. contracts.md §6, `docs/Developer_Guide.md` and the archived
  OI-0018 resolution line all carry the amended policy.
- **The shipped shells stop overlaying their own content.** The tint legend and
  the theme picker were `position: fixed`, so both sat on top of whatever pane
  content scrolled under them and no pane inset could reserve space for that at
  every scroll position (R0011-0085, R0011-0086). Both are now rows of the body
  grid, which overlap nothing by construction. Applied identically to the
  `--html-out`/`--out-dir` bundle template and the `web/index.html` demo shell,
  so the two cannot drift.
- **The sync engine's smooth follow is time-based and respects
  `prefers-reduced-motion`** (R0011-0094, R0011-0093). The lerp closed a fixed
  fraction of the gap *per callback*, so the same gesture settled twice as fast
  on a 120 Hz display as on a 60 Hz one — the refresh rate deciding a product
  behavior. It is now applied per elapsed millisecond against a nominal frame,
  with a ceiling so a backgrounded tab cannot spend its whole arrears in one
  jump; under reduced motion the partner is placed rather than glided. Both
  `sync.js` twins move together, as `sync_js_drift.rs` requires.
- **`scripts/smoke-live.sh` builds once and runs the binary it built**
  (R0011-0055), instead of a workspace build followed by two `cargo run`
  invocations that re-resolved and could rebuild under a different feature
  unification. The binary is resolved from cargo's own JSON — filtered to the
  `transync` bin target, refusing anything other than exactly one match
  (R0011-0013's rule, taken here too) — so it keeps working under the
  workspace's redirected `CARGO_TARGET_DIR`.
- **`scripts/test-browser.sh` parses cargo's JSON instead of regexing it**
  (R0011-0012) and selects the `transync` bin target rather than the last line
  carrying a path (R0011-0013).

### Fixed

- **`scripts/smoke-live.sh` and `scripts/smoke-live-long.sh` were broken in
  every configuration** (R0011-0001, R0011-0002). `DEFAULT_WORKDIR="$(…)"WORKDIR="…"`
  is *one* shell assignment word, not two, so `WORKDIR` was never set and the
  script aborted under `set -u` at its first use; the long wrapper fused an
  assignment with `export` the same way and exported nothing. Both are
  syntactically valid, so `bash -n` and shellcheck were silent — the real gap is
  that the scripts have not been executed since `a30b40d` (2026-09-03); the only
  live-run gap `status.md` tracks is the DCR-0015 endpoint gate
  (`smoke-live-gate.sh`), which never invokes either of these two. A sweep of the
  other scripts that commit touched found no third instance.
- **A run could overwrite its own input** (R0010-0003, R0010-0004).
  `vet_destinations` compared only the output set, so `--output`, `--map`,
  `--report` or a bundle filename naming `--input`, `--profile` or
  `--system-prompt-file` published the translation over the document being
  translated — silently, because the source was already in memory and the write
  was a legitimate publication of a successful run. `--out-dir` had the wider
  version: an input *inside* the target is destroyed without any destination
  naming it. Read-side identities now enter the same preflight, before the
  provider is called. **`--force` does not waive it** — that flag says an
  existing *output* may be replaced, and no argv spelling asks a run to destroy
  its own inputs.
- **A retry round no longer discards the work its earlier sub-batches did**
  (R0010-0006). A terminal error from a later split sub-batch returned before
  the per-unit walk where `cache_put` runs, so units accepted earlier in the
  same round were never cached and had to be re-translated on resume. ADR-0017
  still aborts the run; only the cache write is recovered.
- **An explicitly requested tokenizer vocabulary no longer silently becomes a
  different one** (R0010-0028). `encoder_for_hint` fell back from `o200k_base`
  to `cl100k_base`; ADR-0010 rules on the both-fail case, not on this one.
- **Diagnostics cannot be made to reorder what a terminal shows** (R0010-0068).
  `diagnostic_line` escaped category Cc only, so the nine explicit bidi
  formatting characters — reachable through a provider-authored
  `rejection_reason` or `warning`, i.e. through untrusted source content under
  invariant 7 — passed straight through and could render a diagnostic as a path
  or verdict it does not contain. They are escaped now; `ZWJ`/`ZWNJ` are
  deliberately not, because they carry no reordering power and belong to
  legitimate Persian and Indic terms.
- **A mid-iteration directory-entry error in the staging sweep is reported**
  rather than dropped by `.flatten()` (R0010-0017), which was the more
  misleading half of that silence: the `read_dir` open failure was already
  reported, so the sweep could claim success having never seen the entry it was
  looking for.
- **Bundle-only flags say so consistently.** `--title` and `--target-direction`
  joined `--strict-csp`'s existing note when a run emits no HTML bundle
  (R0010-0059), instead of two of the three doing nothing silently.
- **The stub-provider build no longer skips the preflight it exists to test**
  (R0010-0029). The `test-stub-provider` arm skipped `Url::parse` and
  `ModelId::parse`, so the feature that exists to test the CLI tested a CLI with
  a different preflight; the parse is now shared and feature-independent.
- **Blank-line collapse recognizes lone-CR and CRLF blanks** (R0010-0053), not
  just LF.
- **Live-smoke and live-gate preflights happen before the paid work**, not
  after: bind/port/allow-host go through the real `serve` argv (a no-side-effect
  probe that never opens a socket), target language, profile path and
  system-prompt path are checked before `cargo build`, whitespace-only API keys
  and padded model / non-HTTP base-URL overrides are refused by name
  (R0011-0007 through R0011-0011, R0011-0049 through R0011-0051, R0011-0057,
  R0011-0058).
- **Hook installation reports what it actually did** — a failed `chmod` is no
  longer reported as success (R0011-0015), force-install can override worktree
  and command scope (R0011-0014), remediation commands survive apostrophes in
  paths (R0011-0060), and the pre-commit helper's probe variables stop leaking
  to global scope (R0011-0062).
- **Compaction temp creation cannot truncate an existing file** (R0011-0005),
  its parsing validates the timestamp field (R0011-0035), and a cache put
  serializes from the borrow instead of cloning the whole result first
  (R0011-0071).
- **A provider body-read failure keeps the HTTP status and the retry hint it
  already knew** (R0011-0026), and default endpoints are parsed once rather than
  per request (R0011-0073).
- **The WASM demo panes carry `lang` and `dir`** (R0011-0038, R0011-0039), a
  click on a control inside an editable block keeps its own click instead of
  also opening the editor (R0011-0041), and the editor is reachable by keyboard:
  editable blocks are tab stops and Enter/Space open them (R0011-0042).
- **The shells announce what they are doing.** Panes ship `aria-busy` and a
  visible loading copy that every boot exit clears (R0011-0091), fatal boot
  errors reach a `role="alert"` region instead of the console alone
  (R0011-0090), a sync refusal is stated on screen while both rendered documents
  are kept (R0011-0031), translation status carries a border cue and a clipped
  text label rather than colour alone (R0011-0088), and a stored theme is
  accepted only when the selector offers it (R0011-0089).
- Documentation: the CLI reference stops always naming `out.md` for out-dir mode
  (R0011-0080), the manual introduction stops describing Markdown only
  (R0011-0081), and the WASM build doc stops repeating a workdir fallback the
  helper refuses (R0011-0082) — as do two live-smoke comments that promised the
  same nonexistent fallback (R0011-0052, R0011-0053) and the long-test help text
  that named the wrong port (R0011-0054). An unrecognized
  `TRANSYNC_WORKDIR_POLICY` is now refused by name instead of behaving as
  `refuse` (R0011-0061).

### Security

- **Sanitized inline styles can no longer cover trusted chrome** (R0011-0032).
  DOMPurify's default profile keeps the `style` attribute — the shells' CSP
  comment explicitly accommodates it — so a type-6 html block carried in from
  untrusted source Markdown could ship `position: fixed` and paint over the
  opposite pane or the shell's own controls. Each `.pane` is now a paint
  containment boundary. Containment rather than stripping is deliberate: `style`
  is how a source document's own presentation survives into the pane at all, and
  removing it would change what every legitimate html block renders in order to
  close a case a boundary already closes. Recorded in contracts.md §4 (the
  html-block success bullet).
- `form-action 'none'` (above) and the bidi escaping (above) are the other two
  entries of this kind in this section.

### Also in this window — the Type 1 / Type 2 backlog pass (2026-09-06)

Nineteen backlog entries turned out to be **stale rather than open** — work that had
landed without the register being reconciled — and each now carries a dated note with
the evidence rather than a bare claim. Three were genuinely open and are fixed here:

- **A panicking `serve` connection task is no longer discarded along with its panic**
  (OI-0050 / R0010-0077). All three `join_next()` sites hand the joined
  `Result<(), JoinError>` to one reporter, which stays silent for a task that finished
  on its own terms and otherwise names the death as a **panic** — carrying the task id
  and payload, and the sentence that it is a bug in transync rather than a peer failure
  — or a **cancellation**. Nothing became fatal; the accept loop keeps going. The
  per-connection I/O policy is untouched: `let _ = conn::serve(...)` is deliberate, and
  its "a dead socket is the peer's business" comment remains that decision's record.
- **The two cancellation races stopped being timed** — ti `d41782`'s **option 2**, which
  that ticket named as the strongest outcome, declined to take, and left explicitly
  available. Its option 1 (2026-09-01) had widened the bounds to `CAPPED_BACKOFF / 2`
  and `IGNORED_SLEEP / 10`, which removed the recurring flake cheaply but left a
  stopwatch measuring a host, and one of the two stalls being ruled out is only 30 s, so
  widening has a ceiling. The property both bounds reached for is **abandonment**, not
  speed: it is now pinned by a scheduler-turn budget (yielding does not move the clock,
  so a run sitting out a `tokio::time::sleep` is pending on every turn) and by a counter
  the ten-minute provider increments only past its sleep. Both regression signatures are
  unchanged and both tests still fail decisively against them.
- **Bin-only crates entered the rustdoc gate.** `scripts/lib/rustdoc-gate.sh` gained
  `RUSTDOC_GATE_BIN_CRATES` and a `--document-private-items` leg over `transync-cli`, run
  by both callers, plus a mirrored completeness check for any member with a `src/main.rs`
  and no `src/lib.rs` that is not listed. It is a **second** `cargo doc` invocation rather
  than two more `-p` entries, because `--document-private-items` suppresses the
  `private_intra_doc_links` lint the library leg exists to catch. The nine links this was
  filed for had already been repaired in `2ca092f`; what was missing was the gate that
  would have reported them, which is what this closes. Consequence for the next
  release-prep: `scripts/smoke.sh` now runs **eight** command steps plus its `OK` summary
  line, where the `[0.5.0]` entry below correctly records seven for that cut.

### Decisions recorded

- **ADR-0029 — the `--out-dir` ownership marker is a name, not a credential.**
  Recognition is the filename and file type; the body is documentation and is
  never compared. Safe because an unmarked target is not refused, it falls
  through to the complete-fileset check — and a byte-exact compare would buy
  nothing against an actor who can write into an operator-named output directory
  while making a checkout-converted marker demand `--force`, the false refusal
  OI-0036 action 3 was written to avoid. (R0011-0003.)
- **ADR-0030 — the WASM publish lock fails closed, and the remedy is a human.**
  No automatic stale-owner recovery: the lock is held for two renames, `INT` and
  `TERM` are routed through `exit` so only `SIGKILL` can strand it, and every
  automatic break has a wrong branch whose consequence is the nested-staging
  corruption the lock exists to prevent. The refusal already names the remedy.
  (R0011-0016.)
- **ADR-0031 — a provider request budget is taken verbatim.** `with_timeout` is
  infallible by design and applies no floor or clamp, so `Duration::ZERO` is
  accepted and fails every request immediately while `request_timeout()` still
  reports zero — which is that accessor's one guarantee. Validating a domain
  would break an infallible builder to catch a mistake that already announces
  itself. (R0011-0075.)

### Tracked, not fixed

- ~~**OI-0050**~~ **RESOLVED 2026-09-06** — fixed the same day it was filed, in the
  backlog pass recorded above. It is listed here anyway because the gate routed it
  `track`, and that routing is part of this section's record.
- **OI-0051** — every batch clones its profile and carries a second copy of its
  glossary. The allocation is modest; the reason to fix it is that the
  glossary-sensitive cache key reads one field while the prompt is compiled from
  the other. Needs a breaking window.
- **OI-0052** — the checked provider constructors accept header values that
  cannot become headers. Needs a breaking window (a new `ConfigError` variant).
- **OI-0053** — the shells have no small-screen layout and no visible pane
  headings. One scope question, not three defects; a narrow-viewport commitment
  also implies a viewport dimension in a browser matrix that is Chromium-only
  under ADR-0026.

## [0.5.0] - 2026-09-05

**This release CLOSES the sanctioned v0.5.0 breaking window.** It was opened by
wave 2 of the HTML→HTML feature (ti `490d97`, ADR-0025, DCR-0034), which splits
semantic kind from source spelling in the block IR. **Seven** breaking-by-policy
changes rode the window. Wave 2 contributed four, batched, and none of the four
is user-visible: the corpus regenerates, renders and aligns byte-identically
with zero fixture edits. The other three are not wave 2's. The **fifth** is
`transync-openai`'s unreachable `ProviderError::RateLimited`, removed under this
window by the review-0009 fix pass (`8d4efda`, DCR-0040 §3). The **sixth** is
`transync_html::TagToken::Skip` gaining `kind` and `terminated` (ti `c1f9a8`,
DCR-0047). The **seventh** is the `SkipKind::PlainText` variant DCR-0050 added
(`55e2745`, 2026-09-05) to the enum ti `c1f9a8` had introduced two days earlier
(`5d5c7a2`, 2026-09-03) — it is
counted here rather than left implicit, because DCR-0047's own Semver paragraph
is the criterion and it reaches this change verbatim: `SkipKind` is `pub` and
exhaustive-by-policy in a published crate, so a variant addition breaks a
downstream match and is legal **only** while the window is open. All three are
listed first below — in **six** bullets for seven changes, because
`Block.spelling` and `Document.format` landed in one commit (`258ba01`) and
share theirs.

**What this release is actually about.** Nearly every defect it fixes was one
defect wearing different faces: `transync-html` hand-models HTML and kept **two**
open-element stacks — `scan_tags`' and `walk_elements`' — which popped
differently from each other and from a browser. Every divergence ended
identically, with the sync wrapper's own `</div>` swallowed and the following
block's anchor mounted in the wrong parent or destroyed, reachable from
untrusted source Markdown through one CommonMark type-6 html block. DCR-0051
merged them into one stack that the scanner owns and the walk replays.

What made it visible is DCR-0049/ti `ec235f`'s **browser oracle**: before it,
41 byte-exact goldens, 103 unit tests and correctly-stated properties had
certified this code sound, because every oracle the harness had was computed
from the functions under test, so both sides of every comparison shared the
bug. Measured on a byte-identical generator alphabet, `sec4a:break-nested` fell
from **1,358 to 8** per 10,000 generated inputs and `reserved:dom-only` — a
live `data-sync-id` reaching a real DOM, i.e. anchor injection — fell from
**1 to 0**.

**Release gate (OI-0030 / DCR-0015 Part C): TRIGGERED and NOT RUN.**
`git diff --stat v0.4.0..HEAD` over the trigger paths shows `transync-openai`
(163 lines), `crates/transync-core/src/unit` (1,000+ lines) and
`crates/transync-core/src/llm/prompt.rs` (140 lines — the run-level
HTML-document instruction clause, and the most gate-relevant change in the
range), so the gate is triggered by inspection rather than by recall. It was
not run because it requires a live API key and network, which were not
available at cut time. **This is the third consecutive release to be cut in
that position**, after v0.3.0 and v0.4.0 — said of the *cut*, because v0.4.0's
gate did run, and passed, later the same day. The pattern the checklist names
is the honest diagnosis: nothing in the
ritual forces a key to be available when the release is cut, so the gate keeps
landing *after* the release instead of gating it. Stated here rather than
omitted, because an absent line is indistinguishable from a forgotten one.

**Standing gates at the cut**, each captured bare-to-file with an exit marker
appended after the native status: `scripts/smoke.sh` **PASS** across all seven
steps (workspace build, the `wasm32-unknown-unknown` gate, workspace tests, the
CLI stub suite, the rustdoc gate over all eight members, `scripts/build-wasm.sh`
with its size budget, and a CLI dry run) — **1,565 passed / 0 failed /
8 ignored**, which splits **1,343 / 0 / 8** across the workspace suite's 47 test
binaries and **222 / 0 / 0** across the CLI stub suite. Both halves were re-run
**after** the version bump, so the numbers describe the tree the tag names and
not the one before it. `scripts/test-browser.sh` **45 passed / 0 failed**;
`cargo clippy --all-targets --all-features -- -D warnings` zero warnings;
`cargo fmt --all` a no-op; the five docs-drift suites **17 passed / 0 failed**;
biome format clean over the 12-file `git ls-files` corpus. The wasm demo
artifact measures **raw 1,671,041 B / gzip 686,513 B**, inside
`scripts/build-wasm.sh`'s budget — measured on the last build of a gitignored
artifact that is not in the tag. And `cargo publish --dry-run --workspace`
**exit 0** — the first release to run and record it: all **eight** publishing
members packaged and verify-built in their own sandboxes against the temporary
local registry, with `transync-wasm` skipped by itself on `publish = false`.

### Changed (BREAKING)

- **`transync_html::TagToken::Skip` gains `kind: SkipKind` and `terminated: bool`** (ti `c1f9a8`, DCR-0047), and `SkipKind` is a new `pub` enum. `TagToken` is exhaustive by policy, so a downstream pattern or construction naming `Skip { span }` needs `..` or the two new fields. The fields carry what the scanner knew and nothing downstream can recover — notably that `<![CDATA[` is a CDATA section in foreign content and a *bogus comment* in HTML content, which the balancer cannot tell without the scanner's content-mode stack. Having it re-derive them from the bytes would have been a second opinion about where a comment ends, which is the defect ti `415cdb`, ti `e20490` and ti `2e2453` each were.
- **`transync_html::SkipKind` gains the variant `PlainText`** (ti `bebebe`, DCR-0050). `SkipKind` is `pub` and exhaustive-by-policy in a published crate — the same premise DCR-0047's Semver paragraph states about `TagToken` — so a consumer's exhaustive `match` over it needs the new arm. The variant exists because HTML's PLAINTEXT state is the one tokenizer state with **no exit**, which is the arm ti `c1f9a8`'s terminate-or-delete rule never had: a `<plaintext>` start tag in HTML content and every byte after it are one region, and the region is deleted start tag included. `SkipKind::terminator()` answers `None` for it, as it does for `UnterminatedTag`. The addition landed two days after the enum itself (`5d5c7a2` on 2026-09-03, `55e2745` on 2026-09-05), both inside this window; after the tag the same change waits for a new one.
- **`transync_openai::error::ProviderError::RateLimited` is removed** (`8d4efda`, R0009-0085, DCR-0040 §3). The variant was **dead**: an HTTP 429 has always produced `RateLimitedAfter`, and the bare variant appeared only in the mapping arm it could not reach. `ProviderError` is `pub` in a published crate and carries no `#[non_exhaustive]`, so removing a variant breaks any consumer that named it — construct or match **`RateLimitedAfter(None)`** for the no-hint case, which is also the one-variant end state the sibling Anthropic adapter already shipped. Permitted by this window; outside one it would have waited.
- **`BlockKind::Html { block_type: u8 }` narrows to the unit variant `BlockKind::Html`** (ti `490d97` wave 2, ADR-0025 / DCR-0034). The variant now means one thing — "HTML content with no semantic equivalent" — instead of two. The CommonMark block type moved to the new `Spelling::Html`, where it was always spelling metadata. A pattern or construction naming `block_type` no longer compiles; a bare `BlockKind::Html { .. }` pattern still does, and is now misleading.
- **`BlockKind` gains a `Title` variant** for an HTML document's `<title>`: `id_code` `"title"`, `wire_str` `"title"`, `heading_level()` `None`, and an **explicit** `sync_role` of `non-sync` — a title is translated and aligned but has no DOM anchor, because the browser chrome renders it and the pane has nothing to anchor. Every exhaustive `match` over `BlockKind` in a consumer's tree needs the new arm.
- **`parser::Block` gains `spelling: Spelling`; `parser::Document` gains `format: SourceFormat`.** Engine-tier (§0 tier (c)) types, reached only by a consumer depending on `transync-syntax` directly. Per-block spelling is required rather than stylistic: one Markdown document interleaves HTML-spelled islands with Markdown-spelled blocks, so no document-level bit can carry the axis.

### Changed

- **`transync-html` now keeps ONE open-element stack, and it is HTML's** (tickets `9b4d66`, `307283`, `895fb7`, `da6bb5`, `bb961a`, `a7e625`; DCR-0051). `scan_tags` kept a content-mode stack and `walk_elements` kept an open-element stack, and the two popped differently from each other **and from a browser** — the single root cause behind five open tickets, two of which put an attacker-chosen `data-sync-id` into a live DOM and thereby defeated the render-side strip that OI-0035's closure depends on ("ours are the only sync attributes in this DOM" is a construction, not a scan). The in-code comment licensing the divergence claimed an extra stale frame "cannot change the answer"; every clause of it was true and the conclusion did not follow, because it reasoned about the extra frame's own content mode and never about `truncate` popping the frames above it — and it cited its own falsifier under a name that has never existed in this repository. The scanner now keeps THE stack, reports every mutation on the token (`pre_pops` plus a `Push` / `Close(n)` / `Inert` / `Orphan` effect), and the walk **replays** it, adding only extents, close spans and orphan spans. Each frame carries two content modes rather than one — `child` for the tokenizer, `own` for the namespace — because `<svg><desc>` needs both and reading the wrong one is ti `bb961a`. What the one stack gained, each **measured in headless Chromium before it was written** rather than derived from the crate's own answers: in-body's "any other end tag" stopping at the first **special** element; HTML's **four scopes**, including the table scope that leaves `<div><table>a</div>`'s table open where the crate used to pop it and let the pane's own `</div>` be ignored the same way; foreign content's end-tag dispatch **in the spec's step order**, with `</p>` and `</br>` as BREAKOUT end tags; the start tags "in body" **ignores** (`<td>` outside a table opens no element and earns no closer); implied end tags applied by **scope** rather than at the top of the stack; and the `mglyph` / `malignmark` dispatcher exception inside MathML text integration points, which ti `a7e625` filed as spec-traced and unmeasured and the oracle **confirmed**. "Orphan close tags are dropped" narrows with it: a closer stopped by a special element or a scope terminator *inside* the fragment is now kept, because a browser stops in the same place and deleting on an approximation is not deleting on a verdict — and `</p>` / `</br>` are never orphans at all, since a browser turns each into content. Measured over the browser oracle's 10,000 generated fragments on an **unchanged atom alphabet**, so the numbers compare directly with DCR-0050's pin: agreement **7,697 → 8,619**, mechanisms **13 → 9**, `sec4a:break-nested` **1,358 → 8**, and `reserved:dom-only` **1 → 0** — that last being the only mechanism that ever put a live impostor anchor in a real DOM. Abstentions barely moved (3,874 → 3,863), which is what says the agreement did not rise by making Chromium stop answering. Three of the four `KNOWN_DIVERGENT` routes measure HEALTHY and were deleted from **both** halves of the ledger. No golden moved anywhere in the workspace, and that is checkable rather than lucky: no golden corpus here contains a misnested formatting element, a stray table-section start tag or a foreign-content end tag, which is the same coverage gap the generative harness and the browser oracle were both written for. The **adoption agency algorithm is deliberately not implemented**; the residual is measured (8 of 10,000), recorded in `contracts.md` §4b and owned by ti `525bef`.
- **Chromium is now an independent oracle for `transync-html`'s walk and balancer** (ti `ec235f`, from OI-0046's residual). The generative harness already stated the right properties — the four defects found during the `fdd989` verification all violate P1 or P6 — so the properties were never the gap. Two other things were, and only one of them was coverage. Its oracles were built from `scan_tags` / `walk_elements` / `balance_fragment`, i.e. **from the functions under test**, so a divergence between this crate's stacks and a browser's tree construction was invisible *by construction*: both sides of every comparison shared the bug. `web/tests/html-oracle.spec.js` fixes that by making the two columns come from different places — the Rust emitter writes the generated fragments together with THE CRATE's answers, and the spec computes CHROMIUM's answers itself. The emitter is double-gated (`#[ignore]` plus `TRANSYNC_HTML_ORACLE_CORPUS`), so `cargo test --workspace` still needs no browser, and an absent or empty corpus is an explicit FAIL in `scripts/test-browser.sh` rather than a silent skip. The oracle **proves it can go red**: a third test feeds it a deliberately lying crate column and asserts the comparison catches it — an oracle that cannot fail is not evidence. First run: **10,000 generated cases, 7,511 in full agreement, 2,941 browser-declared abstentions, and 12 distinct divergence mechanisms**, of which `sec4a:break-nested` accounts for 1,490 — so a `contracts.md` §4a wrapper break occurs on ~15% of generated inputs, not on the handful of hand-found routes. The census also splits cleanly by direction and thereby names the two missing halves of HTML's algorithm: 2,126 cases where the browser keeps elements open that the crate closed (no adoption agency, no special-element guard — the crate over-pops, ti `307283`) against 307 where the crate keeps frames the browser popped (no implied end tags — it under-pops, ti `9b4d66`). The four known-divergent routes are pinned in a ledger held in TWO halves that must agree, and an entry that becomes healthy **fails the test** and tells you to delete it in the commit that fixed the ticket, so the ledger retires itself instead of rotting. The generator additionally reaches stale-frame shapes it never did: mis-nested formatting elements, breakout tags inside foreign content, and close tags matching a non-innermost frame.
- **The pre-commit hook's JavaScript leg validates JavaScript** (OI-0039). It used to print a loud SKIP and `exit 0`, so "the pre-commit hook passed" was a statement about Rust alone — and with no CI in this repository, that hook is the only automatic gate, leaving ~318 KB of JS with no format or lint coverage. Now: `biome.jsonc` at the repo root with the **formatter enabled and the linter deliberately disabled**, `@biomejs/biome` pinned exactly (formatter output is not covered by semver), and the hook's JS leg rebuilt around a `git ls-files` corpus invoked **from the repo root** — so it finally reaches `crates/transync-cli/web/sync.js` and `benchmark/scroll-frame/profile.mjs`, the 22% of the corpus the old `cd web` scope structurally could not see. An empty file list, a declared-but-uninstalled tool, and a format config with no formatter are each an explicit failure rather than a quiet skip. 457 lines were reformatted across 10 files; the two byte-identical `sync.js` copies moved together, as `sync_js_drift.rs` requires. The lint half is **not** adopted, on measurement: 62 diagnostics across every historical version of the corpus, 61 of them a single style preference whose every fix edits a defensive guard in the sync engine.
- **Generated inputs now exercise the hand-rolled HTML tokenizer, walk and balancer** (OI-0046). `crates/transync-html/tests/generative_properties.rs` is a plain `#[test]` — a nine-line `splitmix64` generator over four pinned seeds, `std` only, no new dependency and no `cargo-fuzz` target, because the pre-commit hook is the only automatic gate here and a coverage-guided harness would run at no venue. It is deterministic by construction, so a failure names its exact input and can never be a flake. **It found a live defect on its first execution** (see Fixed). The reason 41 byte-exact goldens and 103 unit tests had not: measured, **not one** of the 38 pinned edge cases produces a single orphan close tag, so `balance_fragment`'s orphan-deletion pass had no corpus coverage at all — six entries carry a literal `<`, none carries one *alongside* an orphan closer, and the combination is what breaks. Also in this issue: the rustdoc **completeness** check moved into the shared library the hook already sources (its deferral trigger had already fired — the Developer Guide's documented gate command had been omitting `transync-lang` since 2026-09-02), and `web/playwright.config.js`'s hard-coded port gained an env fallback so two concurrent browser runs stop colliding.
- **`transync_html::splice`'s two passes must agree in release too** (`8d4efda`, R0009-0066, DCR-0040 §1). The phase-A/phase-B text-node agreement was a `debug_assert`, so the *release* failure mode of a divergence was silent rather than loud: `actions.get(idx)` answers `None` past the end, so a short phase B dropped the tail of a translation and a long one shifted every later replacement onto the wrong node — both returning `Ok`. It is a real `Err` now, riding the same splice-failure degrade path the segment-count mismatch already used, so `validate` falls the block back to source and `regen` splices the source bytes. In the same commit: `is_void` / `is_raw_text` fold ASCII case, because HTML tag names are case-insensitive and both helpers are `pub` in a published crate — every in-crate caller already hands over a lowercased name, so the fold only ever decides the answer for a caller outside the crate; `pipeline::merge` materializes source payloads only for the block ids the `SplitPlan` names; disk-cache compaction became byte-reproducible (R0009-0083 — the unit entries always rode their replay sequence, but the two document-scoped maps are `HashMap`s with no `seq` to ride, so one logical cache could compact two ways); and the Anthropic schema profiler stops treating `dependentSchemas` as a name-to-schema map (R0009-0086, latent — no shared schema in `llm::prompt` emits the keyword yet).
- **`transync serve`'s response half gets a deadline, and five parsers stop accepting what they cannot mean** (`69701cd`, DCR-0040 §2). Only the request head was bounded, so a peer that finished its head correctly and then stopped reading parked every response write on the socket's send buffer forever — and `MAX_IN_FLIGHT` makes those parked tasks a finite shared resource, so 128 such peers made the server unavailable **with no malformed byte anywhere in the exchange** (R0009-0001). `serve` now splits into a head phase and a `respond` phase, the second under a total `RESPONSE_TIMEOUT` of **60 s** — total rather than write-idle, because an idle deadline is defeated by a peer that reads one byte per interval — and the 408/431 refusals move inside it, since they are responses like any other. The five tightenings: `head_end` runs both the CRLF-CRLF and LF-LF searches and takes the earlier index, instead of falling back to LF-LF only when no CRLF-CRLF exists anywhere (R0009-0006); the version check matches the two tokens this server implements rather than any `HTTP/` prefix (R0009-0004); `HostPolicy::verdict` refuses a header line with no colon instead of skipping it, matching the obs-fold arm two lines above (R0009-0005); DNS-name validation enforces the edge-hyphen rule and the 63/253-byte limits (R0009-0007); and `sync.js`'s `validateRows` refuses a **non-string** `target_block_id` outright, under forward-minor drift exactly as in an in-band map, because §3 lets a newer minor add enumerated values and never re-type a field (R0009-0022). Also: `warnOffsetParentDrift` reads `getComputedStyle(pane).position` — the precondition it documents — rather than `blocks[0].offsetParent` (R0009-0024), `mountSync` reads `rowIds.size` instead of re-deriving the set (R0009-0026), and `reject_unknown_ids` keeps a sorted eight-entry sample plus a total instead of joining every alien key (R0009-0032). Both `sync.js` copies moved together, as `sync_js_drift.rs` requires.
- **Two measurement gates that had no venue got one** — recorded because a release accounts for every commit in its range, not only the ones that move a shipped surface. `manual_source_refs.rs` resolves all **255** `resource:` paths in the manual's frontmatter (`7355008`, ti `7e2fd0` option (c)): a path is a provenance claim, so a dangling one means the page's warrant moved and nobody followed it, and both instances found so far were found **by hand** — `crates/transync-syntax/src/htmlseg.rs`, dangling since DCR-0032 lifted the module into its own crate, and an ADR the archive prune relocated. It deliberately reads no prose, so it creates no coupling between the docs bundle and the manual bundle; that coupling is still an open decision. And `benchmark/scroll-frame/` profiles active-block selection (`9fd8390`, ti `dd21ad` / OI-0016), whose Required Actions had opened with "if profiling shows jank" and had therefore sat open for eight weeks on a prerequisite rather than on a decision. **Result: no overrun** — linear in block count, and at 5,000 blocks (past anything in this repository, its own CHANGELOG included) the worst case leaves 85% of a 16.67 ms frame unused. So the sorted-offset cache stays **unbuilt**, and the measurement is what says so.
- **The browser suite refuses a stale bundle, and one function decides where a scratch workdir goes** (`a30b40d`, ti `ed2e73` / ti `13a73b`). `scripts/test-browser.sh` regenerates everything and ends in a bare `pnpm exec playwright test`; running that directly — one `pnpm test` away — skipped the regeneration and validated whatever the last full run left behind, and the hazard was live rather than hypothetical: measured, the fixture on disk was **54 hours** behind `crates/`. `playwright.config.js` now refuses a bundle older than `crates/`, `web/js`, `web/vendor`, `web/wasm` or the two shell HTML files, mirroring `reuseExistingServer`'s refuse-by-default/env-opt-in shape; `TRANSYNC_ALLOW_STALE_FIXTURE=1` downgrades it to a warning. `web/tests` is deliberately **not** an input — editing a spec is what the inner loop is for. Two limits are stated at the check rather than left to be discovered: the input list is a second hand-maintained statement of "what feeds the bundle", and mtime fails silently in one direction (a `git checkout` of an older branch leaves sources older than the fixture) — it is a floor, not a fence, and it catches what a bundle-content hash cannot, a Rust edit that was never built. Separately, `scripts/lib/workdir.sh` now holds one `transync_pick_workdir`: three of the five scripts tested the *preferred* path's parent and relocated on that answer alone, so an explicit `TRANSYNC_FIXTURE_WORKDIR` / `TRANSYNC_SMOKE_WORKDIR` / `TRANSYNC_WASM_STAGING` pointing at a path whose parent did not exist yet was silently discarded. An explicit override now wins verbatim, else the preferred path when its parent exists, else the script **refuses** and names the missing root and the variable that fixes it. Refusing rather than relocating is a judgement, so it is one constant: `TRANSYNC_WORKDIR_POLICY=warn` restores the old behaviour.
- **`crates/transync-cli/src/output.rs` split into concern modules** (OI-0043): 4,005 lines become 411, beside five children — `output/fileset.rs` (staged commit and atomicity), `output/publish.rs` (`--out-dir`), `output/preflight.rs` (bundle-directory shape), `output/bundle.rs` (rendering and templates), `output/destination.rs` (vetting and path normalization) — joining the pre-existing `output/lock.rs`. **A pure move, and proven as one rather than asserted:** an independent review wrote its own Rust lexer and found 119 of 119 item bodies and all 49 test bodies byte-exact, with the code string-literal multiset identical at 746/746. Every order-sensitive body is unchanged, so the staging → fsync → rename → rollback sequencing that carries `--out-dir`'s all-or-nothing guarantee (DCR-0006 / DCR-0011 / DCR-0021 / ADR-0024) cannot have moved. Nothing became `pub`; every `crate::output::X` path still resolves through the parent's re-exports. Internal only — no public surface moves.
- **Two worse-than-linear scans in the degraded regeneration cascade are linear** (OI-0042), and the third one nobody had registered. `validate/full_reparse.rs`'s `attribute_offenders`, `pipeline/finalize.rs`'s `widen_to_neighbors`, and `validate/full_rescan_html.rs`'s twin — written eight days after the issue was filed with the identical shape — all move together, because splitting twins across two register entries is how one of them rots. `widen_to_neighbors` also stopped comparing `String` where `usize` was available, which is why it ran 8× its sibling: measured 26 ms / 245 ms / 1.9 s at 5,000 / 20,000 / 50,000 blocks. N is the caller's document, not this repository's, which is what took it off the cold-path-therefore-negligible reading.
- **The inline-protection layer stops appending the whole reference-definition pool to bracket-free payloads** (OI-0040). `inline_inventory` appended `Document.ref_defs` to *both* sides of *every* unit; a `[`-prefilter now skips it when neither payload can carry a reference. Measured cost of the append: +1,756 ms on a 1,289 ms baseline at 5,000 units against a 20 KB pool. The prefilter recovers 51.8% of that at this corpus's bracket density and **0%** at link-reference house style, so the residual is real: the shared-AST seam is deferred, and the deferral ships its own tripwire rather than a promise — `build_batches` raises one run-level warning when `unit_count × |ref_defs|` passes 6,000,000 bytes, naming the knob. A deferral that reports itself when it starts to matter.
- **`TranslatorError` gains `NoProviderAvailable(String)`**, stable code `provider_unavailable`, exiting **6** (ti `30a744`). Additive on a `#[non_exhaustive]` enum, so a consumer's `match` is unaffected. It is its own variant rather than a message inside `Unsupported` because that variant's exit code (5) deliberately means "the CLI cannot tell a configuration fault from a document one", and an offline run that missed is unambiguously the caller's configuration with the caller's remediation. The `stable_code()` vocabulary is append-only, so this is a new code and not a reinterpretation of `provider_unsupported`.
- **The HTML→HTML feature's closure paperwork** (ti `490d97` wave 7, DCR-0039): the browser-suite weld pins four spec files (`SPEC_FILES`, the mention list and the panic message's advice moving in one commit with all three documents that must name them — ticket `729ec8`'s rule); `scenario-matrix.md` gains SCN-16's row, the `title` and HTML-document block-kind rows, and one out-of-scope bullet recording what ADR-0025 leaves out; the **serve fidelity door** is documented (`transync serve` over an HTML run's out-dir is how you see the real page — the panes are a sync surface, never a fidelity preview); and `README.md` / `docs/Quick_Start.md` stop describing a Markdown-only library. **All eight waves have landed — the feature is complete.** Cutting v0.5.0 was a separate owner decision via `docs/project/release-checklist.md`, taken on 2026-09-05; this release is it.
- **The HTML mechanics moved to their own workspace member, `transync-html`** (ti `490d97` wave 0, DCR-0032). `transync-syntax::htmlseg` is gone with no re-export alias; `transync-syntax` and `transync-core` depend on the new crate, and `transync-syntax` no longer depends on `lol_html` or `htmlize`. Nothing on the `transync` facade's surface moves — the mechanics were always tier (c) engine internals. That wave took the publication roster to seven members; `transync-lang` later made it eight — see the `transync-lang` entry below.
- `transync_html::splice` takes a `BlankLinePolicy` instead of a CommonMark block-type `u8`; `BlankLinePolicy::from_commonmark_html_block_type` is the one surviving home of the `6 | 7` rule.
- Every html dispatch site is re-keyed onto the axis it meant — `Spelling::Html` for IR questions, `InputMode::HtmlSegments` for unit questions, `constraints.html` for the layer-3 splice. Behaviour-preserving today, by three-way set identity; the point is what it will mean once an HTML document's `<p>` is a `Paragraph` that ships a segment array.
- The GFM row-window table splitter now excludes html-segments units **explicitly**, with a test. `inspect_table` would have refused one anyway; an accident is not a guard.
- Workspace version bumped `0.4.0` → `0.5.0`. It was opened as `0.5.0-dev` by `ff788f7` (2026-08-24, ti `490d97` wave 2) and carried to `0.5.0` by this release; the five internal `[workspace.dependencies]` requirements move in the same edit, because they do not inherit `[workspace.package].version`.
- The HTML-document refusal message now points at both escapes — `--input-format html` to translate it as the HTML document it is, or `--allow-html-input` to translate it as Markdown anyway — and no longer calls HTML→HTML translation unimplemented (ti `490d97` wave 6). `--allow-html-input`'s documentation narrows to the island case in the same commit; its behaviour is bit-identical. `--out-dir` now allow-lists `out.html`, and its marker-less complete-set recovery is a predicate rather than a name count, because a run writes one translated document and never both.

### Added

- **The SCN-16 browser gate — `web/tests/scn16.spec.js`** (ti `490d97` wave 7, DCR-0039). The HTML-run bundle that `scripts/test-browser.sh` publishes is now driven through the **shipped shell** — fetch, DOMPurify fail-closed mount, `sync.js` — the way an operator meets an HTML run's `--html-out`, and it holds the feature's acceptance line: bidirectional sync by block id over HTML-derived anchors, the `<title>` translated and aligned but **absent from both panes** (present exactly once, on the browser tab, per the bundle-title chain), and a console clean of the engine's warnings. Every negative rides a positive: each pane's anchor list must equal the map's anchor-role rows *in order*, so a leaked title, a rendered thematic break, a dropped block or an ungrouped list item all surface as list drift instead of passing vacuously. Falsified before it shipped, against mutated copies of the real bundle — a leaked title anchor, a dissolved `li` group, an anchor-role title row, and the pre-ruling self-injected custom-element anchor each drive their named assertion red. That last one is the first demonstration **through the real sanitizer** of the hazard wave 6's wrapper ruling closed: every earlier browser proof deliberately bypassed DOMPurify.
- **`transync translate --offline`** — run with no provider credentials, serving every unit from the cache (ti `30a744`, OI-0038, DCR-0046). A fully warm run completes with no key at all; one that misses stops **at the miss** with `no provider available for this run` and exit 6, because the configuration is what to change. Requires `--cache-dir`, refused at argument time: without a cache that outlives the process the first unit misses by construction, so the flag would have exactly one reachable outcome. This makes DCR-0028's central promise — a document is paid for once — reachable in the case that motivated it, re-rendering on a machine with no key or no network. Ordinary runs are untouched: the provider is still built up front and a missing key is still a startup error, because deferring that unconditionally would move every credential diagnostic later to serve an occasional run.
- **`TransyncOpenAI::offline(model, base_url)`** — a credential-free instance for the above. Every other field resolves exactly as `try_new` resolves it, so `fingerprint()` returns the **same bytes** as a credentialed instance built from the same configuration. That equality is load-bearing rather than incidental: `CacheKey` carries `provider_fingerprint` as a namespace axis, so an offline run that fingerprinted differently would miss every entry it was pointed at and present as a corrupt cache rather than a misconfigured run. Pinned by `an_offline_instance_fingerprints_identically_to_a_credentialed_one`.
- **A ninth workspace member, `transync-lang`** — the source-language *gate*: should a translation run start at all? A consumer that captures text (an agent's final answer) can skip the provider call when it is already in the target language, without running the pipeline to find out (ti `e4f4b0`, ADR-0028 / DCR-0045). Three public items: `Language`, `Gate`, and a two-variant `Verdict { AlreadyTarget(Basis), Translate(Reason) }`. The verdict shape is the safety mechanism — "I could not tell" is `Reason::Undecided` *inside* `Translate`, so the only way to skip is to name `AlreadyTarget` explicitly, and the expensive mistake (skipping a translation that was needed) cannot be reached by a careless match arm. It depends on **no workspace member** and carries no serialization, which is what keeps it from becoming the alignment map's provider-authored `detected_source_language` — two answers to one question is the defect class the 2026-09-01 fixes above were each an instance of. Backed by `whichlang`, chosen by measurement over `lingua` and `whatlang` in `benchmark/lang-detect/`, whose second round reversed its own first recommendation; a dependency-free codepoint test ties the winner on real fixtures, so the script test runs first and the library only sees what counting could not settle. **The publication roster is eight members.**
- `transync_html::element_extents` / `ElementExtent` — the balancer's stack walk lifted out from under it, for the HTML intake to come.
- `TagToken::Open` carries the byte span the scanner already computed; `TagToken::Skip { span }` names comments, CDATA sections and bogus comments, including the `<!doctype …>` region that previously produced no token at all.
- `transync_html::strip_reserved_sync_attrs` — removes the six reserved sync-attribute names from element open tags (OI-0035 route (c), render half; no call site yet).
- HTML document intake (`transync-syntax::intake::html`, ti `490d97` wave 3, spec §4): five-class element classification with default STOP, rule T anonymous text runs, whole-element byte ranges, emission-order ids, heading section scopes, `<title>` blocks, force-close warnings, and debug-asserted source-order/non-overlap invariants. `regen::regenerate(parse, &empty)` is byte-identical over the SCN-16 fixture and a real-page corpus. No LLM run, no pipeline change, no core change — the layer-6 HTML twin (wave 4) remains the gate before any HTML translation run. (DCR-0035)
- The HTML layer-6 twin (`transync-core::validate::full_rescan_html`, ti `490d97` wave 4, spec §7, DCR-0036): the post-regeneration gate for HTML documents — document-wide ordered tag ledger, fresh segmentation re-run through the intake (per-`<li>`, D9), gap byte-identity including preamble and tail (closing the ledger's measured `<!DOCTYPE>`/comment blind spots), and boundary sanity. `finalize` dispatches on `Document.format`; `ReparseFailure` is unchanged, so the three-stage fallback cascade is untouched. No HTML translation run is reachable yet — the entry point is wave 5, the CLI flag wave 6.
- **HTML→HTML translation works at the library surface** (ti `490d97` wave 5, spec §6/§12, DCR-0037): `TranslateOptions.input_format: SourceFormat` (additive; `transync::SourceFormat` exported with its §0 row) routes a declared-HTML source through the HTML intake, the same pipeline, and the scanner-side layer-6 gate; `translate()` returns translated HTML with a real alignment map. `[system].prompt_html` is a new additive profile key (shipped in the default profile; a profile without it falls back to `[system].prompt` with an advisory, never a synthesized merge); the user-message instruction gains a run-level HTML-document clause; Html-spelled heading context and the `<title>`-preferred document title project through `transync_html::extract`. No new cache axis — `profile_prompt_hash` and `instruction_hash` both move for an HTML run (contracts §5a records the verdict) — and no `VALIDATION_SCHEMA_VERSION` bump: the new checks are validator tightening, the bump rules' explicit "Do NOT bump" case. Markdown runs are byte-identical throughout: prompts, instructions, goldens, corpus. The `html_dominance_warning` tail now names the entry point instead of calling the feature absent, and declared-HTML runs never evaluate it. Panes for HTML runs are empty until wave 6 (the pane derivation, the CLI flag, `out.html` and alignment schema 1.3.0 are wave 6's).
- **The HTML→HTML operator surface** (ti `490d97` wave 6, spec §8/§9, DCR-0038): `transync translate --input-format html --input page.html --out-dir out/` writes `out.html` — the full page, head, scripts and doctype verbatim, **anchor-free** — plus a bundle whose panes mount and sync by block id. Routing is flag-only: the preamble sniff stays a refusal and is re-tripped even by an explicit `--input-format markdown`, there is no reverse sniff on the html arm, and `--input-format html` together with `--allow-html-input` is an argument error (exit 1). `--allow-html-input` is unchanged in behaviour and narrowed in documentation to its real case: Markdown that genuinely opens with an HTML island. An HTML run's two annotated panes are now the synthesized `<main>` fragments — strip-then-inject into each block's own element, transparent `<div>` for `html`-kind and element-less blocks, `li` blocks grouped, and head/gap/script bytes never present.
- **Alignment schema 1.3.0** (ti `490d97` wave 6, DCR-0038): three additive changes — per-row `source_format` (the block's spelling; a Markdown run's html-island row says `"html"`), map-level `input_format` (the run's intake), and the `"title"` `block_kind` value. Forward-minor by design: no new `sync_role` value, no new required row fact, both additions outside the facts the minimum-usable-row gate polices, and `"title"` rides the `non-sync` role shipped since 1.0 — so a 1.2.0-era engine reads a 1.3.0 map and still drives, which the browser suite proves rather than asserts. The sync engine's logic is untouched; the whole `sync.js` diff is the schema mirror.

### Fixed

- **The browser suite is green again, and the priority-1 anchor capture it was thought to prove never happened** (ti `8cd7ba`, DCR-0052). `web/tests/engine.spec.js` test `l` went red, deterministically, at commit `2ca092f`, and was read as OI-0035's impostor surface reached from the opposite end: with a duplicate `e-0001` planted 750 px down the follower pane, the follower landed at **746**, reported as the partner lookup resolving a listed id to its SECOND occurrence once a reflow recompute had run. It is not that. Measured three ways against the shipped bundle rather than argued: removing the planted duplicate **entirely** still lands the follower at 746; serving `51f0d93`'s pre-clamp `sync.js` under the *unchanged* test lands it at 0; and a probe that appends a duplicate after mount, forces a recompute and drives onto its band pairs with the FIRST copy. The mechanism is OI-0047's new bottom clamp, added in that same commit. The reader was parked 4 px past the last anchor the map claims, so no claimed anchor straddled the reference line and none sat below it; the clamp answered `e-0005` at progress 1; and the follower went to the end of *its* `e-0005` — `600 + 150 - REFERENCE_OFFSET_PX`. That is 746 because the fixture had planted the duplicate at exactly that anchor's bottom, so the two candidate explanations differ by nothing at all in the observed number. **`collectAnchors` is, and remained, the single home of the first-occurrence policy**, gating both structures it fills at mount and on every reflow recompute alike; no engine behaviour changed here, and the clamp is kept, because freezing the follower past the last anchor is the defect OI-0047 was filed for. What was wrong was the assertion: `toBeLessThan(50)` asserted the follower had not moved AT ALL, which is precisely the dead zone OI-0047 removed, so "the late anchor is inert" and "the follower stayed home" were one observation and the test could not tell a legitimate clamp from a capture. The expectation is now written out of the pane's own geometry — the clamp's destination, and separately that it is above the late anchor's copy — and test `l` gains two legs the suite never had: a duplicate arriving *after* mount still pairs with its first occurrence, and the scan array still drops it, which only the clamp can observe. All three legs are falsification-tested against a doctored engine: policy applied at mount only → 700, scan array keeps every duplicate → 146, row gate skipped on reflow → 904, against 746 green. **Ruled out:** biome — the earlier exoneration swapped `web/js/sync.js`, but `engine.spec.js` imports the CLI-embedded twin through the generated bundle, so the engine under test never varied — and any fault pre-existing at `51f0d93`. `activeBlockWithProgress` gains the note that says why an unlisted anchor below the last claimed one buys nothing an ordinary unanchored paragraph of the same height does not; both `sync.js` copies carry it, byte-identical.
- **`transync-lang` stops counting a script block's digits and punctuation as target-script letters** (ti `15acc0`, Review 0010 R0010-0066, adversarially verified). `script::read` tested block membership *before* alphabeticity, and a Unicode block is not a letter set: the Arabic ranges carry the Arabic-Indic digits U+0660-0669 and U+06F0-06F9 plus the Arabic comma, semicolon and question mark; Devanagari carries its digits and both dandas; Kana carries the middle dot. Every one of them counted as a letter of the target script. Measured, `Gate::new(Language::Arabic).verdict("Order ١٢٣٤٥٦ shipped")` gave in_script 6 against other 12 and cleared the one-in-five floor, returning `AlreadyTarget(ExclusiveScript)` — an **English** sentence declared already Arabic. That is the one direction this crate defines as unsafe: naming `AlreadyTarget` is the only way a run is skipped, so an over-count here does not degrade a translation, it cancels one. It also contradicted the module's own vocabulary, since `Reading::NoLetters` is documented as "punctuation, digits, code, or nothing" and `read`'s contract is to count the script "against every other letter" — both true only for targets whose blocks happen to be pure `Lo`. Alphabeticity is now asked first, of both sides. `is_alphabetic` is Unicode `Alphabetic` rather than `L*`, so the modifier letters and combining marks these scripts genuinely need keep counting — Kana's prolonged sound mark, Arabic tatweel and the harakat — and the fix does not buy correctness on digits by dropping those. **Hangul is unaffected and provably so**: all three of its ranges are `Lo`, which is why the measured Korean corpus (`benchmark/lang-detect/RESULTS.md`) never showed the defect and why the backend benchmark does not move. Staged red observed before the fix — `left: Dominant, right: Absent`, the defect itself.
- **Four more tokenizer divergences close, and PLAINTEXT gets the arm the terminate-or-delete rule never had** (ti `bebebe`, DCR-0050). Each of the four ended the same way — the sync wrapper's own `</div>` swallowed, every following anchor mounted in the wrong parent, reachable from untrusted source Markdown through one CommonMark type-6 html block. `RAW_TEXT_ELEMENTS` goes from four names to **eight**: `xmp`, `iframe`, `noembed` and `noframes` join it, because HTML reaches the generic raw-text algorithm for all of them, so `<div><iframe></div></iframe>` stops losing its author's `</iframe>` to the orphan pass. `comment_end` becomes the one home of "where does a comment end" and knows HTML's second closing form **`--!>`**, so `<!-- a --!><div>y<!-- b -->` stops hiding a live `<div>` inside one comment. `tag_attr_value` decodes character references **before** `child_content_mode` compares them, so `<annotation-xml encoding="text&#47;html">` is the HTML integration point a browser makes it — the decode lives there and not in `walk_attrs`, which reports the byte spans the strip cuts with. And `<plaintext>` in HTML content becomes a `SkipKind::PlainText` region covering the start tag and every byte after it, **deleted**: HTML's PLAINTEXT state has no exit, so terminating it is not available, and DCR-0050 records why keeping the tag or keeping the bytes after it is each a break rather than a repair. `<svg><plaintext>` is an ordinary foreign element — `plaintext` is not a breakout tag. All four verified against Chromium rather than against this crate's own answers, and all four go from a §4a break to an empty divergence signature. **The census did not move, and that is the honest result:** `sec4a:break-nested` was 1,490/10,000 before and the four fixes moved it by exactly zero — proven byte-level, since a corpus emitted by the *fixed* crate over the old atom alphabet is `cmp`-identical to the one at `14d6eda`. The old `ATOMS` contained no `plaintext`, no `xmp`/`iframe`/`noembed`/`noframes`, no `--!>` and no entity-spelled attribute value, so ten thousand browser-checked cases had nothing to say about any of the four. Nine atoms and four required coverage states were added and the pin re-blessed at 7,697/10,000 with `sec4a:break-nested` at 1,358 — a **different population, not progress**, and the pin says so; the census is comparable only within a fixed alphabet, which also corrects how the number was described when the oracle landed. No `KNOWN_DIVERGENT` entry retired.
- **`balance_fragment` no longer welds a literal `<` onto its neighbour when it deletes an orphan close tag** (ti `fdd989`, found by OI-0046's new generated-input properties on their first execution). The orphan pass concatenated the bytes on either side of each deleted span, which makes the byte before the span the immediate predecessor of the byte after it. In HTML's DATA state exactly two bytes have a meaning that depends on the byte *after* them — `<` (tag open) and `&` (character reference) — and a `<` sitting immediately before an orphan close tag is necessarily a **literal** one, because `<<` leaves the first a character and every other spelling would have consumed the orphan's own `>`. After the splice that literal could be followed by `!`, `?`, `/` or a letter, so bytes that were **text in the source became a token in the pane** — and a token `walk_elements` never saw, so the balancer appended no closer for it. `<</b>x` became `<x`, an element a browser names `x</div`, which eats the sync wrapper's own `</div>`; the same weld could mint an unterminated comment, an end tag, or — the reachable harm — a live `div` carrying a `data-sync-id` out of bytes `strip_reserved_sync_attrs` had correctly passed as text, which is an anchor-injection route from untrusted source Markdown (invariants 1 and 7, `contracts.md` §4a). The fix is the precedent ported one function over rather than reinvented: the pass now coalesces maximal runs of adjacent orphan spans and substitutes a single U+0020 wherever deletion would weld, exactly as `strip_reserved_sync_attrs` has since ti `490d97` wave 1 — so the crate holds **one** rule about welding instead of two opinions. Coalescing is load-bearing, not tidiness: judging each cut alone reads a byte the pass has already deleted, so `<</b></i>x` would call the seam safe and weld anyway. The deletion bookkeeping also became a **net** figure, because ti `c1f9a8`'s trailing-region repair maps offsets through it and a gross count would over-truncate once per inserted separator. Verified over 1.32M generated inputs and 9,214 Chromium cases with zero mints, and **no golden moved** — checkable rather than lucky, since no pinned edge case or document fixture contains `<` or `&` immediately before an orphan close tag.
- **`TransyncError::InvalidOptions` — a caller-input fault stops reporting itself as an engine bug** (OI-0048 / R0009-0053). An empty or whitespace-only `TranslateOptions.target_language` raised `TransyncError::Internal`, whose stable code `internal` tells a consumer "transync has a bug" about a value the consumer passed in. It is now its own variant with the new stable code **`invalid_options`**, taking the vocabulary from twenty-one codes to twenty-two (8 engine-side + 14 provider-side). Additive on a `#[non_exhaustive]` enum, so no consumer `match` breaks; §1's Rules paragraph independently forbade the cheaper route ("a new variant requires a **new** stable code rather than reuse of an existing one"). The CLI cannot produce it — clap validates `--target-language` — so it stays on `for_pipeline_failure`'s catch-all arm and no exit code moves. Note for consumers: this does **not** obsolete your own validation. Both in-tree roster consumers guard the field for reasons of their own that transync does not cover (a cache-identity delimiter constraint in one, an actionable profile-validation warning in the other); what changes is that a value slipping past such a guard now names the right cause.
- **The scroll-sync follower no longer freezes past the last anchor** (OI-0047; the clamp's contract is DCR-0052). `activeBlockWithProgress` had a clamp above the first anchor and none below the last, so once the reference line passed the final anchor's bottom edge both branches missed, it returned `null`, and the follower stopped. It now tracks the last-ending anchor during the existing walk and returns it at `progress: 1`. In the same change the two drift diagnostics — `warnMapDomDrift` and `warnOffsetParentDrift` — re-run on every coalesced reflow recompute instead of only at mount, **latched** so a warning fires once per verdict change rather than per reflow: the old mount-only budget bought its cheapness by going silent exactly when geometry changes. `contracts.md` §4a's "one property read per pane at mount, never per frame" widens accordingly, and that widening is the contract.
- **A scroll the engine did not cause cancels the follow instead of being lerped away** (ti `7936c2`). The smooth loop could drag a pane back to a destination the reader had already left and — because it re-arms `lockUntil` on every moving frame — swallow the scroll that said so. `releaseDriverLock` handles a gesture takeover, but it listens for `wheel`/`touchstart`/`pointerdown`/`keydown` and a scroll can arrive with none of them: `scrollIntoView`, find-in-page, the browser's scroll restoration, an embedder setting `scrollTop`, the test harness. Measured before the fix, sampling every 80 ms after parking the target at 434, the pane was dragged back through 362 → 233 while the partner never followed. **Two rules, adopted together because each alone turns a browser leg red:** each moving frame records the `scrollTop` it reads back plus the `scrollHeight` that went with it, and a pane counts as externally scrolled when it sits more than `EXTERNAL_SCROLL_TOLERANCE_PX` from that value **and** the `scrollHeight` is unchanged — then the follow and the lock are dropped and the scroll drives the partner normally. The height condition is not defensive garnish: it is the difference between a scroll and a **content reflow**, since a `<details>` mirror grows the follower under a fixed `scrollTop`, which the engine already handles through the disclosure mirror and the `ResizeObserver` recompute. And the baseline outlives its loop on purpose, because a settled loop has no baseline and the scroll then lands inside the 90 ms post-settle lock window and is absorbed. Detection lives at both ends: `handleScroll` is where the signal arrives and acting there is what lets the same scroll drive the partner, and the loop keeps the check as a backstop so it cannot lerp a pane it no longer owns. This fixed `scn13.spec.js` leg k, deterministically red since before waves 3–5. Both `sync.js` copies moved in one commit.
- **`install-hooks.sh` no longer deletes a developer's own untracked `pre-commit`** (R0009-0012). It removed `.git/hooks/<name>` for every tracked hook name it found — keyed on basename and existence, with no content check, no marker and no backup. `.git/hooks/pre-commit` is exactly where a developer's own hook lives; it is untracked and unrecoverable, and sharing a name with a tracked hook says nothing about who wrote it. Removal is now conditional on `cmp -s` against the tracked hook: anything that differs is **preserved**, named on stderr together with the `rm` that would remove it, and left alone — it is inert either way, since `core.hooksPath` decides which hooks run, so deleting it bought a tidier directory at the price of someone else's work. In the same pass (`88964df`): `smoke-live.sh`'s `TRANSYNC_LIVE_BIND=localhost` branch printed a friendly URL and then died in clap, which parses an `IpAddr` — the dead branch is gone, the value is validated as an IP literal *before* the paid translation run, and `::1` replaces `localhost` with the URL brackets it needs (R0009-0010); `playwright.config.js` stops reusing whatever already answered `127.0.0.1:4319` whenever `CI` was unset, because answering a port is not proof of ownership (`TRANSYNC_REUSE_SERVER=1` opts back in, R0009-0017); the browser fixture directory stopped defaulting to one workstation's absolute path and is repository-relative in the config **and** in `tests/support/harness.js`, which carried the same literal — they have to resolve the same directory or the suite reads one bundle while the server serves another, and a missing bundle now fails immediately naming the command that builds one (R0009-0019); and `test-long.sh` stopped inviting a real key into a tracked file, taking its sibling `test.sh`'s wording verbatim (R0009-0016). Five stale documentation claims moved with them (R0009-0087 through R0009-0091): `TranslationOutput`'s docs counting four fields where the type has seven and is `#[non_exhaustive]`, `translate_with_cache` still describing cross-process disk caches as waiting on "a persistence-capable impl" after `DiskCache` shipped, the README filing WASM rendering under post-MVP while `crates/transync-wasm` and `web/demo-wasm.html` ship, `status.md` carrying two bullets both labelled "Workspace version" and neither of them the current one, and one obsolete sentence inside active checklist step 25 claiming zero tags survive. The eleventh finding, R0009-0014, was deliberately **not** closed here — the hook's JS leg still validated nothing, and what changed was that the skip could no longer be read as a pass; closing it properly is the OI-0039 entry above.
- **`transync serve` no longer under-delivers a body or advertises an authority it cannot answer on** (OI-0045). `send_file`'s streaming branch announced `Content-Length` from `resolve`'s metadata and then discarded the byte count `tokio::io::copy` returns, so a file that shrank between the two sent a short body and the server closed the connection as if the response had been delivered; it now compares the two and fails with `UnexpectedEof` naming both numbers, dropping the socket — the only correct HTTP/1.1 answer once the header is on the wire. And `HostPolicy` now advertises only the loopback authorities of the bound socket's own family: a `0.0.0.0` bind is IPv4-only and stops naming `[::1]`, which had been sending operators who followed the startup output at a connection failure. A dual-stack `::` bind still answers for all three.
- **A provider can no longer erase a block's visible text and have it ship as `translated`** (ti `c887bc`, DCR-0048). Every per-unit validation layer compared **structure** — heading level, `(cols, rows, alignments)`, fence info string, node-kind fingerprints, segment count, kind labels, destinations, tag ledgers, gap bytes — and none asked whether the words survived, so a payload that kept each kind's structural fact and none of its text was accepted with `fallback_status: translated` and no warning: `"##"` for a heading, blank cells for a table, a bare `rust` fence for a code block, `"- \u{200B}"` for a list item, `"&nbsp;"` or `"\u{2800}"` or `" \u{FEFF}"` for a paragraph. **All six Markdown block kinds and both HTML paths.** The one arm that did look at text — `per_kind::check_html`'s whitespace guard — was wrong twice: its predicate was `char::is_whitespace`, exactly the 25 Unicode `White_Space` codepoints, so U+200B / U+200C / U+200D / U+2060 / U+00AD / U+FEFF all passed; and it justified being unconditional with "source segments carry at least one non-whitespace char by construction — the extraction drop step", which is false, because `transync_html::extract`'s drop test is the *same* predicate, so a zero-width-only source segment is kept, sent, and had its faithful echo rejected. `validate::text_presence` now owns the question for both intakes: reject when the **source payload carried visible text and the translation carries none**. The scope is what makes it safe to be aggressive about which codepoints render nothing — a `<td>&#8203;</td>` shim, a ZWJ-only emoji fragment, an empty-bodied fence and a thematic break each have no visible source text either, so the check never fires on them. The two intakes read different things: the segment **strings** on the HTML path, because `splice` writes a translated segment as `ContentType::Text` and escapes `&`, so a provider's `"&nbsp;"` reaches the page as six visible characters; and comrak's **decoded text** on every Markdown path, because nothing re-escapes there and `&nbsp;` / `&#8203;` / `&#xFEFF;` are pure ASCII in the payload bytes. Rejection is retryable like every other per-unit layer and reported under the existing `per_kind_shape` layer, so no wire shape moves. Visible garbage is deliberately out of scope — `**` renders two asterisks, and grading a translation is the model's job (architectural invariant 2).
- **A raw-HTML block that ends inside an unterminated comment, CDATA section, bogus comment or tag no longer takes the next block's anchor with it** (ti `c1f9a8`, DCR-0047). `balance_fragment` now terminates such a region where a browser terminates it, or deletes it where a browser abandons it — a tag cut off at EOF mints no element, and no single terminator even works for one cut inside a quoted value. Only an unterminated *comment* swallows everything after it; the other three kinds end at the **first `>`**, which in a mounted pane is the sync wrapper's own `</div>` — so the wrapper closed inside the region, stayed open, and every following block nested inside this one's wrapper, which is `contracts.md` §4a's direct-child break. Reachable from ordinary source Markdown: a type-6 html block ends at a blank line, so `<div>x<!--` plus a blank line plus a paragraph is one html block and a real anchor. ti `95f55b` had stopped the balancer appending a closer the region would swallow, which bought idempotence and left the fragment unrepaired; that carve-out is now closed and its re-scan guard is kept as an invariant check.
- `scan_tags` tokenizes malformed attribute regions the way a browser does (ti `549b20`; DCR-0032 amendment 2026-08-21): a bare quote in attribute-name position is a name byte instead of opening a phantom quoted value; a stray `=` starts an attribute named `=` instead of opening a value, so the strip neither misses a plant behind one nor deletes text a browser paints; and a tag left unterminated at EOF becomes a `TagToken::Skip` spanning to end of input instead of a silent whole-suffix abort. One stray byte could previously desynchronize the scanner and hide everything after it from `tag_inventory`, `balance_fragment`, `element_extents` and `strip_reserved_sync_attrs` — including a planted `data-sync-id` that DOMPurify keeps as a live anchor.
- `balance_fragment` / `element_extents` read a start tag's self-closing `/` the way a browser does (ti `490d97` wave 1; DCR-0032 amendment 2026-08-23). HTML honours the flag in exactly two places — inside foreign content and on the `<svg>`/`<math>` start tags that enter it — and everywhere else it is a parse error the parser ignores, so a flagged non-void tag now OPENS and the author's matching end tag is its real closer. The walk never pushed such a tag, so that end tag was deleted as an orphan, and on the render path the still-open fragment consumed the sync wrapper's own `</div>` — nesting the next block's anchor inside the html block's wrapper, which contracts.md §4a forbids. Reachable from a plain author-written `<div/>`, with no HTML translation involved. `walk_elements` gained per-stack-entry foreign-content tracking so the two real carve-outs keep the XML reading, and `VOID_ELEMENTS` gained the four measured parser-voids `basefont`, `bgsound`, `frame`, `keygen`.
- `strip_reserved_sync_attrs` no longer welds bytes together at the cut (ti `490d97` wave 1). Removing an attribute took its leading whitespace unconditionally, so `<div data-sync-id="a b="c">x` became `<divc">x` — moving `tag_inventory` from `["div"]` to `["divc"]` — and a reserved attribute sitting behind a stray `/` stripped to `<div/>`, setting a self-closing flag the source never carried. Adjacent cuts are now coalesced into one run and the run is replaced by a single space exactly where its absence would change the parse; a well-formed strip is byte-identical to before.
- **A `data-sync-id` written into source content can no longer pre-claim a real block's anchor** (OI-0035, ti `490d97` wave 1, DCR-0033). Closed at both layers. The renderer strips the six reserved sync-attribute names from raw-HTML blocks on the way into a **pane** — `out.md` keeps the author's bytes — and `mountSync` takes its anchor set from the validated alignment rows, so an element the map does not claim is inert forever, including one inserted after mount. Both `web/js/sync.js` and its byte-identical CLI-embedded twin moved in one commit. Recorded residual: an impostor carrying a *listed* id ahead of the genuine anchor still wins the engine's first-occurrence lookup, which is why the render-side strip is not optional.
- **An `<svg>`-wrapped `<div>` no longer swallows the following block's anchor** (ti `e77173`, DCR-0041). `walk_elements` carried one inherited `in_foreign` bool per stack entry, which models foreign content as a region entered once and left once. HTML's is not one: it has **islands** where the parser is back in HTML content while still inside an `<svg>` (`foreignObject`, `desc`, MathML's text integration points, and `annotation-xml` at exactly two `encoding` values), and **exits** that no end tag marks — a breakout start tag pops open foreign elements at the tag itself. So `<div class="wrap"><svg><div>x</svg></div>` left that inner `div` open in a browser while the walk closed it at `</svg>`, and the next block's anchor mounted inside the wrapper. That is a live `contracts.md` §4a break, reachable from untrusted source Markdown through a type-6 html block with no self-closing slash involved. The walk now carries a three-valued content mode per open element.
- **Raw text and RCDATA are HTML-content states, and a sync attribute planted in an SVG `<title>` no longer survives the strip** (ti `2e2453`, DCR-0042). `scan_tags` entered raw-text/RCDATA for `script`, `style`, `textarea` and `title` unconditionally, inside `<svg>`/`<math>` too. A browser does not — those states are entered by the tree construction stage from the "in body" insertion mode, and foreign content has no such rule — so there all four are ordinary foreign elements whose contents are markup and whose self-closing `/` is honoured. Measured: `<svg><title>a<b>c</b></title>` mints a real `<b>`; `<svg><script/>x` leaves `x` a sibling. The consequence that matters is the strip's: in `<svg><title><div data-sync-id="p-0001">` a browser sees a **live** attribute on a real `div`, while the scanner saw RCDATA text and `strip_reserved_sync_attrs` walked past it, leaving a planted anchor standing in a pane whose whole premise is that ours are the only sync attributes in it. The content-mode stack moved out of `walk_elements` and into `scan_tags`, which needs the same answer to decide whether a raw-text run opens at all, so the two layers read one verdict instead of deriving two. SVG `<title>` is now the HTML integration point the spec makes it.
- **A void name used as a real foreign element keeps its end tag** (ti `48f3c6`, DCR-0043). `is_void` decided whether a start tag opens an element globally, foreign content included. HTML's void list is an HTML-content rule: in foreign content "any other start tag" inserts a foreign element and only the self-closing flag pops it. So `<svg><link>a</link>` is a genuine closable SVG element whose `</link>` was classified an orphan and **deleted** from the pane — the balancer editing what the reader sees. Thirteen void names are reachable that way. The `</br>` hazard that kept the rule global — HTML's end-tag-`br` rule turns an appended one back into a fresh `<br>` — cannot arise: `br` is a breakout tag, so `<svg><br>` leaves foreign content before the tag is processed, as do `embed`, `hr`, `img` and `meta`. That set is pinned by test.
- **A start tag named `image` is renamed to `img`, the way HTML's tree builder does** (ti `e923ef`). HTML's "in body" insertion mode handles an `image` start tag by rewriting the token's name to `img` and reprocessing it, so `<image>x` is an **empty** `img` with `x` as its sibling — the voidness follows the rename, not the name the author typed. The crate performed no rename, so the element opened, `x` read as its content, and `element_extents` answered `image` where a browser says nothing at all. Inert for both shipped consumers today (`tag_inventory` compares source against splice and both spelled it `image`; the appended `</image>` is an end tag a browser matches to nothing) and it would have mis-parented content for the wave-3 intake, which asks this walk what element surrounds a run of bytes. The rename lives in the **tokenizer**, not the walk: teaching the walk that `image` is void would have been a second opinion about voidness — the shape ti `415cdb`, ti `e20490` and ti `2e2453` each were — whereas renaming the token means `is_void`, the content-mode stack and the strip all read the renamed name and `VOID_ELEMENTS` stays a list of names that really are void. It is an **HTML-content rule**, the fourth this crate has drawn that line for: in foreign content "any other start tag" inserts a foreign element and SVG has a real `<image>`, so `<svg><image>a</image>` keeps the author's name and their closer, and a breakout tag returns to HTML content and the rename applies again. Golden movement is exactly the two entries that must move (`image-html`) and the one that must not (`image-foreign`, unmoved).
- **The scanner's tag-name and end-tag-open states follow HTML, closing a second strip bypass** (ti `e20490`). HTML's tag-name state consumes every byte up to whitespace, `/` or `>` into the element name — quotes and `=` included — so `<divq"x=" data-sync-id="v">` is one element named `divq"x="` carrying a **real** `data-sync-id`. The scanner ended the name at `divq` and attribute-walked the remainder, burying that plant in a phantom quoted value the strip never examines as a name, so a live anchor survived; only the pane path's DOMPurify mount dropped it, for a reason a consumer that does not sanitize never inherits. Separately, `</` before a non-letter opens a bogus comment in HTML and mints nothing, where the scanner emitted an `Open` and the balancer then owed it a closer a browser reads as orphan junk. One `tag_name_end` now serves both the scanner and the strip, because fixing the tokenizer alone left the hole open — the strip held its own copy of the boundary rule.
- **`strip_reserved_sync_attrs` stops cutting a non-reserved attribute across a stray `=`** (ti `415cdb`). `strip_reserved_sync_attrs("<div =data-sync-id=\"x\">y")` returned `<div =>y`, where a browser folds those bytes into one junk attribute named `=data-sync-id` — nothing reserved is present, yet the cut ran and a non-reserved attribute moved. The direction was always safe (a 51-case browser equivalence probe found 50/51 equal, that one over-deletion, and **zero** under-deletions), so the impostor-anchor property was never at risk. The cause was two attribute walks where there should be one; DCR-0041 had collapsed them onto the shared `walk_attrs`, and this aligns that walk with HTML's before-attribute-name state.
- **`balance_fragment` is idempotent under its own re-scan** (ti `95f55b`). A fragment can end inside an unterminated comment, CDATA section, bogus comment, raw-text run or tag — an author may close an HTML block mid-`<!--`, and invariant 7 says that input is expected. The closers appended at the end then landed **inside** that region, where they are not markup at all: a re-scan hid them, the elements read unclosed again, and the next balance appended another copy without limit (15,726 violations across 200,000 fuzz iterations). The balancer now asks the scanner rather than classifying the region itself — if the appended bytes come back as close tokens they belong, and if the scanner cannot see them they are dropped. The append stays where it is load-bearing: `</textarea>` ends the raw-text run it would otherwise be buried in.
- **A provider payload meets the same nesting ceiling the source had to satisfy** (ti `148fcf`). Source Markdown reaches comrak through one guarded door (`parser::intake`, which refuses on nesting depth before parsing), but a **translated** payload returned by a provider reached the validation layers without passing it. Untrusted-input handling now applies to both directions of the wire at one door in `validate_unit`, before any layer parses, and `validate::full_reparse` identifies which blocks exceeded the ceiling so a rollback names them rather than rolling back nothing. The originally-feared impact — a stack-overflow abort — was **refuted** by verification: comrak 0.27's block parse is iterative and its walkers are heap-based, and `structure.rs` keeps a deliberate 1000-level tripwire test on a 256 KiB stack that would catch a regression in that dependency. The fix is a consistency and rejection-quality change, not a crash fix.
- **A block's DOM anchor is decided by its own alignment row** (ti `18b9c3` / `9ffb97`, DCR-0044). `render_block` enforced "no anchor" with a literal `BlockKind::ThematicBreak` test and never consulted `align::sync_role_for`, so a block whose row says `sync_role: non-sync` could still render a real `data-sync-id` — the row and the DOM disagreeing about the same block, which every anchor rule in `contracts.md` §4a rests on not happening. Not reachable today (nothing mints the one other non-sync kind, `BlockKind::Title`) and reachable as soon as the HTML intake does. The omission moved into `attrs::write_attrs`, keyed on `row.sync_role`: one value, written by `sync_role_for` and read at emission, cannot disagree with itself. `sync_role_for` also lost its `_ => SyncRole::Anchor` catch-all and now names every `BlockKind` variant, so a new variant is a compile error at the one place that decides its wire role rather than silently anchoring. Thematic-break output is byte-identical.
- `DiskCache` no longer discards its whole contents on every open when `max_bytes` is set below the bytes eviction cannot reclaim (OI-0044 / R0009-0082, ti `0a3fca`). The trim evicts entries; the header and the document-scoped records are exempt by design, so a budget at or below their combined size named a total no set of entries could reach — and the loop freed everything, still read over budget, and repeated it next open. It now logs the arithmetic (requested value, floor, and the floor's two components) and applies `max_entries` alone, so the entries survive and the cache converges. Reachable only by a program embedding the crate with a small budget: the CLI always opens with the 1 GiB default (ti `650bbb`), and this was that lever's named precondition.
- **A failed cache-log write closes the log instead of writing past the damage** (OI-0044 / R0009-0081). `DiskState` held its `BufWriter<File>` unconditionally, so a write that returned `Err` left that writer installed and the next `put` / `evict` appended a whole record onto a half-written one — two lines welded into one that cannot deserialize, costing the *following* entry as well as the failed one. Never a wrong value (a truncated JSON object concatenated with a whole one does not parse, and replay skips it with a warning), and it needs a record at or above the 8 KiB buffer, where `write_all` bypasses the buffer into `File::write_all` and can write part of a record before failing — a sub-capacity record self-heals, because `BufWriter::flush_buf` re-queues the remainder. The writer is now retired on the first failure, its buffered remainder dropped rather than flushed: nothing more reaches the file, so the partial bytes stay the file's **last** bytes and the next open repairs them as an ordinary torn tail, losing exactly the record that failed. Later writes answer a `CacheError` naming the closed log, reads keep being served from the index already in memory, and the run is untouched — a `CacheError` degrades it and can never abort it, which is `DiskCacheOptions`' "an accelerator must not kill a run" posture the R0009-0082 fix followed by clamping rather than refusing. The finding's realest part was that **no test covered a failing writer**; one does now, driving the real `put` path through a `File` over a non-blocking socket, which accepts what fits, answers `WouldBlock` mid-record, and starts accepting again once its peer drains — the only way to show what a second write would have done. Against the unfixed code it failed on "the log is closed, so the second record is refused rather than welded on".
- **Replay's per-line read is bounded, and a line past the bound resyncs instead of being materialized** (OI-0044 / R0009-0080). `scan_log` read each physical line with `read_until` into an uncapped `Vec<u8>`, and the header decision is taken *after* the read — so a foreign file with no newline anywhere was copied into memory whole and only then rejected, contradicting the function's own claim that "peak memory is the index it is building plus one record". Each read is now capped at `MAX_RECORD_LINE_BYTES`, which is the log's own default byte budget rather than a number invented for the occasion: a compacted log at budget holds the header, the document-scoped records and every entry, so one line that alone exceeds it would already be the whole cache. A line that runs past the cap is treated as the unreadable line it has to be — skipped, counted, its bytes still charged to the file so a later truncation cuts at the right offset — and the reader skips to the next newline without buffering, so the record after it still replays. **This is not a security fix and is not presented as one:** ADR-0022 already places a local writer to the cache path outside the threat model, naming R0004-0024 — this same memory concern — by number. What it buys is a doc claim that is true for a foreign file and a bounded read plus a resync for an accidentally corrupt one (a truncated write, a half-copied file), both inside DCR-0028's "degrading to re-translation" envelope. Two tests, both observed red first: the oversize *valid* record replayed as a fourth entry (`left: 3, right: 2`), and the headerless newline-free file read to its end before the header verdict.

## [0.4.0] - 2026-08-20

Released. The full entry is in [`CHANGELOG-archive.md`](CHANGELOG-archive.md).

## [0.3.0] - 2026-08-07

Released. The full entry is in [`CHANGELOG-archive.md`](CHANGELOG-archive.md).

## [0.2.0] - 2026-08-05

Released. The full entry is in [`CHANGELOG-archive.md`](CHANGELOG-archive.md).

## [0.1.0] - 2026-05-03

Released. The full entry is in [`CHANGELOG-archive.md`](CHANGELOG-archive.md).

[Unreleased]: https://github.com/QuietJoon/transync/compare/v0.5.0...HEAD
[0.5.0]: https://github.com/QuietJoon/transync/compare/v0.4.0...v0.5.0
[0.4.0]: https://github.com/QuietJoon/transync/releases/tag/v0.4.0
[0.3.0]: https://github.com/QuietJoon/transync/compare/v0.2.0...v0.3.0
[0.2.0]: https://github.com/QuietJoon/transync/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/QuietJoon/transync/releases/tag/v0.1.0
