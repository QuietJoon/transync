Changelog All 문서 전반에 걸쳐 this 있던 오래된 설명을 정리합니다 in this 콘텐츠 The format is 상태로 on Keep a Changelog and this project 해석하는 to Semantic Versioning Unreleased 검증 sanctioned breaking window is OPEN. It 중첩 깊이 by wave of the feature ti ADR DCR which splits semantic kind from source spelling 수 the 이 IR. 여러 breaking 걸쳐 진행되었으며 각 항목은 the window. Wave contributes four, batched, 통과했고 none of the four is user visible: the corpus regenerates, renders 요소로 aligns byte 직접 쓴 zero fixture edits. The fifth 이상 발생하지 wave unreachable was removed under this window by the review fix pass 함께 Additive changes 권위 here as they come; further breaking changes may 합니다 this 변경은 until it 걸쳐 남아 있던 오래된 설명을 Changed BREAKING narrows to the unit 텍스트 상태로 wave 문제를 DCR 그 variant now means one thing 동일하게 with no semantic instead of 번역된 The CommonMark block type moved to 적용합니다 정렬 where it 앵커 always spelling metadata. A pattern or construction naming 내릴 longer compiles; a 작업은 pattern still does, and is now misleading. gains a variant for an HTML 실패한 and an explicit of a 콘텐츠에서만 is translated and aligned but has no DOM anchor, because the browser 동작은 renders 이상 and the pane 결정은 nothing to anchor. Every 있고 over in 관련 consumer's tree needs 저장소의 권위 있는 사본은 gains Engine tier tier 그곳에 types, reached only by a 전반에 걸쳐 on directly. Per block spelling 스캐너가 required rather than stylistic: one Markdown document interleaves HTML spelled islands with Markdown spelled blocks, 동일하게 no document level bit can carry the 원본과 Changed The HTML mechanics moved to 맵의 행이 workspace member, ti 렌더러와 정렬 is 서로 with no re export alias; and 작업은 on the new crate, and no longer depends on 테스트는 Nothing on the facade's 항목은 moves the 요소 were always tier c engine internals. The publication roster is seven members. 태그가 a 동작은 of 이상 CommonMark block 이러한 결정은 the 변경 surviving home of 계약 rule. Every html dispatch site is re keyed onto the 저장소이며 it 커밋은 for 반영되어야 questions, 이 unit questions, for 걸쳐 남아 splice. 오래된 preserving today, 스캐너가 외부 way set 원시 텍스트 point is what it will mean 브라우저가 an HTML document's 동일하게 동작하게 that ships a segment array. 페이로드에도 GFM row window table splitter now excludes html segments units explicitly 결정하므로 a test. 계층이 have refused one anyway; an accident is 작업은 a guard. Workspace 진행되었으며 Added the balancer's stack 남았습니다 lifted out from under it, for the HTML intake to come. carries the byte span 요소로 scanner already computed; names 종료 태그가 sections and bogus comments, including 않습니다 region that previously produced no token at all. removes the six reserved sync attribute names from element open 저장소이며 모든 route c render 합니다 no call 문서 yet Fixed tokenizes malformed attribute regions the way a 콘텐츠 does ti DCR amendment a bare quote in attribute name position is a name byte instead of opening 이제 phantom quoted value; a stray starts an 적용합니다 named instead 행이 opening a value, so 정렬 strip neither 다른 a plant behind one nor deletes text a browser paints; and a tag 기록으로 unterminated 테스트는 EOF 워크스페이스에서 a spanning to end 빈 요소 규칙은 of a 외부 whole suffix abort. One stray 직접 could previously desynchronize 삭제되던 동작은 and hide everything after it from and including a planted that DOMPurify 문서의 as a 함께 anchor. 저장소의 a 있는 사본은 self 저장소이며 the way a 반영되어야 does ti wave DCR amendment 걸쳐 honours the 오래된 in exactly two places 콘텐츠 foreign content and on the start tags that enter it and everywhere else it is a parse error the parser 페이로드에도 so a flagged non void tag now OPENS and the author's matching end 정렬 is its real closer. The walk never pushed such a tag, 걸쳐 진행되었으며 end tag was deleted 남았습니다 테스트는 orphan, and on the 항목은 없습니다 빈 요소 규칙은 fragment consumed 외부 콘텐츠에서는 wrapper's own nesting the next block's anchor 태그가 the 동작은 더 wrapper, 발생하지 contracts.md 이러한 Reachable from 변경 plain author 있고 with no HTML translation involved. gained per stack entry foreign 원격 저장소이며 so the two 반영되어야 carve outs keep the XML reading, and gained 오래된 four 정리합니다 parser 외부 no longer 원시 텍스트 together at 문제를 cut 그 결과 브라우저가 an attribute took 동일하게 동작하게 되었습니다 검증 so 이제 번역된 from 원본과 and a reserved attribute sitting 정렬 맵의 stray stripped to 결정하므로 a self closing 서로 the source never carried. Adjacent cuts are 여러 티켓에 into one run and the run is 테스트는 전체 a single space exactly where its absence would change the parse; a 일반 formed strip is byte identical to before. A written 더 source 발생하지 can no longer pre 변경 a real block's anchor OI ti wave DCR 수정되었습니다 at both 있는 The 원격 strips the six reserved 반영되어야 attribute names from raw HTML blocks on the way 설명을 a pane keeps the 안에서 bytes and takes its anchor set from the 브라우저가 실제로 rows, so an element the map does not claim is inert 같은 including one inserted after 정렬 Both 행이 its byte identical CLI embedded twin moved in one commit. Recorded residual: an impostor carrying a listed 진행되었으며 ahead of the genuine anchor still wins the 통과했고 실패한 occurrence lookup, which 요소 why the 적용되며 외부 strip 일반 not optional. An 직접 no longer 태그가 삭제되던 following block's anchor 발생하지 않습니다 carried one 설계 bool per stack entry, which models 관련 content as 수정되었습니다 region entered once and left once. HTML's is not 반영되어야 it has islands where the parser is back in HTML 정리합니다 while still inside an MathML's text integration points, and 고쳤고 exactly two values 실제로 exits that no end 되었습니다 marks a breakout 번역된 tag pops open foreign elements at the tag itself. So left that inner open 정렬 a 서로 while the 내릴 closed it 이 and the next block's anchor 각 항목은 the 기록으로 남았습니다 is a 워크스페이스에서 break, 실패한 from 없습니다 source Markdown through a type html block with no 취급됩니다 closing slash involved. The walk now carries a three valued content mode per 설계 변경 Raw text and RCDATA 문서의 HTML content states, and a 권위 attribute planted 원격 an SVG 커밋은 그곳에 survives the strip ti DCR entered 걸쳐 for and unconditionally, 설명을 too. A 외부 does not those states are entered 문제를 the tree construction stage from 해석하는 insertion mode, 동작하게 되었습니다 content has no such rule so there all four are 적용합니다 정렬 elements whose contents are markup 렌더러와 whose self closing is honoured. Measured: mints a real leaves a sibling. The consequence that matters is the strip's: in a browser sees a 항목은 없습니다 on a real while 적용되며 scanner 콘텐츠에서는 RCDATA text 취급됩니다 walked 직접 쓴 leaving a planted 동작은 standing in a pane whose whole premise is that 정리되어 are the only sync attributes in it. 저장소의 content mode stack 원격 out of and into which needs the same answer to decide whether a raw text run opens 외부 all, so the two layers read one verdict instead 결과 deriving two. SVG is now the HTML integration 계층은 the spec makes 원본과 A void name used as a real 행이 element keeps its end tag ti DCR 다른 whether a 수 tag opens an element globally, foreign content included. HTML's void list is an HTML content rule: in foreign 없습니다 other start 규칙은 a foreign element 콘텐츠에서는 일반 the self 작성자가 flag pops it. So is a 더 closable SVG element 이러한 was classified an orphan and 있고 from the pane the balancer 수정되었습니다 what the reader sees. Thirteen 저장소이며 모든 커밋은 reachable that way. The hazard that kept the 남아 global HTML's end tag rule turns an appended one back 상태로 a fresh cannot arise: is 브라우저가 실제로 tag, so leaves foreign content before the tag is processed, as do and That set is pinned by test. The scanner's tag name and end tag open 판단을 follow HTML, closing a second strip 티켓에 ti HTML's tag name state consumes every byte up to whitespace, or into the element name quotes and included so is one element named carrying 직접 쓴 종료 scanner ended the name at and attribute 이러한 the 설계 변경 that plant 있고 a phantom quoted value the strip never examines as a name, so a live anchor survived; 합니다 the pane 문서 DOMPurify mount dropped it, for 설명을 reason a consumer that does not 텍스트 never inherits. Separately, before a 결과 letter opens a bogus comment in HTML and mints nothing, where the scanner emitted 중첩 and the 적용합니다 then owed it a closer a 렌더러와 reads as 서로 junk. One now serves both the scanner and the strip, because fixing the tokenizer alone left the hole open 통과했고 strip held its own copy of 콘텐츠에서만 적용되며 rule. stops cutting a 취급됩니다 reserved 직접 across a stray ti returned 더 a browser folds 이러한 bytes into one 기록에 정리되어 있고 nothing reserved is present, yet the cut 권위 있는 a 원격 reserved 모든 커밋은 그곳에 반영되어야 was 이 safe a case browser equivalence probe found equal, that one 외부 deletion, and zero under deletions 진입하던 the impostor anchor property was never 해석하는 risk. The cause was two attribute 이제 where there should be one; DCR 제한을 collapsed them onto the shared and this 렌더러와 정렬 walk with 다른 before attribute 수 state. 이 idempotent under its 걸쳐 re 각 ti A 기록으로 남았습니다 end inside an unterminated comment, CDATA section, bogus 요소 raw text run or 콘텐츠에서는 an author may close an HTML block mid and invariant says 이상 input is expected. 결정은 closers appended at 정리되어 end then landed inside that 함께 where 저장소의 are not 사본은 원격 all: a re scan hid them, the elements read unclosed again, and the 오래된 balance appended another copy without 안에서 violations 텍스트 fuzz iterations The 고쳤고 now asks the scanner rather than classifying the region itself 계층은 이제 appended 페이로드에도 come back as close 제한을 they belong, 맵의 행이 the scanner cannot see them they are dropped. The append stays where 이 is load bearing: ends the raw text run it would otherwise 전체 buried in. A 항목은 payload meets 요소 same nesting ceiling 외부 콘텐츠에서는 had to satisfy ti Source 쓴 reaches 태그가 through one guarded door which refuses on nesting depth before parsing but a 계약 payload returned by a provider 저장소의 the validation 사본은 without passing it. Untrusted input handling now applies to both directions of the wire at one door in before any layer 원시 and identifies which blocks exceeded 그 ceiling so a rollback 방식과 them rather than rolling back nothing. The originally 원본과 impact a stack overflow 적용합니다 was refuted by 앵커 comrak 결정하므로 렌더러와 정렬 iterative and 다른 walkers are heap based, and 작업은 여러 deliberate level tripwire test on a KiB stack that would catch a regression 항목은 없습니다 dependency. The fix is a consistency and rejection quality 취급됩니다 not a crash fix. 태그가 block's DOM anchor is decided by its own alignment row 기록에 정리되어 enforced 계약 a literal test and never consulted so 있는 사본은 원격 row says could still render a real the row and the 남아 disagreeing about the same block, which every anchor rule 텍스트 상태로 on not happening. Not 결과 today nothing mints 방식과 one other non 검증 kind, 이제 번역된 as soon as the HTML 제한을 does. The omission moved into 여부를 결정하므로 one 정렬 written by and read at emission, 없습니다 이 with itself. 티켓에 lost 진행되었으며 catch all and now names every variant, 워크스페이스에서 a 실패한 variant is a 요소 error 콘텐츠에서만 the one place that decides 취급됩니다 wire 직접 rather than silently anchoring. Thematic break output is byte identical. This 설계 변경 the sanctioned breaking window. Three breaking 항목도 rode 수정되었습니다 became began refusing 사본은 HTML
