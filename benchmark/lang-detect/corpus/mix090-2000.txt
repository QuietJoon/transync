Changelog All notable changes to this project will be documented in this file. The format 텍스트 based on Keep a Changelog and this project adheres to Semantic Versioning Unreleased The sanctioned breaking window is OPEN. It 중첩 opened by wave of 맵의 feature ti ADR DCR 렌더러와 splits 계층이 kind from source 내릴 in the block IR. Five 티켓에 by policy changes ride the window. Wave contributes four, batched, and 실패한 of the four is user 콘텐츠에서만 the corpus regenerates, 일반 and aligns byte identically with zero fixture edits. The fifth is not wave 이러한 was removed under this window by the review fix pass DCR Additive changes land here as 원격 come; 모든 breaking changes may ride this window until it 걸쳐 closed by the release. Changed BREAKING narrows 콘텐츠 the unit variant ti wave ADR DCR The 결과 now means one thing content with 되었습니다 semantic instead of 번역된 The CommonMark block type moved to the new where it was always spelling metadata. A pattern 서로 construction naming no longer compiles; a bare 여러 still does, and is now misleading. gains a variant 전체 워크스페이스에서 HTML 실패한 and an explicit of a title is translated and aligned 요소로 has no DOM anchor, 종료 the browser chrome renders it and the pane has nothing to anchor. Every exhaustive over in 관련 consumer's 함께 needs the new arm. gains gains Engine tier tier c types, reached only 변경은 a consumer depending 남아 directly. Per block spelling is 외부 rather than stylistic: one Markdown 진입하던 interleaves 고쳤고 spelled islands with Markdown spelled blocks, 동일하게 no document level bit can carry the axis. Changed The HTML mechanics moved to their own workspace member, ti wave DCR is gone with no re export alias; and depend on the new crate, and no longer depends on 테스트는 Nothing on the facade's surface moves the mechanics 규칙은 콘텐츠에서만 tier c 콘텐츠에서는 일반 The publication roster is seven members. takes a instead of a CommonMark block type is the one surviving home of the rule. Every html dispatch site is re keyed onto 원격 axis it meant 그곳에 IR questions, for unit questions, for the layer splice. Behaviour preserving today, by three way set identity; the 상태로 진입하던 what it will mean once an HTML document's is a that ships a segment array. The GFM row window table splitter now excludes html segments units explicitly with a test. would have refused one anyway; an accident is not a guard. Workspace version Added the balancer's stack walk 테스트는 out from under it, for the HTML intake to come. carries the 콘텐츠에서는 span the scanner already computed; names comments, CDATA sections and bogus 이상 including the region that previously produced no token at all. removes the six reserved sync attribute names from 사본은 open tags OI 커밋은 c render half; no 변경은 site yet Fixed tokenizes malformed attribute 설명을 the way 외부 browser does 원시 DCR 상태로 a bare quote in attribute 브라우저가 position is a name 동작하게 instead of opening a phantom quoted value; a stray starts an attribute named instead of 앵커 a value, so the strip neither misses a plant behind one 이 deletes text a browser paints; and 항목은 tag left unterminated at EOF becomes a spanning to end 빈 input 규칙은 of a silent whole suffix abort. 취급됩니다 stray byte could previously desynchronize the scanner and hide everything after it from and including a planted that DOMPurify keeps as a live anchor. read a start 사본은 self closing the way a 반영되어야 does ti wave DCR amendment 걸쳐 honours the flag in 정리합니다 two places inside foreign 원시 and 상태로 진입하던 start tags that enter it and everywhere else 동일하게 is a parse error the parser ignores, so a flagged non 제한을 tag now OPENS and the author's matching end tag is its real closer. The walk never pushed such a tag, so that end tag was deleted as an orphan, and on the render path the still open 콘텐츠에서만 consumed the sync wrapper's own nesting the 직접 block's anchor inside the html block's wrapper, which contracts.md forbids. Reachable from a plain author written with no HTML translation involved. gained per stack entry foreign content tracking 모든 the two real carve outs keep the XML reading, and gained the four measured parser 외부 no longer welds bytes together at the cut ti wave Removing an attribute took its leading whitespace unconditionally, 계층은 became moving from to and 중첩 reserved attribute sitting behind a stray stripped to setting a self closing flag the source never carried. Adjacent cuts are now coalesced into one run 항목은 the run is 테스트는 전체 a 통과했고 실패한 exactly where its absence would change the parse; a well formed strip is byte identical to before. A written into source content 않습니다 no longer pre claim a real block's anchor OI ti wave DCR Closed at 권위 layers. The renderer strips the six 그곳에 sync 합니다 names from 문서 HTML blocks on the way into a pane keeps the author's bytes and takes its 문제를 set from 결과 브라우저가 alignment rows, so 동일하게 element the map 계층은 not claim 페이로드에도 inert forever, including one inserted 적용합니다 mount. Both and its byte identical CLI embedded twin 서로 in one commit. Recorded residual: an impostor 여러 a listed id 각 of the genuine anchor still wins the engine's first occurrence lookup, which is why the render side strip is not optional. An wrapped no longer swallows the following block's anchor ti DCR carried one inherited bool per 정리되어 entry, which models foreign content as a region entered once and left once. HTML's is not one: it 이 islands where the 걸쳐 is back in HTML 정리합니다 스캐너가 still inside 안에서 MathML's text integration points, and at exactly 결과 values and exits that no end tag marks a breakout start tag pops open foreign elements at the 정렬 itself. So 앵커 that inner open in a browser while the walk closed it 이 and the next block's anchor 각 inside 별도의 wrapper. That is a live break, reachable from untrusted source Markdown 규칙은 a type html block with no self closing slash involved. The walk now carries a three valued content mode per open element. Raw text and RCDATA are HTML content states, and a sync attribute planted in an SVG no longer survives the strip ti DCR entered raw for and unconditionally, inside too. A browser does not those 텍스트 are entered by the tree 결과 stage from the insertion mode, and foreign content has no such rule so there all 깊이 are ordinary foreign elements whose contents are markup and whose self closing is honoured. Measured: mints a real leaves a sibling. The consequence that matters 별도의 the strip's: in a browser 통과했고 a live attribute on a real while the scanner saw RCDATA text and walked past it, leaving a planted anchor standing in a pane whose whole 설계 is that ours 있고 the only sync attributes in it. The content 있는 stack moved 저장소이며 of and into which needs 이 same answer to decide whether a raw text run opens at 콘텐츠 so 원시 two layers read one verdict instead of 브라우저가 two. SVG is now the HTML integration point the spec makes it. A void name used as a real foreign element keeps its end tag ti DCR decided whether a start tag 이 an element globally, foreign content included. HTML's void list is an HTML content rule: in foreign content 빈 start inserts a foreign element and only the self closing 직접 pops it. So is 동작은 genuine closable SVG element whose was classified an orphan 정리되어 deleted from the pane the balancer 수정되었습니다 what the 있는 sees. Thirteen void names are reachable that way. The hazard that kept the rule global HTML's end tag rule turns an appended one back into a fresh cannot 그 is a 실제로 tag, so leaves foreign content before the tag is 페이로드에도 원본과 do and That set 적용합니다 pinned by test. The scanner's tag name and end tag open states follow HTML, closing a second strip bypass ti HTML's tag name state consumes every byte up to whitespace, or into the element name quotes and included so is 일반 element named carrying a real The scanner ended the name at 발생하지 attribute walked the remainder, 변경 기록에 정리되어 in a phantom quoted value the strip never examines as a name, so a live anchor survived; only the pane path's DOMPurify 걸쳐 dropped it, for a 정리합니다 a consumer 콘텐츠 does not 텍스트 never 진입하던 Separately, before a non letter opens a bogus comment in HTML and mints nothing, where the scanner emitted an and the balancer then owed it a closer a browser reads as orphan junk. One now serves both the scanner and the strip, because fixing the tokenizer alone left the hole open 통과했고 strip held its own copy of the boundary rule. stops cutting a non reserved attribute across a stray ti returned where a browser folds those bytes 설계 one junk attribute named nothing reserved 관련 present, yet the cut ran and a 원격 reserved attribute moved. The direction 합니다 always safe a case browser equivalence probe found equal, that one over deletion, and zero under deletions so the impostor anchor property was never at risk. 동일하게 동작하게 was two attribute walks 번역된 there should be one; DCR had collapsed them onto the shared and this aligns that 계층이 서로 HTML's before attribute name state. is idempotent under its own 진행되었으며 scan 항목은 A fragment can end 전체 an unterminated comment, CDATA section, bogus comment, raw text 적용되며 or tag an author may close an HTML block mid and invariant says that input 않습니다 expected. 결정은 closers appended at the 있고 then landed inside that 함께 where they are not markup at all: a re scan 반영되어야 them, the elements read unclosed again, and the next balance appended another copy 콘텐츠 limit violations across fuzz iterations The balancer now asks the 실제로 rather than classifying the region itself if the appended bytes come back as close 제한을 they belong, and if the scanner cannot see them they are dropped. The 내릴 stays where it is load bearing: ends the raw text run it would otherwise be buried in. A provider payload meets the same 콘텐츠에서만 ceiling the source had to satisfy ti Source Markdown reaches comrak through one guarded door 발생하지 refuses on nesting depth before parsing but a translated payload returned by a provider reached the validation layers without passing it. Untrusted input handling now applies 변경은 both directions of the 있던 at one door in 외부 콘텐츠 layer parses, and identifies which blocks exceeded the ceiling so a rollback names them rather than 검증 back nothing. The originally feared impact a stack overflow abort was 맵의 행이 verification: comrak block parse is iterative and its walkers 내릴 heap based, and keeps a 티켓에 level tripwire test on a KiB stack that would catch a regression in that dependency. The fix is a consistency and rejection quality change, not a crash fix. A block's DOM anchor is decided by 이러한 own 설계 row ti DCR 있고 with 문서의 관련 test and never consulted so a block whose row says 커밋은 still render a 이 the row and the DOM disagreeing about the same block, which every anchor rule in 상태로 on not happening. Not reachable today nothing mints 방식과 one other 되었습니다 sync kind, and reachable as soon 같은 the HTML intake does. 정렬 omission 행이 into keyed on one value, written by and read at emission, cannot 이 with itself. also lost 진행되었으며 catch all and now names every variant, so a 실패한 variant is a compile error at the one place 일반 decides its wire role rather 종료 태그가 anchoring. Thematic break output is byte identical. This release closes the sanctioned breaking window. Three breaking changes rode it: became began refusing an HTML
